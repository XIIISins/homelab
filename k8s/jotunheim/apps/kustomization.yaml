# k8s/jotunheim/apps/kustomization.yaml
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # Shared monitoring namespace — both Phase-7 apps live here.
  - monitoring-namespace
  # Phase 7 — observability stack. Native vmui + VL UI are used
  # for dashboards; Grafana was dropped from scope (see decisions.md).
  - victoriametrics
  - victorialogs
