# k8s/asgard/apps/teamspeak/kustomization.yaml
#
# NOT YET wired into k8s/asgard/apps/kustomization.yaml — adding the
# `teamspeak` entry there is the Flux deploy trigger. Until then this
# directory is dormant.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - externalsecret.yaml
  - statefulset.yaml
  - service-voice.yaml
  - service-filetransfer.yaml
  - service-query.yaml
