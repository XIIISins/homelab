# ansible/roles/hermod-api/handlers/main.yml
---
- name: reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: restart apprise-api
  ansible.builtin.systemd:
    name: apprise-api
    state: restarted
