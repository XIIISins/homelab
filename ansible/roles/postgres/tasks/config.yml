---
# TODO Pass 2: template postgresql.conf from defaults; notify restart.
- name: postgres/config — Pass 1 stub
  ansible.builtin.debug:
    msg: "Pass 1 stub: postgres/config.yml will be written in Pass 2."
