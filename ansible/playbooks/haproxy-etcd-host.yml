# ansible/playbooks/haproxy-etcd-host.yml
---
- name: Configure HAProxy/etcd host
  hosts: haproxy_etcd_hosts
  become: true

  roles:
    - role: baseline
      tags: [baseline]
    - role: hardening
      tags: [hardening]
