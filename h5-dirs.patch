--- a/ansible/roles/factorio/tasks/dirs.yml
+++ b/ansible/roles/factorio/tasks/dirs.yml
@@ -33,3 +33,11 @@
     owner: root
     group: root
     mode: "0755"
+
+- name: Create secrets directory (outside /factorio/ — not in operator's SFTP tree)
+  ansible.builtin.file:
+    path: "{{ factorio_secrets_dir }}"
+    state: directory
+    owner: root
+    group: "{{ factorio_group }}"
+    mode: "0750"
