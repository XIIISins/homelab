<!-- docs/incidents/README.md -->

# Incident log

Per-incident retrospectives. Each entry: trigger → diagnosis → resolution → root-cause pattern. Lessons from these incidents are encoded as rules in [`CLAUDE.md`](../../CLAUDE.md) "Known gotchas" and decision rows in [`docs/operations/decisions.md`](../operations/decisions.md).

## By date

| Date | Phase | Summary | File |
|------|-------|---------|------|
| 2026-05-14 | pre-rebuild | etcd storm cascade (≈15h). Adding worker eth1 + reboot triggered etcd IO storm on old Urd, exposed 6 latent classes (Calico autodetection, rp_filter, iSCSI session pinning, MetalLB L2 election, MGMT subnet doc error, tigera SELinux). | [etcd-storm-cascade](2026-05-14-etcd-storm-cascade.md) |
| 2026-05-15 | 5c carry-over | tigera-operator SELinux denial on `/var/lib/calico/mtu` (upstream #7851). Fixed via explicit `mtu: 1450` in Calico Installation CR. | [tigera-operator-selinux](2026-05-15-tigera-operator-selinux.md) |
| 2026-05-16 | 5f | Factorio LXC end-to-end deploy. 7 bugs in factorio/sftpgo roles + minimal-template gaps fixed in-session. | [factorio-deploy](2026-05-16-factorio-deploy.md) |
| 2026-05-17 | 5c teardown+rebuild | Deliberate asgard teardown + rebuild. 9 architectural findings closed (per-component config Kustomizations, sealed-keys backup, idempotency, multi-homed networking, CP rebuild override, restart-handler safety). | [asgard-rebuild](2026-05-17-asgard-rebuild.md) |
| 2026-05-17 evening | 5e | Authentik + Redis deploy. First real K8s workload on rebuilt cluster; surfaced DNS resolv.conf class, env-vs-values precedence, CP-sizing-too-small, brand uniqueness, and more. | [authentik-redis](2026-05-17-evening-authentik-redis.md) |
| 2026-05-21 | Phase 4a + Urd refresh | Urd hardware refresh (MSI Cubi) + CP NoSchedule taint deploy. Surfaced CSI eviction footgun, floating-Helm-pin outages (metallb 0.16, synology-csi 0.11.2), ext4 journal abort under iSCSI timeout. | [urd-refresh-phase-4a](2026-05-21-urd-refresh-phase-4a.md) |
| 2026-05-21 evening | Phase 4a cleanup | Hostkey zero-byte from NUC7 crash mid-write, iSCSI orphan-record reconciliation, Authentik restart-pattern triage. | [phase-4a-cleanup](2026-05-21-evening-phase-4a-cleanup.md) |
| 2026-05-22 | Phase 4b + worker rebuild | Göndul Verd → Urd migration; einherjar-urd worker rebuild. Surfaced orphan-LVs class, iproute 6.17 missing `/etc/iproute2/rt_tables`, Vault chart required pod anti-affinity. | [phase-4b-worker-rebuild](2026-05-22-phase-4b-worker-rebuild.md) |
| 2026-05-22 evening | Phase 5e.1 | Traefik + Gateway API + cert-manager + Authentik HTTPS cutover. 8 findings (Traefik v39+ syntax, Gateway listener-port matching, NET_BIND_SERVICE, anti-affinity + maxSurge deadlock, Helm rollback-removes-values, MetalLB no-ICMP, schema validation gaps). | [phase-5e1-traefik](2026-05-22-evening-phase-5e1-traefik.md) |
| 2026-05-23 | Phase 5e.2 | HTTPRoute migration to midgard Gateway initially missed at 5e.2.f close; surfaced 12h later by Terraform reachability check. | [phase-5e2-httproute-migration](2026-05-23-phase-5e2-httproute-migration.md) |
| 2026-05-23 | post-5e.3 | Authentik PG DNS resolution flapping recurrence (58 restarts/24h). Symptom-mitigated by IP-stopgap pointing at Fulla `10.0.11.230`; underlying class still open. | [authentik-pg-dns-flapping](2026-05-23-authentik-pg-dns-flapping.md) |
| 2026-05-24 | post-5e.3 | Cloudflare zone re-harden after 5e.3.c global-disable. Per-path WAF skip ruleset for protocol-public endpoints; surfaced provider v5 import discriminator + per-app OIDC discovery path. | [cloudflare-reharden](2026-05-24-cloudflare-reharden.md) |
| 2026-05-23 | 5e.3.c | Cloudflare bot protections blocked Tailscale WebFinger probe (403). Mitigated by global-disable initially; properly fixed in 2026-05-24 entry above. | [cloudflare-bot-webfinger](2026-05-23-cloudflare-bot-webfinger.md) |
| 2026-05-24 | 5g.2 close | HAProxy + keepalived VIP `10.0.10.210` + Authentik cutover + failover validation. Generic data-driven roles built. 5 findings: bg-worktree vs local TF state, VRID collision with AGH (spacing-by-10 convention), pg_hba `replication` separate from `all`, `systemctl kill` doesn't actually kill patroni, fast-catchup vs forced-rewind framing. | [5g2-haproxy-keepalived-vip](2026-05-24-5g2-haproxy-keepalived-vip.md) |
| 2026-05-24 | 5i close | NetBox IPAM/DCIM live in asgard K3s. 10 findings spanning a chart-deploy iteration: `tasks_from:` silently ignored in `roles:` block (→ postgres-common role split), Valkey replication+iSCSI fsGroup, RWO media PVC shared between server+worker, existingSecret projects all keys without optional, `small` preset OOMs migrations, in-cluster MetalLB-VIP class generalised via CoreDNS rewrite (`.override` vs `.server` keys), extraConfig.secret doesn't propagate → use extraVolumes+subPath, OIDC group sync not automatic. TF→NetBox direction decided (Option A, e-breuninger/netbox provider, tight coupling); implementation Phase 5i.3 immediate next, not deferred. | [5i-netbox](2026-05-24-5i-netbox.md) |
| 2026-05-24 | 5i.3 | TF→NetBox standing pattern retrofit. ~160 records imported via TF 1.5+ `import {}` blocks across 8 files (sites, clusters, manufacturers, device types, roles, VLANs, prefixes, devices, VMs, IPs, VIPs, tags, custom fields). 3 NetBox-side placement corrections (PBS, idunn, Gjallarbru). 2 upstream blockers: chart-deploy `API_TOKEN_PEPPERS` gap (added to ExternalSecret), provider integer-custom_fields issue #349 (VMID recreated as text). Auth model pivoted from dedicated `terraform` user (provider can't write tokens for v2 NetBox 4.4+) to admin token throughout. | [5i3-tf-netbox-retrofit](2026-05-24-5i3-tf-netbox-retrofit.md) |
| 2026-05-24 | 5i.3 (embedded) | Worker OOM cascade during NetBox rolling restart. einherjar-verd hit load 40 + no memory; kubelet stopped posting. Once pod migrated, next worker OOMed the same way. Resolved by sequential `terraform apply -target=` memory bump 4G→8G across all 3 workers (bpg/proxmox in-place update + reboot, no recreate). New worker memory baseline. | [worker-oom-during-netbox-rolling-restart](2026-05-24-worker-oom-during-netbox-rolling-restart.md) |
| 2026-05-24/25 | Phase 7a close | End-to-end VL+VM stack deploy. 12 findings: VL fsGroup recurrence on iSCSI (chart key `server.initContainers`), vlagent is CLI-flag-only (no YAML), Debian 13 trixie ships no rsyslog, vlagent `/insert/native` (not jsonline), K3s host net can't reach MetalLB VIPs (companion ClusterIP), RHEL SELinux requires privileged on log-collector DaemonSet, Authentik embedded outpost `config.authentik_host` bypasses brand resolution, vmagent default RBAC missing `nodes/proxy`, vmui `unit` field is label-suffix not formatter, cAdvisor groups by `instance` not `node`, PBS apt 401 without subscription, VL HelmRelease pending-install lockout. 23 off-cluster hosts shipping logs. 4 vmui dashboards live. Phase 7b vm-operator migration deferred. | [phase-7-observability](2026-05-24-phase-7-observability.md) |
| 2026-05-25 | post-5b.2 | Terraform state migrated 8 modules → S3 (`xiiisins-homelab-tfstate`, 1.10+ `use_lockfile`). `terraform/aws/` stays local (chicken-egg). 5 findings: `terraform init -migrate-state` vs `-input=false` conflict, multi-module migration pattern, `Bash` tool calls don't inherit interactive shell PATH, pipe-induced subshell hides env exports, **dual-syntax redaction discipline** (bash `KEY=val` + fish `set -gx KEY 'val'` need different regexes — surfaced as a NetBox admin API token leak via fish-line miss; operator rotated). | [tfstate-s3-migration](2026-05-25-tfstate-s3-migration.md) |
| 2026-05-25 | 7c.2 + 7c.7/8 + Round 2 close | Zabbix server LXC (Hugin, 1102 on Urd) → cluster-wide agent rollout (23 LXCs/VMs/hypervisors) → declarative host taxonomy + Round 2 per-service depth (PG/HAProxy/Etcd/PVE/Nginx/PHP-FPM templates). 7c.2 retro covers schema-bootstrap no_log opacity, Admin/zabbix factory password surprise + 5-attempt lockout SQL unlock, Debian nginx default-site shadow, Hugin/Munin naming pair. 7c.7/8 chapter adds 13 more rule-shaped findings: OS-aware agent install (Debian apt + RHEL 9 dnf), community.zabbix 4.x httpapi connection plugin (not env vars), `become: false` on delegate_to localhost (sudo prompt), Zabbix host.create concurrency race (throttle: 1), Super admin role requirement, `zabbix_group` module name, inline Vault lookups over run_once-on-localhost set_fact, group_vars/all/ for pre-task vars, `apply: tags:` on include_tasks, bpg/proxmox user_token `value` is full prefixed string, postgresql_user has no `groups` arg → postgresql_membership, agent2 plugin packages separate (zabbix-agent2-plugin-postgresql), PG 17 split pg_stat_bgwriter view, shell-var interpolation through ansible-adhoc as secret leak vector. 3478/3521 (98.8%) items OK post-deploy. | [zabbix-server-deploy](2026-05-25-zabbix-server-deploy.md) |
| 2026-05-25 | 5h.2 partial | Hermod notifications hub deploy (LXC 1103 on Verd). Native AppriseAPI (pip+venv+gunicorn+systemd, no Docker — explicitly avoiding the 5aff1dd rolled-back Docker-in-LXC attempt). Caddy reverse-proxy with `remote_ip` allowlist matcher gates :80; AppriseAPI bound 127.0.0.1:8000. Four Discord channels (Hrist/Mist/Ölrún/Hel) for `critical`/`alert`/`media`/untagged routing. Smoketest matrix passed end-to-end. Closes 5h.2.a-h + 5h.2.k; 5h.2.i (Zabbix media-type) + 5h.2.j still pending. 9 findings: Apprise YAML schema (URL-as-dict-key, not URL-as-string-with-sibling-fields), Caddy /var/log/caddy ownership recursion (stale root-owned file breaks startup), Cloudsmith stable rolls forward + drops old versions (exact pin without wildcard + `allow_downgrade: true`), config-key leaks via `/notify/<key>` URI in Caddy + gunicorn access logs to VL (acceptable — Caddy IP allowlist is the real gate), vlagent role input schema is `{path, service}` not `{glob, extra_fields}`, NetBox transient OperationalError on apply state-refresh, Discord webhook URL leak event (operator rotated three webhooks), stale SSH hostkey from rolled-back 5aff1dd at same IP, NetBox admin API token leak via `grep` printing the line (operator rotated). | [hermod-deploy](2026-05-25-hermod-deploy.md) |
| 2026-05-26 | 5h.2.i + 5h.2.j | Hermod producer wiring — Zabbix webhook media-type (declarative via `community.zabbix.zabbix_mediatype` + `zabbix_user`) and Patroni `on_role_change` bash callback on all 3 PG nodes. Closes the producer-side gap from the 5h.2 hub deploy; Hermod now receives alerts from its first two concrete producers without operator-in-the-loop. 5 findings: three `community.zabbix.zabbix_mediatype` schema quirks (`recovery: operations` not `operation`, `eventsource` not `event_source`, `webhook_params` not `parameters`); `zabbix_user` "create-OR-update" semantics require `usrgrps` + `passwd` even on pure media updates (paired with `override_passwd: false` for reconcile safety); `include_tasks` tag-propagation gotcha re-occurred AND surfaced a pre-existing bug on the patroni-config include — generalised rule reinforced; auto-mode classifier correctly blocked a live `patronictl switchover` test (script-level coverage proves the callback path; live failover validation deferred to operator); Patroni callbacks must always exit 0 to avoid deadlocking the supervisor loop. | [hermod-producer-wiring](2026-05-26-hermod-producer-wiring.md) |
| 2026-05-25 | 5g close | Teamspeak K3s deploy (LXC pivot). TS3 3.13.7 StatefulSet on asgard, PG via Patroni VIP, MetalLB shared VIP `10.0.20.12` for UDP voice + TCP filetransfer, `ETP=Local` for source-IP preservation. End-to-end validated (external client via existing SRV ring). 4 findings: chown init container needs default capabilities (drop ALL strips CAP_CHOWN/CAP_FOWNER even as UID 0), StatefulSet RollingUpdate won't replace a CrashLoopBackOff pod (manual `kubectl delete pod` to break deadlock), Ansible `include_tasks` doesn't propagate `--tags` to inner tasks (workaround: skip-tags heavy roles + pass facts via `-e '{"key":true}'` JSON to satisfy strict-mode booleans), MetalLB IPAddressPool static/dynamic collision risk (Vault UI auto-allocated `.11`; static neighbor `.12` chosen with comment explaining why; longer-term: pin Vault + carve pool into static/dynamic bands). | [teamspeak-k3s](2026-05-25-teamspeak-k3s.md) |
| 2026-05-26 | 8c.3–8c.6 close | Zabbix SAML SSO deploy. Authentik native SAML 2.0 against Zabbix 7.0 on Hugin (LXC 1102). All 3 documented validation paths working (WAN apex / LAN apex / backdoor). 7 first-deploy fix commits: `provision_groups[].role` not `role_name` (community.zabbix 4.x), SAML fields flat at module level (not nested in `saml:`), `disabled_usrgroup` required with JIT, `web-config.yml` template-render tasks needed `zabbix:saml` tag to re-render conf.php + nginx.conf, nginx X-Forwarded-Proto → HTTPS reflection for ACS URL scheme, `sign_response = true` on `authentik_provider_saml` (default false; Zabbix's `sign_messages: true` requires signed Response), `username_attribute` must match an actually-attached property mapping's `saml_name` (default `.../uid` referred to an unattached mapping → switched to UPN's `xmlsoap.org/.../upn`). Plus: `authentik_provider_saml` URL attrs return `"-"` until `terraform apply -refresh-only`; computed URLs use default-brand apex hostname not `issuer` (cosmetic, both reach same backend). Single-ACS-URL limitation re-confirmed (midgard alias unusable for SAML, by design). | [zabbix-saml-deploy](2026-05-26-zabbix-saml-deploy.md) |
| 2026-05-26 | 5j close | Outline wiki + Garage S3 deploy. First homelab S3 layer (Garage v2.3.0 single-node, RF=1, dangerous-consistency, LMDB, 10Gi+200Gi iSCSI) + Outline 1.8.0-1 wiki on K3s consuming Patroni PG + Garage S3 + Authentik OIDC + sidecar Redis. 16 findings: dxflrs/garage is FROM scratch (no shell, drive logic via admin API), Garage bootstrap chicken-and-egg (readiness probe + Service DNS publish — fixed via publishNotReadyAddresses), mkfs.ext4 on 200Gi iSCSI takes ~20min (TRIM-bound), Synology DS223J 10/10 LUN cap (outline-redis → emptyDir), `outlinewiki/outline` registry path (ghcr 403s), runAsNonRoot+named-USER admission failure (explicit numeric UID required), pg-connection-string treats sslmode=require as verify-full (no-verify needed for self-signed Patroni), ExternalSecret all-or-nothing on multi-path bundle (split by resolve-time-cohort), postgres-common needs current Patroni leader (Idunn at deploy, not hardcoded Fulla), Jobs are immutable (kubectl delete required for spec edits), subPath ConfigMap doesn't hot-reload (cloudflared restart), jkossis/garage v1.0.4 schema (endpoint single-field, global_alias single-string, .id is access_key_id), CF token rotations invisible to consumers (/user/tokens/verify), VAULT_TOKEN expires mid-deploy (set-vault-token root), Authentik per-app group binding gate ("Policy binding 'None' returned result 'False'" if not in group), include_tasks + strict-mode bool conditional re-affirmed. | [outline-garage-deploy](2026-05-26-outline-garage-deploy.md) |
| 2026-05-27 | 5h.3 close | Semaphore + drift-check baseline. 66 commits across the phase (~20 of which are the `--check` mode shakeout). Semaphore K3s StatefulSet (sqlite on iSCSI, inventory-cache on emptyDir LUN-cap workaround), three core templates (refresh-netbox-inventory cron 4h, asgard-drift-check cron 6h, asgard-apply manual), Hermod webhook + Hermod-summary callback plugin. Per-host-group playbook restructure (`asgard-<group>.yml`) + `site.yml` orchestrator. NetBox dynamic inventory live with three-layer defence against the silent-success class (refresh-script retries on transient NetBox 500s, 24h cache TTL > 4h refresh cadence, preflight host-count assert in site.yml). `os-updates` role split out of baseline. Drift-check baseline is zero-changes across the fleet. 12 findings: Semaphore pod-start pip-install pattern (hvac/psycopg2-binary/pytz), undocumented Semaphore config schema (SEMAPHORE_DB_OPTIONS JSON for SSL, enableServiceLinks=false for env collisions, ANSIBLE_HASHI_VAULT_ADDR not _URL, vaults.password.vault_key_id nested), **NetBox dynamic-inventory silent-success on cold cache + transient 500** (generalisable to any CI runner with exit-code-only success criterion), inventory-file vs group_vars isolation (skip vault decrypt at parse-time), keyed_groups tag rename + revert, group renames per role-class (factorio→gameserver), inventory file name-case discipline, UCG-Ultra excluded from dynamic inventory, the `--check` mode caveats catalogue (5 sub-classes: download/extract, service enable+start, mutating API read-back, leader-gated provisioning, fresh-install-only paths), drift-check wrapper preflight-asserts `--check` to prevent accidental mutating re-run, `os-updates` role split rationale, `vault kv patch field=-` stdin unreliable (paired with operational AWS auto-unseal IAM rotation in same window). | [5h3-semaphore-drift-check](2026-05-27-5h3-semaphore-drift-check.md) |
| 2026-05-30 | storage redesign close | Synology storage redesign completion + Volume1 delete/recreate + a Tailscale-broke-the-LAN outage. Class L (Vault iSCSI→local-path, per-node remove→wipe→join, gated on autopilot voter-promotion/failure-tolerance-1, leader last), Class M (garage-meta iSCSI→Volume2 iSCSI, identity-preserving rsync static-rebind), Stage 3 (Volume1 delete+recreate as the dedicated backup volume, PBS datastore moved back + restore-verified). 8 findings: Vault raft node-replace dance (no retry_join → manual join + non-voter gate), `raft snapshot save` is the right backup not a kv-dump, Class-M = identity-preserving meta copy, the **5-holder Synology "volume won't delete" hunt** (iSCSI LUN → package dir → masked-but-respawning `s2s_daemon` → kernel-thread `nfsd` stale-PBS-lock → invisible `Synotify`/fanotify mark; reboot is the catch-all; DSM ships no lsof/fuser so use `/proc` + `dmesg VFS` lines), headless-NAS break-glass via `tailscale down` over the tailnet IP, **Tailscale subnet-router cut the NAS's own LAN** (advertise-supernet-that-contains-self + accept-routes/exit-node; fix `--accept-routes=false --reset`; root cause = web-UI auth not CLI authkey), NAS-reboot bounces the one iSCSI target (quiesce garage first → came back rw not RO), `configure-host` boot-task wiped on package reinstall. Zero data loss; LUN cap 10/10 → 1/10. | [storage-redesign-nas-rework](2026-05-30-storage-redesign-nas-rework.md) |
| 2026-05-31 | 6 Stage 2 close | Frigg Stage 2 finish — deferred roles (Tailscale-SSH/vlagent/zabbix-agent) deployed from Frigg-as-Linux-controller (`-c local`, static inventory; the macOS/3.14 hvac-fork crash doesn't apply on Linux), HA validated (live-migrate zero-downtime, graceful `qm stop`, true `kill -9` qemu crash test → LRM auto-restart ~6s). 5 findings: **Tailscale `accept-dns=true` hijacked the control node's DNS** (overwrote baseline AdGuard resolv.conf with MagicDNS → `server misbehaving` after reboot → Vault unresolvable → homelab-env dead; fixed `tailscale_accept_dns: false` role flag + control group_vars, reboot-persistent; exposed only by the HA crash-test reboot), `qm stop` on an HA resource is a graceful stop not a crash (real crash test = `kill -9` the qemu PID), NetBox dynamic inventory needs `python3-pytz`+`python3-pynetbox` on Frigg (now in toolchain), new Zabbix host class needs its host group created first (`zabbix-host-groups.yml --limit frigg`), hardening sshd template dropped `Subsystem sftp` (re-stated `internal-sftp`). | [frigg-stage2-finish](2026-05-31-frigg-stage2-finish.md) |
| 2026-06-10 | post-5h.3 | Semaphore ran image-bundled `community.zabbix 2.3.1` (not the pinned 4.2.0) → `zabbix-agent` register task failed from Semaphore, passed from Frigg. Root cause: Semaphore never galaxy-installs `requirements.yml` + vendored collections are gitignored, so a pin is only real where installed. Fixed via the homelab's first custom GHCR image (`ghcr.io/xiiisins/semaphore-homelab`). 8 findings: pin-only-real-where-installed, "transient" mis-call on a deterministic controller diff, no_log forensics + stdin-token repro, **r1 false-positive verification** (kubectl-exec env ≠ Semaphore task env — installed to /usr/share + pod-env var, never reached the task), r2 right-channel (install into ansible venv site-packages, matching MacBook/Frigg), `ansible-galaxy list`/raw-import bypass the finder (use `_AnsibleCollectionFinder`), `allow_override_args_in_task=false` ignores the API `limit`, repo went public mid-work. | [semaphore-collection-skew](2026-06-10-semaphore-collection-skew.md) |
| 2026-06-27 | post-1.0 | Estate-wide security headers via Traefik `websecure` entrypoint middleware (HSTS/nosniff/Referrer-Policy + strip Server/X-Powered-By), prompted by a `paste.xiiisins.com` scan. Deploy surfaced a 33-day latent bug: update-strategy set under the chart-ignored `deployment.strategy` (not root `updateStrategy`) → live `maxSurge:1` deadlocks the roll against per-node anti-affinity. No outage. 4 findings: entrypoint middleware is the only apply-to-all path + **confirmed to cover Gateway-API routers**, chart `updateStrategy` key, "succeeded Helm upgrade ≠ values honored", classifier correctly blocked a live kubectl patch. | [traefik-security-headers](2026-06-27-traefik-security-headers.md) |
| 2026-07-17 → 07-20 | post-1.0 | **OPEN — Skuld repeated hard crashes.** Three instant unclean losses (07-17 ~16:22, 07-17 19:29, 07-20 11:52) with no panic/OOM/lockup/trace in any. Crashes 1–2 each left an ACPI **BERT fatal hardware-error record** (CPER `81212a96-…` Firmware Error Record, severity 0x01); Urd+Verd on identical Cubi hardware + identical `7.0.6-2-pve` kernel have zero BERT records ever → **Skuld-specific hardware**. Ruled out: thermal (54 °C), NVMe (SMART clean), kernel/fleet-wide, workload. Leading suspect **non-ECC generic DIMMs**. Crash 3 diverges — no BERT, rasdaemon captured nothing, ~5h wedge needing manual power-cycle (possible distinct hang path). Mitigated: `rasdaemon` installed+enabled for component attribution; PG primary confirmed off Skuld (Patroni had already auto-failed `idunn` to Verd — no manual switchover needed). **Root cause unconfirmed; memtest86+ pass outstanding, BIOS 8.80 update unchecked.** | [skuld-hard-crashes](2026-07-17-skuld-hard-crashes.md) |
| 2026-06-18 | post-1.0 | LXC reboot-persistence drift — the PVE-host-patch reboot hit every LXC and `asgard-drift-check` reported `changed=48`, **LXC-only** (VMs/bare-metal clean). Four latent persistence bugs that only manifest on container restart: hardened `net.*` sysctls reset by interface bring-up after `systemd-sysctl` (fix: dedicated drop-in + `niflheim-sysctl-reapply` oneshot `After=network-online`); `vlan10-policy-routing.service` failed the boot race before the interface was addressed (fix: `ExecStartPre` gateway-readiness poll); `/etc/default/locale` is a `systemd-tmpfiles` `L+` symlink recreated each boot (fix: manage `/etc/locale.conf`); PVE rewrites `/etc/resolv.conf` from container config each start (fix: own DNS at the provision layer — TF `dns {}` block per LXC + `baseline_manage_resolv_conf: false`, applied in-place 0-replace). Separate unrelated finding: the upstream SFTPGo apt repo dropped its signed trixie `InRelease`, failing `apt update` on factorio (the lone `asgard-apply` error). Validated: saga double-reboot → 0 drift; resolv via saga+hermod reboots. | [lxc-reboot-persistence-drift](2026-06-18-lxc-reboot-persistence-drift.md) |
