<!-- docs/operations/decisions.md -->

# Key decisions log

| Decision | Choice | Reason |
|----------|--------|--------|
| Orchestrator | K3s only | Single orchestrator |
| Two K3s clusters | Asgard + Jotunheim | Separation by failure-domain risk, not by maturity — both host production services |
| Asgard K3s contents | Core infrastructure + automation + production services | See [Asgard K3s cluster](../services/asgard-k3s.md) section for current planned list |
| Cluster split criterion | Cascade-failure OR recovery-blocking criterion for asgard | Earlier "cascade only" framing missed automation (recovery-blocking) and daily-use services (user-blocking). Not production-vs-experimental — both clusters host real services |
| GitOps | Flux CD | Terminal-native |
| Flux structure | infrastructure + per-component config Kustomizations | CRD timing — CRDs must exist before dependent resources; per-component split avoids shared failure domains |
| Terraform GitOps | Tofu Controller (flux-iac org) | Flux-native, push-to-main matches the existing Flux model. Atlantis rejected — PR-flow friction not worth it for solo dev |
| Identity | Authentik (asgard K3s) | OIDC + LDAP, cascade risk |
| DNS | AdGuard Home (not Pi-hole) | More polished, fully free, self-hosted sync |
| Galera | Dropped | Nothing requires MySQL — all on PostgreSQL |
| Database | PostgreSQL LXC cluster only | Zabbix migrated to PostgreSQL |
| IPAM | Netbox (replaces phpIPAM) | Enterprise standard, API-driven |
| Log aggregation | VictoriaLogs | Better performance, lower resources than Loki |
| Metrics | VictoriaMetrics | PromQL compatible, lower resources than Prometheus |
| Jellyfin | Privileged LXC on Urd | QuickSync /dev/dri passthrough |
| Teamspeak placement | Asgard K3s (`k8s/asgard/apps/teamspeak/`), not LXC | Pivoted 2026-05-25 from planned LXC 1121. TS3 3.13+ ships first-party PG plugin (`ts3db_postgresql`) so the original SQLite/LXC concern dissolves. K3s wins on GitOps uniformity + iSCSI storage + ESO secrets, same as Authentik/NetBox. No HA loss — TS server is single-writer (one virtualserver, one identity keypair in PG); failover is DNS-SRV at the client side (`hel-ts3` + `do-ts3` ring already in place). |
| Router | UCG-Ultra | Zone-based firewall, polished UI |
| Internet exposure | KPN DMZ → UCG-Ultra | UCG is sole policy boundary; KPN is dumb pipe |
| OOB | Tailscale on Synology | Independent of Proxmox |
| Storage | iSCSI (not NFS) for K3s PVs | NFS creates polluting shared folders on Synology |
| StorageClass | synology-csi-iscsi-retain only | Single default class, no NFS/SMB pollution |
| K3s storage workaround | Init container chown /vault/data | SKIP_CHOWN + iSCSI fsGroup issue |
| Calico install method | K3s addon manifest, templated by Ansible | Not Flux-managed; addon controller applies it |
| Calico IP autodetection | Pinned to `cidrs: [10.0.21.0/24]` | Workers are multi-homed; `firstFound` bound overlay to the VLAN 20 NIC |
| Calico MTU | Explicit `mtu: 1450` | Workaround for upstream issue #7851 — operator can't read `/var/lib/calico/mtu` under SELinux MCS |
| Worker rp_filter | Loose mode (`2`) | Strict mode drops MetalLB traffic on multi-homed nodes |
| MetalLB L2Advertisement | `nodeSelectors` exclude CP nodes | CP nodes have no eth1 — L2 election must not pick them |
| Vault listener TLS | Flip `tls_disable=1` → a single TLS listener on a cert-manager **internal CA** (selfsigned → CA ClusterIssuer → leaf), distributed by **trust-manager**. Traefik verifies the backend via Gateway-API `BackendTLSPolicy`; ESO via `caProvider`; Semaphore via `ANSIBLE_HASHI_VAULT_CA_CERT`. FQDN consumers (operator CLI/TF, Frigg) keep the public LE cert at Traefik — no new trust. | Decided 2026-06-10, design refined 2026-06-19. Supersedes the earlier "`tls_disable=1` deliberate, revisit at hardening" stance. **Gated before Phase 7 (Jotunheim)** so the second cluster inherits the finished internal-PKI pattern instead of replaying the plaintext→TLS migration. Single TLS listener — NOT an http+https split (two listeners is a footgun: you'd have to guarantee nothing ever LB-exposes the plaintext one). Real exposure closed: the `vault-ui` MetalLB LB `10.0.20.11:8200` served the plaintext API on routable VLAN 20. Internal CA (not LE) is required for the non-public SANs (`*.svc.cluster.local`, `*.vault-internal`, `127.0.0.1`, the VIP). trust-manager bridges cert-manager (writes Secrets only) → BackendTLSPolicy (needs the CA in a ConfigMap, no skip-verify). The same `homelab-internal-ca` later retires Outline's `sslmode=no-verify` + the self-signed Patroni leader cert. Runbook: [vault-tls-migration.md](../procedures/vault-tls-migration.md). |
| Secrets architecture | Three stores by access pattern: 1Password (humans), HashiCorp Vault (machines at runtime), Ansible Vault (machines at bootstrap) | "Centralize everything" rejected after considering Vaultwarden, 1Password Connect, Infisical, OpenBao alternatives — see [Secrets management](../architecture/identity-secrets.md) section |
| Bootstrap-vs-runtime split | Ansible Vault for bootstrap, HashiCorp Vault for runtime | Resolves circular dependency: HashiCorp Vault lives in asgard K3s, so anything K3s itself needs to come up cannot live there. Bootstrap layer is narrow, stable, and rare-touch |
| Ansible Vault scope | Bootstrap secrets only (k3s_token, RHEL keys, SSH pubkeys, AWS KMS re-seal copy) | Narrow permanent role, not legacy — runtime machine secrets go to HashiCorp Vault |
| Ansible → Vault auth | AppRole, two roles (`ansible-local` for MacBook, `ansible-awx` for cluster) | Industry-standard for non-K8s automation; RoleIDs from Terraform outputs, SecretIDs generated manually and kept out of TF state (90-day rotation, 1Password recovery copy) |
| Control-node credential loading | All control-node env vars and tokens loaded from 1P at function-invoke time via `homelab.fish` in `conf.d/`. No env file on disk. | Eliminates the rotation-drift class between an on-disk env file and the 1P recovery copy. Single source of truth (1P) matches the "1P is the recovery copy" posture for AppRole creds. Cost: Touch-ID prompt per shell session (acceptable). Decided 2026-05-23. |
| Long-term Vault successor | OpenBao (migration ~12 months out) | HashiCorp BSL + IBM acquisition risk; LF governance preferred long-term |
| Repo visibility | Private (GitHub) | Reduces exposure; SealedSecrets still used for bootstrap secrets |
| VM naming | Valkyries (CP) + Einherjar/Drengr (workers) | Norse theme, conceptually fits K3s |
| Node naming | Urd, Verd, Skuld (Norns) | Fate controllers = hypervisors |
| NAS naming | Munin | Raven of memory |
| PBS type | Privileged LXC | NFS mount requires it |
| K3s on Urd | CP placement restored 2026-05-22 (Phase 4b) | The original constraint (N5095 + mSATA too slow for etcd) is gone after the MSI Cubi swap. Phase 4b moved Göndul back to Urd. Final CP topology: Göndul on Urd, Hlökk on Verd, Sigrún on Skuld — same as 2026-05-14 pre-incident design, now valid hardware-wise. |
| Göndul placement | Urd (Phase 4b applied 2026-05-22) | Originally on Urd at the 2026-05-14 incident, moved to Verd 2026-05-17 to escape Urd's etcd thrashing. Urd hardware refreshed 2026-05-21 (MSI Cubi); Phase 4b returned Göndul to Urd 2026-05-22. CPs are 2vCPU/4GB symmetrically. |
| CP cpu/memory | Per-node via `locals.control_planes` map | Gondul needs more than hlokk/sigrun; map structure enables per-CP overrides cleanly |
| Per-component-config Kustomizations | `<component>-config/` next to `infrastructure/`, `dependsOn: infrastructure` | SealedSecrets and CRD-dependent resources can't live in same Kustomization as the chart that installs the CRD. Splitting per-component avoids shared failure domains too. |
| MetalLB multi-homed plumbing | rp_filter=2 + route_localnet=1 + VLAN 20 policy routing | All three required; missing any one breaks VIP reachability. Discovered iteratively; 2026-05-17 closed the last gap. |
| VLAN 20 policy routing implementation | systemd oneshot service, `ip rule` + `ip route` | OS-independent (no NetworkManager / netplan / ifcfg coupling). Pure ip(8) + systemd. |
| K3s + MetalLB sysctls location | `roles/k3s/tasks/network.yml` (not hardening) | Hardening role should be OS hardening; K3s-specific requirements belong with K3s. Refactored 2026-05-17. |
| K3s role idempotency | `detect-state.yml` skip-install on healthy nodes | Re-running play against a healthy cluster could fire restart-k3s handler → duplicate-join failure. Skip install when `systemctl is-active k3s && node is Ready`. |
| Sealed-secrets master keys | Back up to 1Password after every controller install | Loss makes every SealedSecret in Git undecryptable. Discovered 2026-05-17 when both SealedSecrets had to be re-sealed from plaintext. |
| Vault test KV entry | Managed declaratively in `terraform/vault/main.tf` | Was manual state on the old cluster; lost on rebuild. Anything Vault-side must be IaC. |
| OS-independent roles | All Ansible roles should be OS-independent where possible | Use systemd (universal) instead of nmcli/netplan/ifcfg (distro-specific). Kernel-level operations (sysctl, ip(8)) are universal too. |
| LXC build order | Revised: Factorio → PG+Teamspeak → Authentik → Tailscale → rest | Original sequence conflated Authentik-dependent and Authentik-independent LXCs |
| LXC provisioning | Terraform module `asgard-lxcs/` parallel to `asgard-k3s/` | Same provider, same auth, consistent pattern |
| Factorio operator UX | SFTP-only self-service via SFTPGo virtual user | Zero shell access; control via JSON files in `/factorio/control/` |
| Factorio reconcile pattern | Root systemd timer (30s), Python stdlib script, owns state | Operator declares intent in JSON, reconciler converges. Decoupled. |
| Factorio operator auth | Password + SFTPGo defender | Ease-of-use over pubkey complexity; brute-force protected |
| Factorio install location | `/opt/factorio-<version>/`, symlinked to `/opt/factorio` | Atomic version swap, side-by-side versions, easy rollback |
| Factorio chown after extract | Recursive `factorio:factorio` on install dir | Factorio writes `.lock` in install dir — needs runtime user write access |
| LXC bootstrap flow | `ansible-playbook -e ansible_user=root --tags baseline` then full playbook as ansible | Root SSH only for the brief baseline window; hardening locks root after |
| Debian LXC user creation | `password: '*'` in `user` task | Ansible default `!` triggers PAM account lock even for key auth |
| LXC nesting flag | `nesting=true` on Debian 13 LXCs | Required for systemd 257; orthogonal to nested containerization |
| Service LXC naming convention | Norse, themed to function — DB → Frigg's handmaidens (Fulla/Vör/Idunn); HAProxy TBD; Factorio kept literal | Extends host-naming pattern (Norns → hypervisors, Valkyries → CP) to service LXCs. Single-purpose oddballs like Factorio stay literal — not every LXC needs a Norse name. |
| Cluster naming meta-principle | Primary defines the theme; replicas are a logical expansion within it | DB = library → librarian-keepers. New service cluster: pick primary by archetype, replicas by the natural cohort around it. |
| PG storage backend | Local LVM-thin only (not NFS) | NFS fsync semantics vary; WAL latency over 1 GbE kills throughput; Synology DSM NFS not on any validated list. Data scale fits in LVM-thin disk. |
| PG cluster build sequence | Standalone Fulla first; Vör/Idunn + HAProxy post-Authentik | Cluster expansion validated against real consumer (Authentik DB) rather than synthetic load. Authentik points at fulla direct until VIP exists; single config flip later. |
| PG cluster sizing | Identical across nodes (cluster-wide constants, not per-node map) | Failover symmetry — asymmetric sizing means failover silently degrades. Promote sizing into the locals map only when deliberate per-node tuning is needed. |
| PG TLS | Self-signed via Ansible (4096-bit RSA, 825d, SAN covers host/FQDN/IP) | In-LAN doesn't excuse cleartext at senior posture. cert-manager via ESO push to LXC is the long-term replacement. |
| PG management user split | Two SUPERUSER roles: admin (hand-on-keyboard), ansible (automation). Service DB roles are LOGIN only | Operational distinction (who is acting) without security boundary cost. Tighten later if any consumer warrants least-privilege. |
| PG replication-ready config day 1 | `wal_level=replica`, `max_wal_senders=10`, `archive_mode=on` with no-op `archive_command` even on a single node | These settings require a restart to change. Setting them right from the start avoids a config-thrash + restart later when 1131/1132 join. |
| PG per-service DB provisioning | Declarative list (`postgres_databases`) in group_vars; role iterates idempotently | New consumer = group_var entry + replay. No role editing. AWX automation later. |
| PG database deletion | Manual via `psql -c 'DROP DATABASE'` — NOT declarative from group_vars | Removing an entry from `postgres_databases` is config drift, not destructive intent. Drop must be explicit; typos must not be able to drop data. |
| Baseline role scope | Timezone, locale, minimal-template gap-fillers (sudo, acl, tzdata, gnupg, ca-certificates), **DNS resolver config** (`/etc/resolv.conf` + cloud-init `manage_resolv_conf: false` drop-in) live here, not in every consuming role | Roles authored against minimal Debian rediscover the same gaps. Baseline owns the class. DNS added 2026-05-17 evening after cloud-init's `1.1.1.1` fallback poisoned internal-zone resolution during Authentik deploy. List grows over time; that's expected. |
| K3s CP sizing | Identical across nodes (cluster-wide constants, not per-node map) | Failover symmetry — same rule as PG nodes. Asymmetric sizing means failover silently degrades. All three CPs 2vCPU/4GB as of 2026-05-17 evening. Promote sizing into the locals map only when deliberate per-CP tuning is needed. |
| K3s CP workload isolation | `node-role.kubernetes.io/control-plane=:NoSchedule` taint on all three CPs (Phase 4a — applied 2026-05-21) | CPs run only control-plane components + DaemonSets that explicitly tolerate the taint (Calico-node, kube-proxy, metallb-speaker) + K3s-shipped pods (CoreDNS, metrics-server, calico-apiserver). Workload pods cannot land on CPs — pressure can't reach etcd via fsync contention. With taint, 4 GiB is the correct CP size. Side effect: Synology CSI node-plugin evicted from CPs (closes the CP-grabs-worker-LUN gotcha class). **Caveat:** evicting CSI from CPs while stateful pods still live there breaks the unmount path — drain stateful workloads first, OR add a CP-toleration to the CSI DaemonSet. Architectural decision on the long-term posture pending — see [Open questions](open-questions.md). |
| K3s `node-taint` is registration-time only | Apply taints via `kubectl taint node` for existing clusters; config-template covers fresh bootstraps | Discovered 2026-05-21 Phase 4a: K3s does not re-apply `node-taint:` config to existing node objects on restart. Updating the config template alone leaves existing nodes untainted. Going-forward: both paths are in place — config-template for future cluster rebuilds + Phase 4b's gondul re-registration; kubectl for the current cluster. |
| K3s role config rendering | `config.yml` separate from `install.yml` | Until 2026-05-21, config-template tasks lived inside `install.yml`, which is wholesale-skipped on healthy nodes via `detect-state.yml`. Genuine config changes never rendered on healthy CPs. Split out during Phase 4a. The restart-k3s-on-healthy-CP concern (potential "duplicate node name") was empirically *not* observed — steady-state restart is safer than the fresh-bootstrap warning suggested. |
| Synology CSI node-plugin on CPs | **Open architectural question** — defaults to "off" (CSI runs only on workers) | Phase 4a evicted CSI from CPs as intended for the cross-node-iSCSI-fight gotcha class. But the eviction-while-stateful-pods-still-there path is a footgun (see Known gotchas "CSI eviction footgun"). Options: (a) accept the footgun, document the "drain first" rule; (b) add CP-taint toleration to CSI DaemonSet (production default for GKE/EKS) — keeps the cross-node-iSCSI-fight risk but eliminates the unmount-hang risk. Decision deferred to a separate session post-Phase 4a. |
| Helm chart pin policy | Concrete-pin all HelmRelease charts. No minor-floats (`0.x`, `2.x`, `0.15.x`). Renovate deferred until stable state. | Closed 2026-05-22 as Phase 4b prerequisite. The original position (`0.x` placeholders, pin at "2.0") was retired after the 2026-05-21 Phase 4a session — two outages from floating pins in one day (metallb 0.16 broken template, synology-csi 1.3.0 image not published) disproved the assumption that minor-version bumps would be safe. All four remaining float-pins tightened to concrete on 2026-05-22 (sealed-secrets 2.18.6, vault 0.32.0, external-secrets 0.20.4, metallb 0.15.3). Updates are deliberate operations until Renovate is activated post-stable-state. |
| IaC pin policy (generalized) | Concrete-pin ALL IaC versions — Helm charts, Terraform providers, Ansible role versions. Minor-floats (`~> 4.0`, `0.x`, `2.x`) banned across the board. | Decided 2026-05-23 as 5e.2.a prerequisite. The Helm-only rule above generalized — Terraform providers have the same failure mode (Cloudflare v4→v5 is 40+ resource renames in a minor-version stream; same shape as the metallb 0.15→0.16 break). New `terraform/cloudflare/` pins concrete from day 1. The last two floating provider pins — `terraform/vault/` + `terraform/proxmox/zabbix-access/`, both `hashicorp/vault ~> 4.0` — were tightened to `4.8.0` (the resolved version) on 2026-05-31 (Wave S5). Updates remain deliberate operations. |
| Sub-1.0 charts pinned concrete are compliant | A `0.x.y` pin is concrete (e.g. `vault 0.32.0`, `metallb 0.15.3`, `victorialogs 0.12.5`); only an `0.x`-style *float* violates the policy. No action needed to "graduate" sub-1.0 charts to 1.0. | Clarified 2026-05-31 (Wave S5) after the pin audit flagged eight sub-1.0 charts. The pin policy bans *floating* operators, not sub-1.0 versions — the upstream not having cut 1.0 is irrelevant to determinism. Documented so a future audit doesn't re-flag them. |
| Ansible Galaxy collections concrete-pinned | `ansible/requirements.yml` collections pinned to exact versions (was `>=X.0.0` floats). | Decided 2026-05-31 (Wave S5), closing the last floating-version class. The generalized IaC pin policy covers Galaxy collections — `ansible-galaxy collection install` resolves the newest matching version, so an unbounded `>=` silently pulls breaking majors (community.zabbix 4.x dropping env-var auth is the canonical break). An audit found one host env had drifted to `community.hashi_vault 7.1.0` despite the stated `<7.0.0` intent — proof the float wasn't enforced. Pinned to the tested set: hashi_vault 6.2.1, crypto 3.2.1, postgresql 4.2.0, netbox 3.22.0, zabbix 4.2.0. |
| S4 alerting — active prober for app/edge state, Zabbix for host metrics | Cloudflare-token / cert-expiry / Patroni+etcd quorum / PBS-backup-failure are checked by an active Semaphore-scheduled prober (`infra-health-check.yml`) that POSTs to Hermod; host-resource thresholds (disk/inode) stay in Zabbix via global macros. Did NOT introduce `community.zabbix.zabbix_trigger`/`zabbix_item`. | Decided 2026-05-31 (Wave S4). Zabbix is the alerting brain for host/agent metrics (vmalert deliberately off), but the S4 gaps are *active* checks of edge/app state (an API token's validity, a served cert's expiry, cluster quorum, a backup job's outcome) — naturally expressed as deterministic, testable playbook probes rather than triggers on scraped metrics. This also sidesteps blind first-use of the never-before-used community.zabbix trigger/item modules (a dozen module-shape gotchas warned against it) and the fact that the official PBS Zabbix template needs 7.2 (we run 7.0 LTS). The prober is defence-in-depth for the Patroni `on_role_change` callback (which can't fire if the node holding it died) and complements, not replaces, Zabbix. |
| Renovate — stay manual through stabilization | Do NOT activate Renovate yet; version bumps remain deliberate manual operations. | Decided 2026-05-31 (Wave S5). The 1.0-stabilization plan is about *measurable confidence in what's deployed*, not chasing upstreams. Renovate's PR-noise (every chart/provider/collection bump as a PR across a 16-chart + 13-TF-module + 5-collection surface) is net-negative while the goal is "don't change what works." Revisit post-Phase-7 when jotunheim exists as a safe place to absorb auto-bumps first. The pin-policy comments + decisions rows already document every version's rationale, so a future Renovate activation inherits that context. |
| DNS fallback resolver | Never a public resolver. UCG → AdGuard VIP only, optional fallback peer AdGuard | Public resolvers (Cloudflare `1.1.1.1`, Google `8.8.8.8`) return NXDOMAIN for internal zones. Glibc and CoreDNS treat NXDOMAIN as authoritative and cache it. Discovered 2026-05-17 during Authentik deploy — Authentik couldn't reach `fulla.niflheim.xiiisins.com` because CoreDNS had cached a stale NXDOMAIN from a brief moment where the K3s node had queried `1.1.1.1` via secondary fallback. |
| Authentik Redis | Hand-rolled StatefulSet (`redis:7-alpine`), not Bitnami sub-chart | Chart 2026.x dropped its bundled Redis. Hand-rolled is ~50 lines of YAML — simpler than adopting Bitnami's metrics/sentinel/HPA scaffolding for a single-replica homelab Redis. Single replica, AOF persistence on iSCSI. If a future service wants its own Redis (e.g. AWX fact caching), it gets its own — namespace-bundled. |
| Authentik service config injection | All env vars via ExternalSecret template, not chart `values:` block | The chart exposes config via both paths; env vars win silently. Setting only the values block produces ghost-localhost-fallback (Authentik tried `127.0.0.1:5432` for an hour before this was diagnosed 2026-05-17). Going forward: every service config knob settable via env goes through ExternalSecret. |
| Authentik day-1 GitOps | Blueprints in Git from day 1, no UI configuration ever | Single source of truth. Cluster rebuild reconstitutes identity. Brand + personal user as the first two blueprints; subsequent OIDC providers (Tailscale, Grafana, etc.) follow same pattern. Branding assets follow the same rule (populator init container reads from ConfigMap today, S3-compatible tomorrow — pattern preserved). |
| Sub-kustomization per component | Every component is a self-contained directory with its own `kustomization.yaml`; parent `infrastructure/kustomization.yaml` only references directories | Forced by Authentik's `configMapGenerator` for blueprints (Kustomize generators can't flow through flat-file-reference parents). Migrated all components for consistency. Closed the design doc's open nested-vs-flat question 2026-05-17 evening. |
| Stateful worker rebuild — Vault quorum posture | Accept 2/3 voters for the duration of a single-worker rebuild; do NOT migrate the stateful pod off the doomed worker first | Vault's chart ships pod anti-affinity as `requiredDuringSchedulingIgnoredDuringExecution` — at 3 replicas on 3 workers, draining or cordoning any one worker leaves the displaced pod Pending until the doomed worker returns. The cordon+migrate dance designed around "always 3/3 voters" hits this wall. 2/3 is operationally fine for ~20-30 min windows: Vault stays fully read+write, etcd is untouched, only risk is a second voter failing during the window (low probability for healthy peers). Discovered 2026-05-22 during einherjar-urd worker rebuild. |
| Stateful worker rebuild — leadership step-down before drain | Step Raft leadership off the doomed worker before pod-delete or drain | Vault's pod-delete forces a Raft election under termination pressure if the doomed pod is leader. `vault operator step-down` first lets the cluster pick a new leader cleanly while the original leader is still healthy. Cost ~10s, eliminates the election-during-termination race. Applied 2026-05-22; would have been worth doing in Phase 4a too. |
| K3s playbook execution model | Default `serial: 1`; override for cluster-from-zero rebuilds (planned) | Default-parallel is a multi-node-outage footgun — a single role change that triggers `restart-k3s` will fire across all 6 nodes concurrently, defeating HA. Validates with Phase 4a finding that per-node K3s restart is empirically safe, but simultaneous restart across the cluster is not. Captured 2026-05-22 as a pending change; not yet implemented. |
| Baseline OS package update | Tagged `os-updates`, skip-by-default (planned) | Current behavior runs `dnf update "*" → latest` + reboot on every play, conflating config-idempotency runs with OS-maintenance runs. Default-on-with-reboot is wrong for incremental config; should be opt-in via `--tags os-updates` for explicit maintenance windows. Captured 2026-05-22 as a pending change; not yet implemented. |
| PVE host patching | Dedicated discovery-driven rolling playbook (`proxmox-host-patching.yml`), NOT in `os-updates.yml` | A PVE host reboot takes every VM/LXC on it down — it needs orchestration the flat per-group `serial: 1` of `os-updates.yml` can't express. The playbook discovers each host's work live (`pvesh /cluster/resources` + `kubectl` + Patroni REST — nothing about the symmetric placement is hardcoded), then: switchover the Patroni leader off the target (via `patronictl`, which carries the `restapi` auth the raw REST POST lacks), `ha-manager migrate` HA guests (Frigg), cordon K3s, `apt dist-upgrade`, reboot, and **gate on full 3/3 reconvergence** (guests running + `pvecm` quorate + Patroni/etcd 3/3 + K3s Ready) before advancing. Two pre-flight gates refuse to start against an already-degraded cluster. Strictly serial. Run from the MacBook control node (an orchestrator can't reboot the host it runs on); hands-off Frigg/Semaphore scheduling deferred to phase 2 (needs a resumable checkpoint). The companion `proxmox-host` role (config-only — removes the enterprise + ceph apt sources; no-sub templating opt-in) is drift-safe and wired into `asgard-proxmox.yml`. Decided + validated 2026-06-18 (canary on urd incl. reboot, then full rolling round). Runbook: [`docs/procedures/proxmox-host-patching.md`](../procedures/proxmox-host-patching.md). |
| Cluster routing API | Gateway API (sigs.k8s.io/gateway-api) v1.5.1 Standard channel — over Ingress and Traefik IngressRoute | Picked 2026-05-22 Phase 5e.1. Gateway API is the K8s-spec successor to Ingress (GA in 1.30, widely adopted by 2026): role-separated (platform owns Gateway, app owns HTTPRoute), expressive enough to skip annotation soup, portable across controllers (Traefik, Envoy, Cilium, etc). Ingress was viable but dated; Traefik IngressRoute teaches Traefik specifically and doesn't generalize. Gateway API is the modern + portable + senior-signal answer. Middleware attachment via `ExtensionRef` filters pointing at Traefik `Middleware` CRs — small portability tax (routes stay portable, middleware definitions don't). |
| Gateway API CRD distribution | Vendored upstream manifest in `infrastructure/gateway-api/` | Raw YAML from upstream release (`standard-install.yaml`, ~200KB) committed to repo. Alternatives (Flux `GitRepository` source, Kustomize remote resource) introduce reconcile-time fetch dependency on upstream availability. Vendoring gives deterministic, auditable, offline-safe CRDs. Renovate-track-by-comment when Renovate is activated. |
| TLS cert strategy | Single wildcard `*.niflheim.xiiisins.com` (ECDSA P-256) | One Certificate covers all internal services. Per-FQDN certs add object count + renewal traffic without security benefit at this scope. ECDSA over RSA: smaller TLS handshake, modern default — Let's Encrypt has supported ECDSA for years. SAN includes apex (`niflheim.xiiisins.com`) so future apex route works without re-issuance. |
| ACME challenge | DNS-01 via Cloudflare API (zone-scoped token, `xiiisins.com` only) | DNS-01 works for wildcard certs (HTTP-01 cannot); also keeps cert issuance independent of inbound HTTP reachability. Token scoped to single zone with `Zone:DNS:Edit` + `Zone:Zone:Read` (the latter is needed for cert-manager's zone-discovery step, easy to miss). Token in Vault at `secret/k8s/cert-manager/cloudflare`, K8s Secret materialised via ExternalSecret with field rename `api-token` (kebab Vault) → `apiToken` (camel cert-manager expects). |
| ClusterIssuers — staging + prod day 1 | Both `letsencrypt-staging` and `letsencrypt-prod` deployed together | Wildcard cert starts on staging for chain validation (unlimited rate limit, no risk of burning prod's 50/week per-domain quota). Flipped to prod after end-to-end validation. Both Issuers reference the same Cloudflare token Secret; only the ACME endpoint differs. `dnsZones` selector locks each Issuer to `xiiisins.com` so misuse against a wrong zone fails noisily. |
| Traefik VIP | `10.0.20.10` — pool extended `.11–.99` → `.10–.99` for it | Asgard LB demand realistically caps at ~3 LBs ever (Vault, Traefik, occasional non-HTTP edge like Teamspeak). Cloudflared targets ClusterIP DNS, not MetalLB IPs — so external-only services don't consume MetalLB IPs. Reasoning for `.10`: memorable, sits just below the pool, single edge LB doesn't justify a block-allocation scheme. |
| Traefik privileged-port binding | `NET_BIND_SERVICE` capability + entrypoints on 80/443 directly (no Service-port-rewrite shim) | Chart default uses 8000/8443 internally (non-root can't bind <1024) with Service-level `exposedPort: 80/443` mapping. Works for traditional Service-IP access, but breaks Gateway API: Traefik's Gateway provider matches listener `port` to the entrypoint's *internal* port. With `NET_BIND_SERVICE` (the smallest possible Linux cap, nothing else added) Traefik binds 80/443 directly; Gateway listeners and entrypoints match 1:1, manifests read naturally. Pattern is what most production Traefik deployments use and what the chart's own examples recommend. |
| Traefik replicas + rollout strategy | 3 replicas (one per worker) with `requiredDuringScheduling` anti-affinity + `strategy.rollingUpdate.maxSurge: 0, maxUnavailable: 1` | Required anti-affinity ensures MetalLB L2 election (which picks one node for ARP) always lands on a node with a local Traefik pod — pairs with `externalTrafficPolicy: Local` for source IP preservation. 3 replicas on 3 workers fills every slot exactly. `maxSurge: 0` is mandatory: default 25% rounds to +1, but there's no 4th worker to satisfy anti-affinity → rollout deadlocks. `maxUnavailable: 1` keeps rolls serial. During a roll, briefly serves on 2/3 replicas — fine because only one is actively serving via L2 election anyway. |
| Gateway namespace policy | Single shared `niflheim` Gateway in `traefik` namespace, `allowedRoutes.namespaces.from: All` | One Gateway for asgard; HTTPRoutes live in their app's namespace (`authentik/`, future `outline/`, etc.) and attach cross-namespace via `parentRefs`. Pattern 1 (shared Gateway) is simpler and matches "single admin" homelab reality; Pattern 2 (Gateway-per-app) is more idiomatic for multi-tenant clusters but unnecessary here. Tighter scoping (`Selector`-based, only specific namespaces) is possible later if needed. |
| Cloudflared backend targeting | Always via ClusterIP DNS, never MetalLB LB IPs | Cloudflared is in-cluster; resolving a backend via its `<svc>.<ns>.svc.cluster.local` name keeps traffic in pod network. Targeting a MetalLB IP would create tromboning: packet leaves pod → hits LB IP outside cluster → MetalLB ARPs it back into the cluster → kube-proxy DNATs to a pod IP. Pointless extra hops with same eventual destination. **Exception:** if a route needs middleware policy (rate limit, IP allowlist) applied to externally-tunnelled traffic, target Traefik via its ClusterIP DNS with `originRequest.httpHostHeader` override + `noTLSVerify: true` for internal certs. Still no MetalLB IP consumed. |
| DNS — three-zone scheme | `xiiisins.com` apex (external, Cloudflare-resolved) / `midgard.xiiisins.com` (internal alias for publicly-reachable services, AdGuard-resolved) / `niflheim.xiiisins.com` (internal-only, AdGuard-resolved) | Closed 2026-05-22 during 5e.2 design. Earlier docs used `midgard` interchangeably for "public" without distinguishing apex from internal alias; this conflated three distinct concerns (external resolution + internal alias for public services + internal-only services). The three-zone framing keeps zone selection mechanical: external → apex, internal-but-publicly-reachable → midgard, internal-only → niflheim. Each zone gets its own wildcard cert (5e.1 issued niflheim, 5e.2 adds midgard + apex). |
| External TLS posture | Cloudflare Tunnel + Cloudflare universal cert externally; our LE wildcard `*.xiiisins.com` on the origin side (Traefik → Cloudflared origin-pull) | "Pattern (C)" decision 2026-05-22. Free-tier Cloudflare Tunnel terminates TLS at the edge with Cloudflare's universal cert; custom edge certs require Business/Enterprise plan or Advanced Certificate Manager. Our LE wildcard is what Cloudflared connects to Traefik with — invisible to browsers, real for origin authentication. This matches the prior Docker Swarm setup. Alternatives considered: pattern (A) DNS-only + UCG port-forward (drops DDoS protection + IP hiding, gains end-to-end LE), pattern (B) paid custom edge cert (~$10/mo+). Picked (C) for cost + tunnel benefits; can mix in (A) per-service if a service ever needs end-to-end LE. |
| Two-Gateway separation | `niflheim` Gateway for internal-only routes; `midgard` Gateway for apex + midgard routes | Decided 2026-05-22 during 5e.2 design. Single-Gateway alternative was simpler but mixed internal-only and publicly-reachable routes under one policy surface — easy to misroute a niflheim-named HTTPRoute onto the public zone. Two Gateways enforce the zone separation at the Gateway level: an HTTPRoute attaching to `midgard` is explicitly opting into public exposure. Both Gateways live in `traefik` namespace with `allowedRoutes.namespaces.from: All`. |
| WebFinger hosting | Tiny Caddy pod (`apps/apex-static/`) attached to the `midgard` Gateway's apex-bare listener at `xiiisins.com/.well-known/webfinger`. Returns RFC 7033 `application/jrd+json`. | Decided 2026-05-22 design; **revised 2026-05-23 during 5e.2.f after discovering Traefik v3 OSS has no built-in static-response middleware** (only header/redirect/auth middleware ship in OSS; static-response is a Yaegi-loaded community plugin). Switched to a pod (Caddy 2.11.2-alpine, 2 replicas, ConfigMap-backed Caddyfile + JSON). Trade vs. plugin approach: ~16MB extra memory + 1 extra Service vs. avoiding a Yaegi plugin pin and a Traefik values-change shortly after 5e.1 stabilized. Pod also generalizes — future apex static endpoints (`robots.txt`, `security.txt`, future startpage) reuse the same pod without re-touching infra. Pod is required only at tailnet creation per Tailscale docs but kept always-on for re-creation flows. |
| App vs infrastructure split | `k8s/asgard/apps/` = leaf consumer workloads (apex-static, startpage, future Outline/Immich/etc.). `k8s/asgard/infrastructure/` = platform services that other services depend on (cert-manager, traefik, gateway-api, ESO, MetalLB, Vault, sealed-secrets, synology-csi, **cloudflared**, **authentik**). | Decided 2026-05-23 as 5e.2.f prerequisite when wiring `apps/` for the first time. The split is "kill this and what breaks" — kill apex-static and only WebFinger breaks; kill cloudflared and all public ingress breaks (same tier as Traefik). Authentik is in `infrastructure/` because every service eventually authenticates through it — universal-dependency rule. Borderline cases get the explicit call here in the decision log when they land. `apps/` reconciles via its own Flux Kustomization (`flux-system/apps.yaml`) with `dependsOn: infrastructure` so apps don't start reconciling until cluster platform is Ready. |
| Module ownership of Vault KV secrets | Each Terraform module owns the Vault KV entries for secrets *it generates*. `terraform/vault/` owns Vault shape (mounts, auth, policies, roles) but not KV data. Manually-minted secrets (Cloudflare DNS-01 token, 1P-sourced creds) stay manual until a broader IaC pattern for those lands. | Decided 2026-05-23 during 5e.2.d. The pre-existing mixed pattern (Vault config in Terraform, KV contents manual) was an unintentional gap — for any secret a module generates, the only safe target IS Vault, and a manual `terraform output | vault kv put` pipe step is strictly less safe than letting the module do it directly (same tfstate exposure either way, but eliminates the shell-pipe window). Secret duplication in tfstate is an accepted homelab-simplicity tradeoff (private repo, same posture as the existing Vault test KV). Long-term: VSO/Vault Agent runtime fetch retires the K8s-Secret intermediate. |
| Tailscale plan + auth model | Free plan (3-user cap on custom OIDC). Split-auth: auto-renewing TF-minted keys for LXC servers (1113/1114/1115) and one always-on user device — Tailscale's API caps key lifetime at 90 days, so "auto-renewing" means `reusable=true` + `recreate_if_invalid="always"` + max-allowed `expiry=7776000`, with the rebuild-an-LXC workflow being "TF apply this module first, then Ansible" (see auth-key gotcha). NAS authkey is out-of-band (UI-minted, 1P-stored — see "Munin Tailscale authkey" row). Default-expiry keys for phones/laptops via Authentik OIDC. | Decided 2026-05-22 (auto-renewing semantics clarified 2026-05-23 during 5e.3.e.ii planning when the 90-day cap was discovered; NAS pulled out of the auto-renewing cohort 2026-05-23 during 5e.3.f when human-consumer → 1P precedence was settled). Free plan supports custom OIDC for up to 3 users (per Tailscale's own announcement) — enough for a personal homelab. Split-auth pattern: server-class devices never depend on Authentik for re-auth (they'd be locked out by an Authentik outage exactly when you need VPN access to fix it); user-class devices keep proper expiry rotation. The "one always-on device" lets the owner reach the tailnet without OIDC when traveling and Authentik is down. |
| Tailscale device scope | 3 LXCs (1113/1114/1115) + Tailscale on the NAS only. NOT universal-Tailscale across all servers. | Decided 2026-05-22. Earlier framing was "all servers get Tailscale + indefinite keys" — too broad. Universal Tailscale adds a daemon, an authkey, and a failure mode to every host for no gain over the subnet-router pattern (already gives full LAN access). Three LXCs + NAS gives redundancy (3 advertisers of the same routes; Tailscale picks one, fails over), keeps the daemon footprint to 4 devices, and isolates the break-glass path on the NAS (K3s-independent). |
| NAS Tailscale role | Subnet router advertising the same routes as 1113/1114, K3s-independent break-glass path. Installed as DSM-native package (Synology Package Center) — chosen over manual `.spk` from `pkgs.tailscale.com` for the simpler install + DSM-managed auto-update story. | Decided 2026-05-22, installed 2026-05-23 (Phase 5e.3.f). NAS's role is "reachable when K3s is down." Munin runs DSM (independent of K3s), so a Tailscale advertiser on it keeps the tailnet's subnet-route path functional even if all three K3s nodes are down. No active/standby semantics needed — Tailscale picks one advertiser at a time and 1Gb links mean route selection is irrelevant. DSM Package Center over manual SPK: Synology's official package is ~one version behind upstream stable, fine for a break-glass advertiser that doesn't need bleeding-edge features. |
| Cloudflared management mode | Locally-managed tunnel: `config.yaml` ingress rules in Git as ConfigMap, per-tunnel `credentials.json` (JWT) via ESO from Vault. NOT remotely-managed (UI/dashboard config) NOR account-level `cert.pem`. | Decided 2026-05-23 as 5e.2 prerequisite. Locally-managed is the supported Cloudflare path that keeps all config in Git (the "Git is truth" rule). Remotely-managed parks ingress rules in the Cloudflare dashboard — UI as source of truth violates the same rule that produced the Authentik day-1 blueprints decision. Account-level `cert.pem` is for tunnel/DNS creation; that work is owned by Terraform in our flow, so the runtime pod only needs the per-tunnel JWT. |
| Cloudflare resources in IaC | Tunnel objects + DNS records via Terraform in `terraform/cloudflare/`. Same pattern as `terraform/vault/`. | Decided 2026-05-23 as 5e.2 prerequisite. Cloudflare side gets a new Terraform module with its own purpose-scoped API token. Token includes `Account:Cloudflare Tunnel:Edit` (cert-manager's existing token is zone-scoped only and insufficient for tunnel object creation). Token NEVER in Terraform state — generated manually, stored in 1Password + local env, same as the existing Cloudflare DNS-01 token pattern. |
| Cloudflared replica/scaling posture | 3 replicas, hostname anti-affinity, no autoscaling. | Decided 2026-05-23 as 5e.2 prerequisite. Per Cloudflare's own Kubernetes guidance: scaling down breaks existing user connections to the removed replica. 3 replicas pinned to 3 workers via anti-affinity matches the Traefik posture (and the worker-rebuild-tolerance argument that justified it). Each replica handles all tunnel ingress rules — no per-replica config split needed. |
| Personal admin identity | `ghost@xiiisins.com` — single human identity across Authentik admin user, WebFinger subject, and (forward) every OIDC consumer. | Decided 2026-05-23 as 5e.2 prerequisite. One identity, mailbox-backed at the apex zone (Proton). Picked over `admin@` because the WebFinger subject is identity, not role; `admin@` would read as a service-role account in Tailscale's `acct:` URI. Personal username carries naturally to Outline/Immich/Grafana/etc. as those services are deployed. |
| Terraform state storage (initial) | Local-only on operator workstation, gitignored across every `terraform/*/` module. Never in Git. Backup is operator's responsibility (1Password attachment, encrypted external drive). | Foundational rule, predates the doc — gitignore was correct from day 1, just unstated until 2026-05-23. State contains decrypted secrets (`random_password`, `vault_kv_secret_v2` `data_json`, etc.) and identity material; committing it inverts every Vault/1Password discipline. **Superseded 2026-05-25 by remote-backend migration — see "Terraform state backend" row below.** |
| App vs infrastructure split (`k8s/asgard/`) | `infrastructure/` = platform services that other services depend on (cert-manager, traefik, gateway-api, ESO, MetalLB, Vault, sealed-secrets, synology-csi, **cloudflared**, **authentik**). `apps/` = leaf consumer workloads (apex-static, future startpage, future Outline/Immich/etc.). | Decided 2026-05-23 as 5e.2.f prerequisite when wiring `apps/` for the first time. The split is "kill this and what breaks" — kill apex-static and only WebFinger breaks; kill cloudflared and all public ingress breaks (same tier as Traefik). Authentik is in `infrastructure/` because every service eventually authenticates through it — universal-dependency rule. `apps/` reconciles via its own Flux Kustomization (`flux-system/apps.yaml`) with `dependsOn: infrastructure` so apps don't start reconciling until cluster platform is Ready. |
| WebFinger hosting | Tiny Caddy pod (`apps/apex-static/`) attached to the `midgard` Gateway's apex-bare listener at `xiiisins.com/.well-known/webfinger`. Returns RFC 7033 `application/jrd+json`. | Decided 2026-05-22 design; **revised 2026-05-23 during 5e.2.f after discovering Traefik v3 OSS has no built-in static-response middleware** (only header/redirect/auth middleware ship in OSS; static-response is a Yaegi-loaded community plugin). Switched to a pod (Caddy 2.11.2-alpine, 2 replicas, ConfigMap-backed Caddyfile + JSON). Trade vs. plugin approach: ~16MB extra memory + 1 extra Service vs. avoiding a Yaegi plugin pin and a Traefik values-change shortly after 5e.1 stabilized. Pod also generalizes — future apex static endpoints (`robots.txt`, `security.txt`, future startpage) reuse the same pod without re-touching infra. Pod is required only at tailnet creation per Tailscale docs but kept always-on for re-creation flows. |
| Module ownership of Vault KV secrets | Each Terraform module owns the Vault KV entries for secrets *it generates*. `terraform/vault/` owns Vault shape (mounts, auth, policies, roles) but not KV data. Manually-minted secrets (Cloudflare DNS-01 token, 1P-sourced creds) stay manual until a broader IaC pattern for those lands. | Decided 2026-05-23 during 5e.2.d. The pre-existing mixed pattern (Vault config in Terraform, KV contents manual) was an unintentional gap — for any secret a module generates, the only safe target IS Vault, and a manual `terraform output \| vault kv put` pipe step is strictly less safe than letting the module do it directly (same tfstate exposure either way, but eliminates the shell-pipe window). Secret duplication in tfstate is an accepted homelab-simplicity tradeoff (private repo, state never in Git). Long-term: VSO/Vault Agent runtime fetch retires the K8s-Secret intermediate. |
| Authentik IaC split: config in Terraform, content in blueprints | `terraform/authentik/` owns identity-and-access configuration (OAuth2Providers, Applications, PolicyBindings, **users, groups**, scope mappings). Blueprints own true content (brand, branding assets). | Decided 2026-05-23 during 5e.3.b. Pattern matches `terraform/vault/` (config in TF, contents in consumers): blueprints had been doing double-duty as both config and content, which conflicted with the broader "config-in-Terraform-when-a-provider-exists" rule established for Cloudflare. Users/groups belong in TF because identity IS config that other config (PolicyBindings, future per-service group assignments) references. Existing `personal-admin` user migrated via `terraform import` 2026-05-23. Future apps' OAuth2Provider+Application+group-binding all land in this module. |
| Identity-as-data (users.yaml/groups.yaml) | Users + groups defined in YAML data files, decoded by `for_each` in `terraform/authentik/identity.tf`. Membership defined from the user side only (each user lists their groups); group resources have no `users` field. Cross-reference validation at plan time via `terraform_data.identity_validation` precondition. | Decided 2026-05-23 during 5e.3.b. Naive shape (one HCL block per user) doesn't scale past ~5 users; identity-as-data is the standard medium-scale pattern (matches what real enterprises do before they switch to a true IdM system). Membership-from-one-side avoids the dual-side-of-relationship plan-flapping. Pre-existing users (whose passwords are in 1P) flag `skip_initial_password: true`; new users get a random initial password to Vault at `secret/k8s/authentik/users/<username>/initial-password` until first-login change. |
| Tailscale ACL bootstrap pattern | Manual two-step bootstrap before Terraform can take over: (1) edit tailnet policy file in UI to plant `tagOwners` block defining `tag:terraform-tag-owner` (no owners — only `autogroup:admin` applies) plus child tags it owns (`tag:subnet-router`, `tag:exit-node`); (2) create OAuth client in Trust credentials with required scopes + tagged `tag:terraform-tag-owner`. Then `terraform import tailscale_acl.this acl` brings the existing policy into TF state. From that point all ACL changes go through TF. | Decided 2026-05-23 during 5e.3.d. The chicken-and-egg: OAuth client must carry a tag to mint authkeys (Tailscale's tag-ownership rule), but the tag must exist in the ACL first. Bootstrapping the tag in the UI is one-time cost; the alternative is a TF-managed OAuth client (which requires either a personal API key or another OAuth client to create it, recursing the problem). Standard pattern documented in Tailscale's Kubernetes Operator + OAuth client docs. |
| Tailscale ACL syntax: grants (not acls) | Tailnet policy file uses Tailscale's `grants` syntax. The legacy `acls` block is not used. | Decided 2026-05-23 during 5e.3.d. Per Tailscale's own docs (validated Feb 2026): "Tailscale recommends migrating to grants and using grants for all new tailnet policy file configurations because ACLs will not receive any new features." Grants are a superset of ACLs; for default-allow they're equivalent (`{src: *, dst: *, ip: *}` vs `{action: accept, src: *, dst: *:*}`); for future group-scoping grants are more expressive (separates `dst` from port via `ip` field, supports application-layer capabilities). The pre-existing Tailscale-default tailnet policy already used `grants` — staying with grants is the path of least diff and aligns with where the product is going. |
| Tailnet advertised subnets: `10.0.0.0/16` supernet via `tag:subnet-router` | Subnet routers advertise the `10.0.0.0/16` supernet (covers every homelab VLAN — mgmt, asgard, jotunheim, client, storage, untrusted, plus any future /24 carved from `10.0.X.X`). Auto-approved via `autoApprovers.routes` so devices joining with `tag:subnet-router` self-approve without admin click. Exit node tag `tag:exit-node` auto-approves the IPv4+IPv6 default route. | Decided 2026-05-23 during 5e.3.d. Supernet over per-VLAN list because: (1) future VLANs get reachability for free; (2) one route to manage; (3) overshoot is minimal — the only "shouldn't be tailnet-reachable" subnets in `10.0.0.0/16` are HL-CLIENT (`10.0.60.0/24` — your own client devices, security non-issue from your own tailnet) and Untrusted (`10.0.222.0/24` — Tailscale ACL can scope which members reach it regardless of route advertisement). Routing decisions stay with UCG-Ultra; Tailscale just delivers packets to it. Day-1 ACL is default-allow within tailnet (split-auth model gates membership: servers via Vault-minted authkeys, user devices via Authentik OIDC + `tailscale-users` group gate). |
| Tailscale ACL policy in external HuJSON file | The `tailscale_acl.this.acl` field reads from `terraform/tailscale/policy.hujson` via `file()`. Not inline heredoc. | Decided 2026-05-23 during 5e.3.d. ACL grows over time as services + groups land; an external file gets HuJSON syntax highlighting in the editor, cleaner git diffs, and round-trips through Tailscale's visual policy editor (copy-paste between editor and file). The provider's `acl` field accepts both JSON and HuJSON, so no string-encoding overhead. `reset_acl_on_destroy = true` set on the resource for clean teardown; `overwrite_existing_content = false` (default) keeps the import-first safety gate. |
| Vault path convention: `secret/<consumer-domain>/...` (machine consumers only) | Vault KV entries are namespaced by the *consuming* domain, not the *writing* tool — for **machine** consumers. Secrets consumed by K8s workloads live under `secret/k8s/<service>/<key>`; secrets consumed by Ansible-on-LXCs live under `secret/ansible/<service>/<key>`. Path is independent of which Terraform module mints the secret — a Terraform module writing for an Ansible consumer writes to `secret/ansible/...`. **For human consumers, this rule does not apply — see "Secrets: two access patterns, three stores" in CLAUDE.md: human-consumed secrets live in 1Password, not Vault.** | Codified 2026-05-23 during 5e.3.e.iv; machine-consumers-only scoping added 2026-05-23 during 5e.3.f when the Munin Tailscale authkey surfaced the first human-consumer-but-TF-mintable case. Pre-existing layout had `secret/k8s/tailscale/oauth-client` (K8s/TF-consumed) + `secret/k8s/authentik/tailscale-client-secret` (K8s-consumed) + `secret/ansible/sftpgo/admin-password` (Ansible-consumed) — the convention was implicit. Tailscale LXC authkeys (Ansible-consumed but minted by `terraform/tailscale/`) initially went to `secret/k8s/tailscale/authkeys/<host>` matching the minter; corrected to `secret/ansible/tailscale/authkeys/<host>` to match the consumer. Generalization: consumer determines path; minter is only a write-time fact. Future TF modules minting secrets for Ansible consumption follow the same pattern. |
| Munin Tailscale authkey: out-of-band | Munin's Tailscale authkey is generated by hand in the Tailscale admin UI (by an admin user, single-use, `tag:subnet-router`), stored in 1Password "Homelab" vault, and used once at install time. NOT in Vault, NOT minted by Terraform, NOT auto-renewing. Tag assignment + auto-approve stay IaC via `policy.hujson` (`autogroup:admin` added to `tagOwners["tag:subnet-router"]` 2026-05-23 to enable admin-UI key minting with that tag). | Decided 2026-05-23 during 5e.3.f when the consumer-domain rule (machine consumers) and the 1P/Vault split (human → 1P) pointed opposite directions. Resolution: human-consumer rule wins. Rationale: (1) authkey is one-shot at install — Tailscale issues the device its own node key after auth, so the "auto-renewing for rebuilds" benefit doesn't apply to a physical NAS that won't be rebuilt routinely; (2) honest to "human lookup → 1P" instead of pretending it's an Ansible secret to keep path-convention symmetry; (3) avoids introducing a new "manual"/"human" Vault namespace just for this one case. Trade-off: small inconsistency with the LXC pattern (1113/1114/1115 are TF-minted + Vault-stored + auto-renewing), but the LXCs are routinely-rebuildable and need the auto-renewal; Munin is not. |
| Cloudflare zone security posture | Default-on bot/integrity protections + per-path WAF skip ruleset for protocol-public endpoints | Decided 2026-05-24 closing the standing "Cloudflare re-harden after stability" task left open during 5e.3.c. The global-disable from 5e.3.c was the unblock fix, not the correct fix. Production posture: `browser_check: on` + Bot Fight Mode `fight_mode: true`, with a `cloudflare_ruleset` resource (kind=zone, phase=`http_request_firewall_custom`, action=`skip`) bypassing all CF automated protections on `xiiisins.com/.well-known/webfinger` and `authentik.xiiisins.com/application/o/*`. Skip action carries `phases = [http_ratelimit, http_request_firewall_managed, http_request_sbfm]` + `products = [bic, hot, rateLimit, securityLevel, uaBlock, waf, zoneLockdown]` — the "let protocol-public callers through anything CF would automatically interpose" posture. Authentik OIDC discovery lives per-app under `/application/o/<slug>/.well-known/openid-configuration` (not at host root — see gotcha), so the `/application/o/*` prefix match covers the full set: per-app discovery + authorize + token + userinfo + jwks + end-session. Two-phase apply pattern (skip rule first via `--target`, then protections) to ensure skip rules are live before protections flip. |
| PG HA management — Patroni | Patroni-managed cluster with automatic failover, replacing the original streaming-replication + manual-promotion plan | Decided 2026-05-24 as 5g.2.a. Self-healing is a homelab design goal — manual primary promotion under failure means human operator latency adds to RTO, and is also a known-bad failure mode when the on-call doesn't have the cluster state in their head. Patroni adds a DCS (etcd) dependency and ~50MB RAM per PG node for the patroni daemon. Trade-off accepted: more moving parts, but the failure path becomes "system recovers in seconds" rather than "system pages a human." The "for resume signal" goal is also better served by Patroni — manual-promotion stacks are not what modern enterprises run. |
| Patroni DCS placement — etcd co-located on HAProxy nodes | etcd runs on the HAProxy LXC trio (Hlin/Eir/Snotra), NOT on PG nodes and NOT in a dedicated etcd trio | Decided 2026-05-24 as 5g.2.a. Two real wins over co-locating with PG: (1) **fsync separation** — etcd and PG are both fsync-heavy; sharing a LVM-thin volume puts them in competition (the 2026-05-14 etcd storm taught us how badly etcd reacts to fsync starvation, even though that was K3s etcd on slow hardware now-replaced); (2) **failure-domain separation** — a PG node death doesn't touch DCS quorum, and vice versa. Two real wins over a dedicated etcd trio: (1) zero additional LXC cost (HAProxy nodes were otherwise underutilized — pure TCP proxy + keepalived sits at <100MB RAM, single-digit CPU); (2) fewer moving parts overall. HAProxy/etcd LXCs sized 2vCPU/2GB to accommodate etcd's ~300-500MB working set + Raft log headroom. Less-textbook than dedicated etcd, but Patroni doesn't care where its DCS lives — this placement is a deployment choice, not an architectural compromise. |
| pgbouncer in the connection chain | Skip pgbouncer entirely. Workloads connect direct via HAProxy VIP → Patroni leader. PG `max_connections` raised from default to 300 to accommodate cluster-wide demand. | Decided 2026-05-24 as 5g.2.a after compatibility research (see [incident log](../incidents/README.md)). pgbouncer's homelab payoff is mostly theoretical: connection-multiplexing only pays at thousands of clients; failover transparency is already delivered by Patroni + HAProxy; prepared-statement reuse only matters at high QPS. Total demand across all 7 planned PG consumers (Authentik/Outline/Immich/NetBox/n8n/Zabbix/TeamSpeak) is ~150-200 connections — `max_connections=300` costs ~15MB RAM per extra connection, trivial vs the operational complexity of pgbouncer (per-workload pool modes, per-app config flags, monitoring). **Revisit triggers**: (a) any individual workload sustains >100 connections; (b) PG total connections regularly exceed 250; (c) a future workload's deployment docs explicitly recommend a pooler. At trigger, deploy pgbouncer *targeted* at the specific workload, not as a global infra layer. Documented compatibility per consumer in the research entry — Authentik/Outline/NetBox/n8n/Zabbix tx-mode safe, Immich session-mode only (advisory locks at startup), TeamSpeak unknown but low-connection-count moot. |
| PG consumer connection model | All consumers point at the single HAProxy VIP `10.0.10.210` for both reads and writes; no reader-vs-writer endpoint split | Decided 2026-05-24 as 5g.2.a. Patroni's HAProxy backend checks (`/master` returns 200 only on the current leader) ensure the VIP always points at the current writable primary. Splitting read traffic onto a replica-only endpoint would add app-side complexity (most ORMs don't handle R/W split cleanly without explicit code paths) for negligible benefit at homelab QPS. Replicas serve as failover targets, not active read load. Revisit if a single workload's read load saturates the leader; at homelab scale this is unlikely. |
| HAProxy/etcd trio sizing + placement | Hlin (1133/Urd), Eir (1134/Verd), Snotra (1135/Skuld). 2vCPU / 2GB RAM / 16GB disk / VLAN 11. Identical sizing across nodes per failover-symmetry rule. | Decided 2026-05-24 as 5g.2.a. 2GB RAM accommodates etcd (~300-500MB working set + Raft log) + HAProxy (~50MB) + keepalived (~20MB) with headroom for log/metric scraping. Disk: 16GB for etcd snapshots + WAL + OS, comfortable. Per-node placement matches the PG cluster pattern — one HAProxy/etcd LXC per Proxmox host (Urd/Verd/Skuld), maximises failure independence. Sizing-identical-across-nodes rule from the existing PG + K3s CP decision rows applies here too: asymmetric sizing means failover silently degrades. |
| Patroni install — source tarball from GitHub, pinned + SHA-verified | Patroni installed from `https://github.com/patroni/patroni/archive/refs/tags/v<patroni_version>.tar.gz` with explicit SHA256 verification, into a venv at `/opt/patroni` with `[etcd3,psycopg2-binary]` extras. Symlinks to `/usr/local/bin/{patroni,patronictl}`. Pinned via `patroni_version` + `patroni_sha256` in `ansible/roles/patroni/defaults/main.yml`. | Decided 2026-05-24 during Patroni-role write. PGDG apt ships a `patroni` package but the version floats between apt-pin tightenings and repo updates. Concrete-tarball-pin matches the existing K3s/etcd role discipline — Patroni upgrade is a deliberate operation (edit both fields, verify with `curl \| sha256sum`, re-run play). PyPI was the runner-up — same version-pin discipline via `pip install patroni==X.Y.Z` but losing one layer of attestation (no per-file SHA on the wheel without `--require-hashes`). `psycopg2-binary` over `psycopg3` because Patroni 4.x has a mixed-bytes-and-str-keys bug under psycopg3's strict type checks — see [Known gotchas](../../CLAUDE.md). **Generalized backlog**: mirror this tarball — and PGDG packages, etcd tarballs, K3s binaries, Tigera apt repo — to a self-hosted internal repo so version control lives inside the infrastructure, not on upstream CDNs. |
| Tailnet DNS — split DNS to AdGuard, no apex split | Internal zones `niflheim.xiiisins.com` + `midgard.xiiisins.com` split-DNS'd via Tailscale to AdGuard VIP `10.0.10.200`. Apex `xiiisins.com` deliberately NOT split. MagicDNS enabled (gate that makes split-DNS reach the client OS). Search path `niflheim.xiiisins.com` pushed so bare hostnames resolve. Single resolver target per zone (the VIP, not the three AGH node IPs). | Decided 2026-05-24 as 5e.4. Discovered the gap when a tailnet-connected client couldn't resolve `vor` / `vor.niflheim.xiiisins.com` — 5e.3 deployed LXCs and subnet-router connectivity but left DNS to client-local resolvers, which NXDOMAIN'd both internal zones. Three independent calls in the decision: (1) **don't split apex** — apex records already resolve publicly (`authentik.xiiisins.com` → Cloudflare tunnel); splitting it on tailnet would create vantage-point-dependent resolution behavior, breaking "one zone, one path"; (2) **DO split midgard** — AGH rewrites (e.g. `authentik.midgard → 10.0.20.10`) are AGH-only state, public DNS NXDOMAINs midgard, so tailnet clients must consult AGH to see those records; (3) **VIP only as resolver, no fallback to backing nodes** — VIP is the published contract, and listing backings (.201/.202/.203) would couple tailnet config to AGH's HA topology and mask VIP outages a client would otherwise notice. Search path only pushes niflheim, not midgard — niflheim holds actual host records, midgard is service aliases accessed by full FQDN; pushing both would create short-name ambiguity for any record present in both zones. Implementation surfaced: provider 0.28.0 resource is `tailscale_dns_split_nameservers` (one-per-domain), not the map-form `tailscale_dns_split_dns` from later versions; OAuth client needed `dns:write` scope added — accepted in-place via Tailscale admin UI with no remint required. |
| HAProxy/etcd LXC dual-NIC | eth0 on VLAN 11 (etcd peer + Patroni REST API + default gateway) + eth1 on VLAN 10 (VIP + VRRP) at `10.0.10.233/234/235`. Each LXC has a real address on the VIP segment. | Decided 2026-05-24 during 5g.2 HAProxy/keepalived step. VRRP requires all peers L2-adjacent on the VIP's broadcast domain, so the VIP's VLAN must terminate on every keepalived peer — needs an interface + address per node. Putting only the floating VIP on eth1 (no host IP) works for VRRP claim but loses diagnostic clarity and the source IP for VRRP advertisements. Pattern reusable for any future VIP whose L2 segment differs from the service IP — same shape on AGH (LXCs on VLAN 11, VIP on VLAN 10). |
| keepalived election model — all peers BACKUP | Every `vrrp_instance` sets `state: BACKUP` and relies on `priority` for election (no static MASTER). Per-host priorities via `keepalived_priorities` map. | Decided 2026-05-24 during 5g.2. Static-MASTER + BACKUP-others would let dual-MASTER happen on simultaneous boot when interfaces come up out of order — a static MASTER announces itself authoritatively before noticing the other peer also announcing. All-BACKUP defers to priority, so the election is deterministic regardless of who starts first. Trade-off accepted: ~1 advert interval (1s) longer to first VIP-claim on cluster cold start, since no node is pre-confident enough to claim immediately. |
| VRID spacing convention | Allocate `virtual_router_id` in increments of 10 (AGH=51, PG=61, next=71, ...) across all keepalived clusters on a shared L2 segment. | Decided 2026-05-24 immediately after discovering VRID-51 collision between AGH (manually configured) and PG (5g.2 first deploy) — both on VLAN 10 HL-ASG-VIP, both at VRID 51, both logging "received an invalid passwd from <peer>" continuously because of mismatched `auth_pass`. Spacing-by-10 leaves room to insert intermediate clusters without renumbering, and "always a multiple of 10" makes the rule self-checking when reading a new keepalived config. Allocations live as a comment in `ansible/inventory/group_vars/haproxy_etcd_hosts.yml`; future VIPs grep for existing values before picking. |
| HAProxy + keepalived install method — Debian apt | Both installed from the Debian apt repository (not source tarballs, not pinned versions). `haproxy` package + `keepalived` package, current Trixie versions. | Decided 2026-05-24 during 5g.2. Both are stable distro packages with reasonable upgrade cadence and well-understood CVE response from Debian's security team. Unlike Patroni (which we pinned to source tarball + SHA because PGDG apt floats the version), HAProxy + keepalived's Trixie versions are stable APIs unlikely to break our config across point-release upgrades. The discipline is calibrated: pin upstream-fast-moving things, accept the distro's cadence for upstream-slow-moving things. Revisit if either package breaks our config across a point-release. |
| Generic Ansible roles — data-driven via list-of-dicts | `haproxy` and `keepalived` roles are backend-agnostic. All configuration via `haproxy_listens` / `keepalived_vrrp_instances` / `keepalived_vrrp_scripts` / `keepalived_source_policy_routing` list-of-dicts supplied by the caller (group_vars). Roles never reference PG, AGH, or any specific consumer. | Decided 2026-05-24 during 5g.2 role write. Specific-purpose roles (one role per backend) would scale poorly with planned future VIPs (Teamspeak, observability dashboards, etc.) and would scatter HAProxy/keepalived knowledge across role forks. Generic roles + per-group_vars data is the same shape as Ansible community roles (`geerlingguy.haproxy`, `evrardjp.keepalived`, etc.) which validates the pattern at scale. Trade-off: caller has to learn the schema (documented in role READMEs with worked examples), but the schema mirrors the upstream config so anyone who knows HAProxy/keepalived configuration recognizes it immediately. Escape hatches at every level (`_extra` raw-line lists, `extra` per-listen/per-server fields) cover features the role doesn't model. |
| NetBox placement — asgard K3s, not jotunheim | NetBox deployed in asgard K3s (same cluster as Authentik / Vault / etc.), exposed internal-only via `netbox.niflheim.xiiisins.com` HTTPRoute. NOT in jotunheim (which doesn't exist yet anyway). | Decided 2026-05-24 as 5i scope, correcting a stale memory entry that placed it in jotunheim. Reasoning: (a) asgard is the production cluster; jotunheim is the "can implode" cluster — NetBox is operationally critical for IPAM lookups during recovery, must live where reliability lives; (b) PG (Patroni) and Authentik OIDC are both in asgard, so asgard placement avoids cross-cluster dependencies; (c) the "lookup during cluster outage" disaster scenario is addressed by the deferred recovery LXC (Phase 5i.2) — fully independent with local PG from PBS pg_dump, local admin auth, no OIDC — NOT by cross-cluster placement of the primary. |
| TF→NetBox integration direction — Option A (TF writes NetBox records) | Every new LXC/VM TF resource MUST declare matching `netbox_virtual_machine` + `netbox_ip_address` (etc.) via `e-breuninger/netbox` provider. TF stays the source of truth (Git is the spec); NetBox is the queryable view written by TF. Implementation tight-coupled (provider in the same module as the LXC resources) + dedicated `terraform` NetBox user + retrofit existing records via `terraform import`. | Decided 2026-05-24 as 5i closure (consensus after the 5i.c three-way research between TF→NetBox writes, NetBox→TF reads, webhook-driven runs). Reasoning: (a) Git is already the IaC spec per existing discipline — inverting to NetBox UI as spec would be a regression; (b) Option B (NetBox→TF) makes NetBox a hard runtime dependency of `terraform apply` and exposes the IP-allocation race in `netbox_available_ip_address` (issue #59); (c) Option C (webhook-driven) needs remote TF state + a runner, neither of which we have; (d) `e-breuninger/netbox` v5.3.0 is the only actively-maintained provider and is the consensus pick for official-provider promotion in the NetBox community discussion. Implementation deferred to Phase 5i.3 (next phase, not "deferred-indefinitely"). |
| CoreDNS rewrite for K8s-fronted internal FQDNs | When a pod needs to reach an internal FQDN whose external resolution is a MetalLB VIP (the K8s-edge LB), CoreDNS rewrites the query to Traefik's ClusterIP DNS so the pod bypasses the VIP tromboning. Configured via K3s's `coredns-custom` ConfigMap with `*.override` keys (NOT `*.server`). One `rewrite name exact <fqdn> traefik.traefik.svc.cluster.local` line per K8s-fronted FQDN. External clients (browsers, tailnet) unaffected — they resolve via AGH directly. | Decided 2026-05-24 as 5i.d. Generalises the pre-existing "Cloudflared targets ClusterIP DNS, NEVER MetalLB IPs" rule (which used Cloudflared's app-level config) into a cluster-wide CoreDNS-level mechanism that works for any pod with any HTTP client — including ones with no Host-header-override / TLS-verify-disable capability (e.g. python-social-auth, which NetBox uses for OIDC). The Traefik ClusterIP target resolves at query time via cluster DNS, so it's volatility-safe (Service recreation gives a new IP, DNS resolution follows). SNI stays the original FQDN → matches Traefik's wildcard cert → TLS validates cleanly without `noTLSVerify` cheat. Anti-pattern to avoid: rewriting FQDNs that point to LXCs/VMs (those aren't behind Traefik). Add rewrites as new K8s-fronted FQDNs land; do NOT blanket-rewrite the whole `*.midgard` / `*.niflheim` zone. |
| TF→NetBox auth model — admin token, no dedicated `terraform` user | The 5i closure decision specified a dedicated `terraform` NetBox user + token managed via TF. Reversed during 5i.3 implementation: `terraform/netbox/` authenticates as the NetBox admin user via env-var `NETBOX_API_TOKEN` (sourced from 1P "Asgard - NetBox - admin API token" via homelab.sh). The originally-planned `netbox_user.terraform` + `netbox_token.terraform` resources were dropped from the module. | Decided 2026-05-24 mid-5i.3 Stage 1 after discovering three e-breuninger/netbox v5.3.0 incompatibilities with NetBox 4.6 that made the dedicated-user pattern impossible: (a) `netbox_token` POST+immediate-PUT pattern is rejected by NetBox 4.4+ v2 token API ("Cannot assign a new plaintext value for an existing token"); (b) when create does succeed, the v2 secret is only in the POST response and the provider's schema has no `token` field, so we can't capture it for Vault; (c) `netbox_user` doesn't expose `is_superuser`, so a TF-created user can't be granted permissions to actually manage anything. Workarounds (manual UI elevation + manual token mint + manual Vault write + terraform import) would have been more moving pieces than the value justified. Admin token is operator-side anyway (TF runs from operator workstation, not in-cluster), so the security delta is minimal. Revisit if/when the provider catches up to NetBox 4.6 — track via [upstream issue #349](https://github.com/e-breuninger/terraform-provider-netbox/issues/349) class of token+user resource fixes. |
| Asgard K3s worker memory baseline — 8 GB | K3s worker VMs (Einherjar-urd/verd/skuld) sized at 8192MB each (was 4096MB). Floor for any future worker added to asgard. | Decided 2026-05-24 mid-Phase 5i.3 as incident response. Original 4G was sized for the "small homelab" workload baseline; phase-after-phase additions (Vault 3-replica, Authentik+Redis, NetBox medium preset + bundled Valkey, system daemons) compounded RSS demand past what 4G could hold. Rolling-restart of NetBox during 5i.3 Stage 0 cascaded an OOM chain: pod migrates → new node OOMs → kubelet dies → pod migrates to another node → repeat. Verd worker hit load 40 with no memory available; userspace hung while kernel kept responding to ICMP. Bumped sequentially via `terraform apply -target=...` (bpg/proxmox treats memory.dedicated as in-place update + VM reboot, NO recreate). Skuld host (N100/16GB) is the tightest after the bump — accepted; revisit if Skuld saturates. Full retro: [`docs/incidents/2026-05-24-worker-oom-during-netbox-rolling-restart.md`](../incidents/2026-05-24-worker-oom-during-netbox-rolling-restart.md). **Superseded — current baseline is 16 GB:** later raised 8 GB → 16 GB as cumulative workload grew (commits `a7056ea` right-size per host, `68668aa` final node to 16 GB), and Skuld was since refreshed off the N100 to an MSI Cubi i3-1215u/32GB so it's no longer the tight node. `locals.workers` in `terraform/proxmox/asgard-k3s/main.tf` is authoritative (`memory = 16384`). |
| Phase 8 observability stack — no Grafana | VictoriaMetrics + VictoriaLogs in asgard K3s, no Grafana. Dashboards via vmui (built into vmsingle) + the native VictoriaLogs UI. The Phase-8 manifests in `k8s/asgard/apps/` reflect this — only `victoriametrics/` + `victorialogs/` + `monitoring-namespace/`, no `grafana/` subdir. | Decided 2026-05-24 during Phase 8 prep, before any deploy. Rationale: vmui covers interactive PromQL + chart playground; VictoriaLogs UI covers LogsQL; together they handle the ad-hoc query + small-dashboard workflow that's been the actual homelab need. Grafana adds a dashboards-as-code layer + cross-source annotations that we don't yet have a real use case for — and adds an OIDC client, an admin password Vault path, a chart-pinning + sqlite-PV lifecycle to maintain. Drop it until we have a concrete trigger. Revisit triggers: (a) need canned dashboards mixing metrics + logs with synchronized time ranges, (b) need to share read-only dashboards with non-operator users (Zabbix already handles infra-level shared status), (c) need to share dashboards across clusters. Until any of those hit, vmui + VL UI is enough. |
| VM/VL placement — asgard, not jotunheim | VictoriaMetrics + VictoriaLogs land in **asgard K3s** (Phase 8 manifests at `k8s/asgard/apps/{victoriametrics,victorialogs,monitoring-namespace}/`). Earlier draft put them in jotunheim per the "monitoring independent from production" instinct; reversed before any deploy. | Decided 2026-05-24 during Phase 8 prep. Three reasons for asgard: (a) jotunheim's deploy timeline is uncertain (Phase 7 is unbuilt) — staging Phase 8 there blocks observability on the larger cluster-bootstrap project; asgard already has the infrastructure VM/VL need (Synology CSI, MetalLB, Traefik, ESO, Vault). (b) The bulk of log/metric producers live in asgard (K3s pods, the LXC fleet on VLAN 11). Keeping VL in the same cluster as the producers shortens the ingest path: pod → in-cluster Service DNS → VL. Versus a cross-cluster path: pod → MetalLB on asgard → routed cross-VLAN → jotunheim MetalLB → VL. (c) Failure-domain separation is preserved by **Zabbix** (5h LXC, fully outside K3s). When asgard K3s is the thing on fire, Zabbix still tells us about it — VL going down with the rest of asgard is an acceptable secondary loss. Revisit if/when jotunheim is up + we want a "view asgard's logs from outside asgard" capability for major-outage diagnostics; trivial to migrate VL+VM to jotunheim later (move manifests + flip shipper endpoints). |
| AGH rewrites — Terraform managed via gmichels/adguard, write-to-origin | DNS rewrites declared in `terraform/adguard/rewrites.tf` as a `locals.rewrites` map (FQDN → answer), one `adguard_rewrite` resource via for_each. Provider targets Saga (10.0.11.201) directly — the AGH **origin**, not the keepalived VIP — and adguardhome-sync fans writes out to Mimir + Kvasir. 27 existing hand-created rewrites retrofitted via TF 1.7+ for_each `import {}` block. Provider auth via env vars `ADGUARD_HOST`/`ADGUARD_SCHEME`/`ADGUARD_USERNAME`/`ADGUARD_PASSWORD`, loaded by `homelab-env` from 1P item "Adguard - admin" + the static map. | Decided 2026-05-24. Triggered by the standing "AdGuard DNS records for new VMs/services as provisioned" open-questions item — adding records via the UI scaled to ~27 entries fine, but the next class of work (Phase 8 observability, future apex-LAN-bypass entries, NetBox-derived projections) wanted them in Git for diff-reviewability. Three sub-decisions: (1) **write to the origin, not the VIP** — VIP is the published contract for *readers*, but writers must target Saga because that's what adguardhome-sync treats as the source of truth; writing to the VIP races whichever node is currently elected and inverts the sync direction. Parallels the existing tailnet-DNS "VIP for readers, not for backings" pattern from `terraform/tailscale/dns.tf`. (2) **flat domain→answer locals map, not list-of-pairs** — gmichels/adguard's import ID is `domain\|\|answer`, so domain-keyed is the natural shape; if multi-answer per domain ever becomes a need (round-robin / fallback), restructure to list-of-pairs keyed on `${domain}\|\|${answer}`. (3) **provider auth env-driven, not Vault-backed** — AGH admin is operator-consumed (humans log in via UI with the same account), so 1P is the right home per the Homelab vault scope rule. Mirrors the netbox+tailscale auth pattern. Future direction: drive the locals map from a NetBox `data "netbox_ip_addresses"` source filtered on `dns_name != ""` so AGH becomes a thin projection of NetBox truth — deferred until scale-of-pain justifies (see `project_source_of_truth_layering.md` memory). |
| Terraform state backend — S3 with native locking, key-per-module | Remote backend is S3 bucket `xiiisins-homelab-tfstate` (eu-west-1) with Terraform 1.10+ native locking (`use_lockfile = true` — no DynamoDB). Key-per-module mirrors the repo tree: `cloudflare/terraform.tfstate`, `proxmox/asgard-k3s/terraform.tfstate`, `vault/terraform.tfstate`, etc. `terraform/aws/` (the bootstrap module that creates the bucket + IAM user `terraform-state` scoped to bucket-only ops) keeps LOCAL state by chicken-egg necessity — can't store its own state in a bucket it hasn't created yet. State user is bucket-scoped GetObject/PutObject/DeleteObject + ListBucket only (no s3:ListBucketVersions — versioning visibility is separately gated). Per-module `versions.tf` declares `required_version >= 1.10.0` (use_lockfile floor). 8 modules migrated from local state on 2026-05-25 (cloudflare, adguard, authentik, netbox, proxmox/asgard-k3s, proxmox/asgard-lxcs, tailscale, vault); local state files moved to `~/tmp/stale/tfstate/<module>/` rather than deleted, recoverable for ~30d. | Decided 2026-05-25, superseding the 2026-05-23 "local-only forever" position. **Why now:** the `terraform/proxmox/asgard-lxcs-root/` split (separate decision row) wanted its OWN state file, and the workspaces-vs-separate-modules choice settled on separate modules — which immediately wanted state in a place shared between operator + future runners + the asgard-lxcs/asgard-lxcs-root pair. Multi-operator wasn't the trigger; the **shared-key-namespace + per-module-locking** properties of a real backend were. Picked S3 over Vault KV (homelab Vault would be inside one of the things TF deploys — bootstrap circular) and over Terraform Cloud (vendored proprietary backend for what's a 10MB-of-JSON storage problem). Terraform 1.10+ native locking removes the historical DynamoDB cost+complexity. State still contains decrypted secrets — same private-bucket discipline applies (no public read, IAM-scoped, encryption-at-rest on). The chicken-egg `terraform/aws/` exception is the standard pattern; same shape as `terraform/vault/` bootstrapping itself with HCL that references Vault. |
| LXC TF modules split by Proxmox auth class | Two modules under `terraform/proxmox/`: `asgard-lxcs/` (single API-token provider, holds normal LXCs — Factorio, Postgres, HAProxy+etcd, AdGuard, Zabbix) and `asgard-lxcs-root/` (single root@pam provider, needs `PROXMOX_VE_PASSWORD` env, holds LXCs whose create-time config touches Proxmox API endpoints that only accept ticket auth — today just Tailscale's `device_passthrough` for `/dev/net/tun`, future: `fuse`, `keyctl`, additional passthroughs). Backend keys `proxmox/asgard-lxcs/` and `proxmox/asgard-lxcs-root/`. Add new LXC: pick by auth class, not by service-family. The `-root` suffix names the **Proxmox auth axis**, not LXC privilege — the LXCs themselves are unprivileged. | Decided 2026-05-25 closing two open-question entries dating to 5g.2.b + 5b.2 friction. Originally `terraform/proxmox/asgard-lxcs/` carried an aliased `proxmox.root` provider just for Tailscale's three LXCs (the only `device_passthrough` consumer); this forced `PROXMOX_VE_PASSWORD` in env on *every* apply — including AGH / Postgres / Factorio applies that only need the API token — recurring `op read` foot-gun. Three sub-decisions: (1) **split by auth class, not by service-family** — earlier framing floated `asgard-tailscale-lxcs/` (service-specific) or `asgard-lxcs-privileged/` (the open-question's wording). Auth-class wins because the failure mode is auth-driven (token vs ticket), and any future `fuse` / `keyctl` / additional `device_passthrough` LXC will join the same module rather than spawn a third. Service-specific naming would force renaming on the second occupant; "privileged" misnames the LXCs (which are themselves unprivileged). (2) **separate state files per module, not workspaces** — workspace-per-auth-class would share the providers config + force the same TFstate object's resources to all carry the wrong-provider blast radius. Separate modules with separate S3 backend keys give true isolation. State migration via `terraform state pull` → local-file `terraform state mv` → `terraform state push` to both backends preserved all 26 resource instances cleanly. (3) **manually-called `set-proxmox-password` in the `homelab-env` shim, NOT autocalled** — added to both shim files (`.config/scripts/homelab.sh` + `.config/fish/conf.d/homelab.fish`) mirroring `set-aws-creds` shape, but excluded from the `homelab-env` autocall path because PROXMOX_VE_PASSWORD's only consumer is a single TF module + occasional ad-hoc work; loading on every shell would be wasted 1P churn. Cache write-through follows the same pattern as VAULT_TOKEN and AWS creds — set once, subshells inherit via `~/.cache/homelab/env.{sh,fish}`. |
| Notifications — Apprise on a dedicated LXC, tag-driven routing | Single AppriseAPI instance on LXC 1103 (Hermod, Verd, 10.0.11.22) acts as the homelab's notification aggregation point. Sources POST JSON to `/notify/<key>` with `title`, `body`, `type` (info/success/warning/failure → drives Discord embed colour), `tag` (critical/alert/media/... → drives **routing**), and `format`. Hermod's Apprise yaml maps tags → Discord webhook URLs (one per channel) read from Vault under `secret/ansible/hermod/discord/<tag>`. Routine successes are NOT routed to Hermod — vlagent already ships every host's syslog/journald to VictoriaLogs, which IS the audit trail; adding a parallel hub→VL path duplicates infrastructure. | Decided 2026-05-25, locked in as Phase 5h.2 (sequenced after Zabbix LXC because Zabbix is the first concrete alert producer). Three sub-decisions: (1) **LXC, not K8s pod** — alerts about K3s being down must fire when K3s is down; same independent-failure-domain rationale as Zabbix LXC outside K3s. (2) **AppriseAPI** (caronc/apprise-api) over per-source webhooks because adding a new delivery channel (ntfy, email, Slack) shouldn't touch source-side code; AppriseAPI is OSS, stateless, 80+ backends, single-Python-process small. Alternative considered: K8s CronJob + raw curl per source — rejected because every source then needs the destination URL set + every destination change is N source changes. (3) **Tag-driven routing, not severity-driven** — severity (`type` field) is a *display* concern (Discord embed colour); routing is an *operational* concern (which channel, which destination). Decoupling means `tag: media` (future Sonarr) reuses the same hub without inheriting infra-alerts' destination set. Full design + schema + source→tag mapping table + LXC spec in [`docs/services/notifications.md`](../services/notifications.md). |
| Notification severity — response-time semantic, not impact descriptor | Tags are defined by *expected response time*, not by impact words. `critical` = "look within minutes, even at 2am" (cluster quorum lost, environment down, service hard-unavailable, disk >90%); `alert` = "look within hours, business-day OK" (single-node failure but cluster operational, drift detected/correction-failed, sustained 5–15 min resource load, disk 70–80%); `media` (future) = "whenever". Untagged events go to VictoriaLogs only via the vlagent pipeline (drift-check-clean, scheduled-reconcile-OK, routine success). | Decided 2026-05-25 alongside the Apprise architecture row. Response-time framing makes the tag boundary unambiguous when a new event-type lands ("does this need 2am attention?" → critical, "can it wait for tomorrow?" → alert, "it's just status?" → no tag). Impact-descriptor framing ("service unavailable" vs "node down") drifts because reasonable people disagree on which descriptor a given event matches. Response time is observer-relative (operator commitment), not event-relative (technical truth), so the boundary stays sharp. Source-side mapping table (Zabbix Disaster/High → critical, Average → alert; VMAlert severity:critical → critical, warning → alert; Semaphore apply-fail → critical, drift-check fail → alert; Patroni leader change → alert, all-replicas-lost → critical) is the policy artifact — committed in the design doc, PR-gated to change. |
| Ansible playbooks — per-host-group naming, not per-role | One playbook per inventory host-group: `ansible/playbooks/asgard-<service>.yml` runs every role that group needs (baseline + service roles + agents + hardening). `site.yml` is the top-level orchestrator via `import_playbook` of every `asgard-*.yml` + cluster-wide agent playbooks (`vlagent.yml`, `zabbix-agent.yml`). Cluster-wide agent rollouts stay as their own playbooks so agent deployment is a deliberate one-shot rather than an implicit side-effect of every group's playbook. The old `*-host.yml` / `*-hosts.yml` / `*-agents.yml` suffix mix is collapsed — host-group affinity is implicit in the playbook name + `hosts:` selector. | Decided 2026-05-25 as part of Phase 5h.3. The current layout grew per-role + per-host-group + per-agent-fleet playbooks ad-hoc; adding a sixth or seventh group surfaced the naming inconsistency (`-host` singular for service stacks, `-hosts` plural for groups, `-agents` for cluster-wide rollouts). Per-host-group as the single rule resolves three problems: (1) "what state does this group of hosts hold?" has one answerable file; (2) adding a new role to a group is a one-place edit; (3) `site.yml` becomes the contract for "converge everything." Alternative considered: per-role playbooks driven by group selection at invoke time (`ansible-playbook postgres.yml -l postgres_hosts`) — rejected because it pushes the "which roles run together" knowledge to invocation sites instead of into version control. |
| Ansible playbooks — multi-play files when `serial:` differs | A single playbook file can hold multiple `- name:` plays with different selectors + serial settings. When one stage needs ordered rollout (Patroni replicas first via `serial: 1`) and another stage should run in parallel (agent install across the same group), use two plays in one file. Don't split into separate playbook files. | Decided 2026-05-25 as part of Phase 5h.3. The pattern landed because `asgard-postgres.yml` needs `serial: 1` for the PG/Patroni stack but doesn't need it for the agent rollout — paying the serial=1 tax on agent install would triple a parallel-friendly task's wall time. Two-files-per-group works but inflates `site.yml`'s `import_playbook` list and obscures the "this is one group's state" contract. Multi-play within a file keeps the group-to-file invariant and lets each play choose its own serial. |
| Ansible scheduler — Semaphore in asgard K3s (over AWX) | Push-mode scheduled execution via [`semaphoreui/semaphore`](https://github.com/semaphoreui/semaphore) deployed as a StatefulSet in asgard K3s (`semaphore` ns, sqlite on iSCSI PVC, Authentik OIDC, internal-only HTTPRoute on niflheim Gateway). Three core task templates: `refresh-netbox-inventory` (cron 4h, manual-refresh on demand), `asgard-drift-check` (cron 6h, `--check --diff` on `site.yml`, results → Hermod `tag: alert` on diff/failure), `asgard-apply` (manual, `tag: critical` on failure). Semaphore's webhook notifications POST to Hermod per the notification JSON schema. | Decided 2026-05-25 closing the long-running "scheduler choice" question. AWX rejected on resource footprint (3 GB image, multi-pod operator + Postgres + Redis + Receptor + Task) — overkill for the homelab's ~10-playbook scope. Semaphore is a single Go binary + sqlite, ~100 MB resident, single-replica StatefulSet. The "so-so work experience" caveat with Semaphore came from an environment lacking NetBox host-groups in the dynamic inventory; resolved here by `group_by: tag` projecting NetBox tags as Ansible groups (separate decision row). Revisiting Semaphore is cheap because the playbook structure is scheduler-agnostic — any future swap (AWX, Rundeck, K8s CronJob + raw ansible) doesn't need playbook changes. Considered alternatives: K8s CronJob + raw ansible-playbook in a pod (rejected: no UI, no run history, no per-task secret-injection convention worth keeping); Rundeck (rejected: Java footprint and more complex setup for the same outcome); operator-only execution from MacBook (rejected: defeats the drift-detection-without-operator goal — the whole point is that drift gets caught when the operator isn't running playbooks manually). |
| Ansible inventory — NetBox dynamic + static hosts.yml as DR fallback | Primary inventory source is `netbox.netbox.nb_inventory` plugin querying NetBox at `https://netbox.niflheim.xiiisins.com`. Group membership flows from NetBox tags via `group_by: tag` (convention: `ansible:<group>` tags like `ansible:postgres`, `ansible:k3s-cp`). jsonfile cache, 4 h TTL, manual-refresh template in Semaphore. Static `ansible/inventory/hosts.yml` retained permanently as DR + day-1 + NetBox-down fallback; merged via Ansible's inventory plugin chain. File-based `group_vars/` / `host_vars/` continue to overlay on top of dynamic facts for Ansible-specific knobs that don't belong in NetBox. | Decided 2026-05-25 as part of Phase 5h.3. Splits the source-of-truth cleanly: NetBox answers "what is this host?" (hostnames, IPs, group membership via tags, primary interface metadata); files answer "how should Ansible configure it?" (passwords via Vault lookup, tunable knobs, per-env overrides). Static `hosts.yml` retained — not deleted — because (a) day-1 bootstrap precedes NetBox existing, (b) NetBox-down + cache-expired needs a path forward, (c) cluster-rebuild from cold should not have an Ansible dependency on NetBox being up first. 4 h TTL chosen as the routine-add-VM-to-NetBox-and-it-gets-picked-up cadence; manual refresh covers "I just added it, run *now*." Per the source-of-truth-layering memory: shape NetBox data so the eventual swap from static→dynamic→NetBox-only is a data-source change, not a re-architecture. |
| Drift detection — `--check --diff` against site.yml, no auto-apply | `asgard-drift-check` runs `ansible-playbook site.yml --check --diff` against the whole fleet on a 6 h cadence via Semaphore. Clean → no notification (audit trail in VL via vlagent). Drift detected (`changed=N`) → Hermod `tag: alert` with diff summary. Hard failure → Hermod `tag: alert`. Manual `asgard-apply` template is the human-triggered converge step. No scheduled auto-apply. | Decided 2026-05-25 as part of Phase 5h.3. Two posture choices baked in: (1) **alert on diff, not on clean** — silence is the steady-state signal; alerts mean operator attention. Notifying on every clean cycle is noise that trains operators to ignore the channel. (2) **No scheduled auto-apply** — drift-check produces a signal, a human decides whether to converge. Removing the human is a separate decision with separate failure-mode implications (a drift-detection bug + auto-apply = silent self-corruption; with human-in-loop the bug surfaces as an unexplained alert and gets caught). Check-mode false-positives are a per-role responsibility — when a role surfaces noise in `tag: alert` from `--check` not matching real apply, the fix is in the role's task design (e.g. `check_mode: false` on POST-then-read patterns), not in the drift-check template. |
| Zabbix promotion — Phase 8c (observability core), not Phase 5h (long-tail LXC) | Zabbix gets its own phase as part of the observability stack (8a = K8s metrics/logs, 8b = vm-operator refactor, 8c = host/LXC metrics+alerts), not bundled under "remaining LXCs". | Decided 2026-05-25 during 8c pre-implementation planning. The "Zabbix is its own Phase 5h LXC" wording in the previous Phase 8 build-sequence summary + the "Zabbix LXC (Phase 5h, pending)" framing in observability.md undersold what Zabbix actually does for the homelab: it's the K3s-independent failure-domain that lets observability survive a cluster outage, and the structural counterweight to 8a's in-cluster placement (Phase 8a's "no cross-cluster placement of VL/VM" decision is explicitly justified by Zabbix's separation — see "VM/VL placement — asgard, not jotunheim" row above). Treating it as long-tail-LXC backlog buries that role and the decision graph reads wrong. Reclassification is documentation-only — no architectural change. |
| Zabbix UI auth — native SAML, not Traefik ForwardAuth | Authentik configured as SAML 2.0 IdP via `authentik_provider_saml`; Zabbix 7.0's built-in SAML support handles the auth round-trip end-to-end. NameID = email; group claim drives JIT user provisioning into Zabbix usergroups. `homelab_admins` Authentik group gates application access. | Decided 2026-05-25 during 8c pre-implementation planning. Two alternatives considered + rejected: (a) Traefik ForwardAuth + Zabbix HTTP-auth header mode — would have mirrored vmui/VL pattern (Phase 8a) for max consistency, BUT vmui/VL only got that pattern because they have no native SSO; Zabbix does. Picking ForwardAuth here would default to it for the wrong reason (familiarity, not necessity), and adds a K3s dependency for what's supposed to be a K3s-independent service's auth path. (b) Native OIDC — Zabbix 7.0 LTS only has OIDC for MFA/secondary factors, not as the primary web UI auth backend; would require waiting for 7.2+ or building a proxy. SAML is the load-bearing primary auth in Zabbix 7.0 and works today. Trade-off accepted: cluster-wide auth pattern fragments slightly (ForwardAuth for K3s-native apps without SSO, native-SAML/OIDC for apps that have it), but each app uses its strongest available path. |
| Zabbix LAN TLS termination — Traefik fronts the LXC, plus operator backdoor | `hugin.midgard.xiiisins.com` (LAN) + `hugin.xiiisins.com` (WAN via Cloudflared) both route through Traefik on the midgard Gateway. K8s Service with no selector + manually-managed EndpointSlice points at LXC `10.0.11.21:80` as Traefik's external backend. Plain HTTP between Traefik and the LXC over VLAN 11; TLS terminates at Traefik using the existing `*.midgard.xiiisins.com` + apex wildcard certs. Operator backdoor: `hugin-direct.niflheim.xiiisins.com` → LXC nginx :80 direct, bypassing Traefik entirely, for K3s-down emergency UI access (local-Admin auth only, no SAML). All three FQDNs are identity-themed (`hugin.*` not `zabbix.*`) — matches the LXC's Norse identity. | Decided 2026-05-25 during 8c pre-implementation planning. Two alternatives considered + rejected: (a) **LXC nginx terminates TLS itself** — would have kept Zabbix UI fully K3s-independent for non-emergency access (which is the whole point of Zabbix-on-LXC). BUT introduces a new pattern (cert-manager-on-LXC or manual certbot) for one service, and the existing wildcard certs already cover both hostnames for free via Traefik. WAN path also gets more complex (Cloudflared → LXC :80 direct vs Cloudflared → Traefik ClusterIP DNS, the apex pattern already established for Authentik). (b) **No backdoor URL** — single SSO entry point would be cleaner from an access-control standpoint, but means a K3s-or-Traefik outage that takes the SSO path down ALSO takes UI access down — even though Zabbix server itself is still collecting data + firing alerts. The backdoor preserves Zabbix's "see infra when infra is on fire" promise without weakening the normal-path auth posture (backdoor's local-Admin password is in Vault + 1P; it's not unauthenticated, just SSO-bypass). |
| Self-hosted S3 — Garage (not MinIO, not SeaweedFS) | Garage v2.3.0 single-node (RF=1, `consistency_mode="dangerous"`, LMDB) in asgard K3s. First homelab S3 layer; first consumer Outline, planned future consumers Immich + backup targets. Operator workflow: TF (`terraform/garage/`, jkossis/garage v1.0.4) hits the admin API via `kubectl port-forward`. | Decided 2026-05-25. MinIO ruled out post-OSS pivot — no longer trustable upstream. SeaweedFS skipped because its POSIX/WebDAV surfaces are redundant with the existing iSCSI (StorageClass `synology-csi-iscsi-retain`) + NFS (Munin shares) paths, and the homelab already has cross-platform file access covered without adding a new abstraction. Garage's simplicity wins: Rust + LMDB + distroless image + admin API. Single-node + RF=1 is the right shape until a second NAS lands — replication "inside" Garage doesn't add safety while the data already lives on a single Synology, and `consistency_mode=dangerous` skips quorum reads we don't need. |
| Outline placement — asgard K3s (`k8s/asgard/apps/outline/`) | K3s app, not LXC. PG on Patroni VIP, Redis as in-namespace sidecar StatefulSet, S3 via Garage in-cluster, OIDC via Authentik. Three hostnames (apex public via cloudflared + midgard LAN-fast-path + niflheim internal-only). | Decided 2026-05-25 as part of the wiki bring-up. Same K3s-over-LXC reasoning as Authentik/NetBox/Teamspeak — GitOps uniformity (Flux+ESO), no new operational pattern needed, scales with the rest of the cluster. The wiki's purpose is *living* docs (HOWTOs, friends/family-readable how-to-use-the-homelab content) distinct from git-tracked `docs/` (deploy-shape truth). Access gated through the `outline-users` Authentik group; per-user opt-in via `terraform/authentik/users.yaml`. |
| Outline Redis — emptyDir (not iSCSI PVC) | Sidecar `outline-redis` StatefulSet runs on emptyDir with `--save "" --appendonly no`. Cache-class state only. | Decided 2026-05-26 during deploy when the Synology DS223J's iSCSI LUN cap surfaced at 10/10 — outline-redis' 11th PVC failed with `"Number of LUN reach limit"`. Redis state for Outline is ephemeral (BullMQ job queues, Yjs collab docs in transit, websocket presence) and fully recoverable from Postgres on pod restart, so emptyDir is the right tradeoff. Revisit to iSCSI if the Synology LUN cap is investigated + lifted (DS223J per-volume cap may be UI-raisable — see [open-questions.md](open-questions.md)). |
| Outline DATABASE_URL — `sslmode=no-verify`, not `require` | Connection string against Patroni VIP uses `?sslmode=no-verify`. | Decided 2026-05-26 during deploy after `SequelizeConnectionError: self-signed certificate` killed the first boot. Outline's `pg-connection-string` + `sequelize` stack has tightened `sslmode=require` to behave like `verify-full` (deprecation warning shipped — `pg-connection-string` v3 will adopt libpq semantics). Patroni's cert is self-signed (no public CA chain), so `verify-full` rejects every connection. `no-verify` keeps the TLS envelope (Patroni `hostssl`-only pg_hba satisfied) but skips chain + hostname verification — the right mode for self-signed PG inside the private network. Pattern: any new Node/PG consumer in the homelab needs the same treatment until private CA + cert-manager-on-LXC lands. |
| ExternalSecret — split by resolve-time-cohort, not one per consumer | Outline ships **two** ExternalSecrets (`outline-redis-secret` for the Redis password only; `outline-app` for the full 4-Vault-path bundle). | Decided 2026-05-26 during deploy after the single-ExternalSecret design blocked Redis from booting. ESO refuses to materialize the target Secret until EVERY `data` ref resolves — if any Vault path is missing (because the TF that mints it runs later in the deploy), the whole Secret stays empty. Splitting by which subset of refs are available at the same time lets day-1 consumers (Redis sidecar) boot while waiting on day-N consumers (Outline pod, which needs garage + authentik TF apply). Generalisation: any time consumers have different readiness requirements against the same Vault tree, give them separate ExternalSecrets. |
| Vault UI as primary human accesspoint for homelab secrets (Phase 6) | Vault UI behind Authentik OIDC becomes the *primary* lookup point for human-readable homelab credentials (admin passwords, API tokens, recovery info). 1Password retains: bootstrap creds that must survive Vault being down (Vault root token, AWS KMS unseal token, SSH recovery key, sealed-secrets master keypair backup); non-homelab credentials (banking, personal, family-shared); offline mirror of every homelab secret as belt-and-suspenders. Operator workflow becomes "open vault.niflheim.xiiisins.com → SSO → look up the secret"; 1P is the recovery path when Vault is unavailable. | Decided 2026-05-26 — slotted as Phase 6 between former 5j (Outline) and Jotunheim. Three drivers: (1) consolidates the homelab secret hierarchy — machines already read from Vault, humans gain the same single source of truth without the manual "is this in 1P or Vault?" lookup tax. (2) Per-access audit trail tied to identity (Authentik OIDC → Vault token TTL → audit log per read); 1P access is silent. (3) Removes the per-secret 1P-write step from operator's workflow when minting new secrets — TF mints to Vault as primary, 1P-mirror becomes periodic discipline (and the secret is still in Vault if the mirror lags). Three things that prevent eliminating 1P entirely: (a) Vault's own bootstrap creds (root token, KMS unseal token) must live in a store that survives Vault being down, by definition. (b) Off-LAN access — Vault UI is internal-only by security posture; 1P works on phones / cellular / a friend's laptop / when the homelab is offline. (c) Non-homelab scope — 1P holds banking, personal, family-shared items Vault doesn't host. Trade-off accepted: dual-stash discipline (every new Vault-stored homelab secret also gets a 1P entry) is operator-driven, not automated; drift risk bounded by the discipline rule + periodic audit. |
| Vault UI auth — Authentik OIDC via Traefik (Phase 6) | Vault gains an `oidc` auth method bound to a new Authentik OAuth2 application; the existing `vault-ui` Service flips from `LoadBalancer` → `ClusterIP` + gets an HTTPRoute on the niflheim Gateway at `vault.niflheim.xiiisins.com`. Day-1 group mapping is single-tier (`vault-admins` Authentik group → `homelab-admin` Vault policy → `read+list` on `secret/*` + `update` on `sys/wrapping/*`). Token TTL 1h. Code structured so adding a `vault-readers` tier later is one new group + one new policy + one new role binding. | Decided 2026-05-26. Two alternatives considered + rejected: (a) **Enable native TLS on Vault's listener** — would have also closed the deferred "Vault tls_disable revisit" question. Bigger lift: cert provisioning, HelmRelease values update, every `VAULT_ADDR` consumer update (TF / Ansible / operator env), ESO ClusterSecretStore caBundle update. Phase 6's value is "human accesspoint" not "Vault hardening" — defer TLS until a real driver (tailnet-exposing Vault, external auditor requirement). (b) **LDAP auth method instead of OIDC** — would mirror the future SSSD-for-SSH plan and ride on Authentik's LDAP outpost. Rejected because the LDAP outpost isn't deployed yet (`ssh-users` group exists in `groups.yaml` future-fit, no consumer), and OIDC gives per-token TTL + identity-tied audit trail more cleanly than LDAP binds. Revisit when the LDAP outpost lands for SSH and consolidation cost is low. Side-effect benefit: flipping `vault-ui` LoadBalancer → ClusterIP releases the dynamic-allocated MetalLB `.11` back to the pool, closing the static/dynamic collision gotcha. |
| PG backup — daily logical `pg_dumpall`, leader-only, not `pg_basebackup` | The `pg-backup` role installs a systemd timer on all 3 Patroni nodes; the script self-gates on the Patroni `/primary` REST endpoint so only the current leader dumps (per-DB `pg_dump -Fc` + globals, atomic per-file rename, 1-day local retention; PBS captures it nightly + keeps 7d). | Decided 2026-05-31 (Wave S2). **Logical over physical:** at homelab DB sizes the recovery case is "restore one app's DB" — `pg_dump -Fc` gives per-DB, version-portable, compressed restore; `pg_basebackup` is whole-cluster-only and, *without WAL archiving*, buys nothing over a logical dump. **Leader-only:** a long `pg_dump` of a large/high-churn DB (zabbix `history_*`) on a hot standby gets cancelled `conflict with recovery`; the primary has none. The REST gate means no Ansible-side leader discovery and 3 nodes share one timer. **PITR deferred:** true point-in-time recovery (continuous WAL archiving — `archive_command` is already scaffolded as a no-op in the Patroni config + `pg_basebackup`) is a backlog item, not "recovery confidence" scope. |
| SFTPGo admin + Factorio operator passwords — preserved-lift to Vault, operator-minted | Both moved onto the TF→Vault→Ansible-AppRole rail at `secret/ansible/{sftpgo/admin-password, factorio/operator-password}`, but as **operator-minted** Vault secrets (written directly, not TF `random_password`), with their **existing values preserved** rather than rotated. | Decided 2026-05-31 (Wave S3). The operator pw is already distributed to humans (rotating it would break their SFTP clients); the admin pw was preserved to dodge an SFTPGo-side API reconcile (`CREATE_DEFAULT_ADMIN` only fires when no admin exists, so a new pw can't be applied by restart). Preserved values can't be `random_password`-minted, so they're operator-minted (Hermod-Discord precedent) — TF management + rotation is a clean follow-up once a real rotation driver appears. Closes the on-disk-plaintext admin pw + the last `vault_*` Ansible-Vault runtime app secret. |
| LXC `/etc/resolv.conf` ownership — PVE, not ansible | Each managed LXC declares an `initialization { dns { servers, domain } }` block in `terraform/proxmox/asgard-lxcs/lxcs.tf`; ansible stops managing resolv.conf (`baseline_manage_resolv_conf: false` on the remaining LXC groups + the adguard role's resolv copy task dropped). AGH trio → `127.0.0.1` + Quad9 `9.9.9.10` bootstrap fallback; all other LXCs → AdGuard VIP `10.0.10.200` + UCG `10.0.254.1` fallback (mirrors the old `baseline_nameservers`). | Decided 2026-06-18. PVE rewrites a container's resolv.conf from its config at every start, so ansible-managed content was clobbered on every reboot (perpetual drift — surfaced when the PVE-host-patch reboot hit the whole fleet). Owning DNS at the provision layer is reboot-stable by construction (PVE re-writes the *correct* content each boot) and ends the per-boot fight. Considered + rejected: (a) a per-boot ansible oneshot re-asserting resolv.conf — a workaround that keeps fighting PVE; (b) `chattr +i` — unavailable in unprivileged LXCs (no CAP_LINUX_IMMUTABLE). The bpg `dns` block applies **in-place** (`pct set`, 0 replacements — plan-confirmed), but only manifests on next container start. Tailscale LXCs unchanged (already `false`, MagicDNS-owned); VMs/Frigg unaffected (not subject to the PVE LXC rewrite). Gotcha + retro: [`../known-issues/lxc-proxmox.md`](../known-issues/lxc-proxmox.md), [`../incidents/2026-06-18-lxc-reboot-persistence-drift.md`](../incidents/2026-06-18-lxc-reboot-persistence-drift.md). |
