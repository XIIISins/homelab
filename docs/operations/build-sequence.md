<!-- docs/operations/build-sequence.md -->

# Build sequence

| Phase | Status | Description |
|-------|--------|-------------|
| 1 — Design | ✅ | Complete |
| 2 — UCG-Ultra | ✅ | VLANs, zones, firewall |
| 3 — Synology | ✅ | Factory reset, volumes, NFS, iSCSI target |
| 4 — Proxmox cluster | ✅ | Urd/Verd/Skuld, cluster niflheim formed |
| 5a — PBS | ✅ | LXC 1101 on Skuld, connected |
| 5b — AdGuard Home | ✅ | Saga/Mimir/Kvasir + keepalived VIP + sync |
| 5c — Asgard K3s | ✅ | VMs ✅, K3s ✅, Flux ✅, Sealed Secrets ✅, Synology CSI ✅, Vault ✅, ESO ✅, MetalLB ✅, tigera-operator ✅. Full teardown+rebuild validation 2026-05-17 — see [incident log](../incidents/README.md). |
| 5d — KPN DMZ | ✅ | DMZ → UCG-Ultra WAN (IPv4 + IPv6) |
| 5e — Authentik + Redis | ✅ | Deployed 2026-05-17 evening. Bare LoadBalancer at `10.0.20.12`, plaintext HTTP. Traefik + cert-manager (next phase) will put it behind TLS. Then unblocks Tailscale LXCs. |
| 4a — CP taint (retro-sequenced) | ✅ | Deployed 2026-05-21. All three CPs tainted `node-role.kubernetes.io/control-plane=:NoSchedule` via kubectl on existing nodes (the K3s `node-taint:` config-template is registration-time only — covers future bootstraps). Workload pods migrated to workers. Vault HA naturally spread: vault-0/1/2 on einherjar-urd/skuld/verd. Synology CSI node-plugin evicted from CPs as intended. **Surfaced two outages and ~10 findings** — see [incident log](../incidents/README.md) entry. |
| 4b — Göndul Verd → Urd | ✅ | Applied 2026-05-22. Göndul VM destroyed on Verd and recreated on Urd via `terraform apply --target='proxmox_virtual_environment_vm.control_plane["gondul"]'`; stale etcd member cleared via `kubectl delete node gondul`; rejoined as `--server` (not init) via `-e 'k3s_init_node=hlokk'` override. Surfaced one finding (orphan LVs from a NUC7-era partial migration blocked the first clone — recovery via `lvremove`, see [incident log](../incidents/README.md)). Vault Raft stayed 3/3 voters throughout. CP topology is now Göndul on Urd, Hlökk on Verd, Sigrún on Skuld — back to the original 2026-05-14 design intent, now valid hardware-wise. |
| Worker rebuild — einherjar-urd template_node correction | ✅ | Applied 2026-05-22 evening, immediately after Phase 4b. Worker VM destroyed + recreated to correct stale `template` / `template_node` references in TF (pointed at Verd template even though VM ran on Urd). Doubled as deliberate validation of the worker-rebuild path. Surfaced four findings — see [incident log](../incidents/README.md) entry. Vault accepted 2/3 voters during the ~25 min window by design (vault-0 Pending due to chart's hard-required pod anti-affinity; once new einherjar-urd was Ready it scheduled there, attached its PVC, rejoined Raft). |
| 4c — Verd hardware refresh | ✅ | Applied 2026-05-23. Beelink MINI-S12 (N100, 16GB) → MSI Cubi (i3-1215u, 32GB). Procedure: live-migrate VMs/LXCs off Verd → shutdown → SSD transplant → boot new Cubi → fix NIC name → OS updates + reboot test → migrate workloads back. **No workload-affecting downtime** — cluster stayed 3/3 throughout. Only surface was NIC rename `nic0` (Beelink UEFI-labeled) → `enp45s0` (Cubi predictable-naming), fixed at console with vim `:%s/nic0/enp45s0/g` in `/etc/network/interfaces` + `ifreload -a`. Both Cubi nodes now identical hardware; Skuld is the N100/16GB outlier. The NIC-rename class is now in CLAUDE.md Known gotchas — silently hit during the Urd refresh too. Surfaced the reboot-test persistence-validation rule (now in CLAUDE.md process expectations). |
| 5e.1 — Traefik + Gateway API + cert-manager | ✅ | Deployed 2026-05-22 evening. Gateway API v1.5.1 Standard CRDs (vendored), cert-manager v1.19.0 (`enableGatewayAPI: true`), Traefik v40.2.0/proxy v3.7.1 (Gateway API provider only, `NET_BIND_SERVICE` for direct 80/443 binding). MetalLB pool extended `.11–.99` → `.10–.99`. Wildcard `*.niflheim.xiiisins.com` (ECDSA P-256) issued by Let's Encrypt prod (E8 intermediate), DNS-01 via zone-scoped Cloudflare token. Authentik exposed at `https://authentik.niflheim.xiiisins.com` via HTTPRoute attached to `niflheim` Gateway in `traefik` namespace. Original direct LB on `.12` released back to MetalLB pool. Sub-phases 5e.1.a (Cloudflare token + Vault entry) → 5e.1.i (DNS cutover + Service → ClusterIP). Surfaced eight findings — see [incident log](../incidents/README.md). |
| 5e.2 — Cloudflared + apex zone + WebFinger | 🟡 | Deployed 2026-05-23 (with HTTPRoute migration fix the day-after — see [incident log](../incidents/README.md)). Cloudflared 2026.5.0 in `infrastructure/cloudflared/` (3× replicas, hostname anti-affinity, locally-managed tunnel `asgard-k3s`); credentials.json via ESO from Vault (`secret/k8s/cloudflared/credentials`, written by `terraform/cloudflare/`). Two new wildcards `*.midgard.xiiisins.com` + `*.xiiisins.com` (apex SAN covers bare apex) via cert-manager DNS-01. Second `midgard` Gateway in Traefik with three HTTPS listeners (`websecure-midgard`, `websecure-apex-wildcard`, `websecure-apex-bare`). WebFinger via Caddy pod in `apps/apex-static/` (revised from Traefik middleware — Traefik v3 OSS lacks built-in static-response middleware; see decision row). Authentik now at `authentik.xiiisins.com` (external) + `authentik.midgard.xiiisins.com` (internal alias to `10.0.20.10`). End-to-end validated. |
| 5e.3 — Tailscale OIDC blueprints + LXCs | ✅ | Closed 2026-05-23. All sub-phases complete: `terraform/authentik/` bootstrap + identity-as-data + Tailscale OIDC provider + OIDC smoke test (fresh tailnet via custom OIDC) + `terraform/tailscale/` ACL module (grants syntax, default-allow within tailnet, autoApprovers `10.0.0.0/16` supernet for tag:subnet-router + IPv4+IPv6 default routes for tag:exit-node) + LXCs 1113/1114/1115 (Bifrost/Heimdall/Gjallarbru) deployed and joined tailnet end-to-end (Terraform LXCs with `device_passthrough` `/dev/net/tun`, authkeys via Vault at `secret/ansible/tailscale/authkeys/<hostname>`, new `ansible/roles/tailscale/`) + **5e.3.f** Munin DSM Tailscale subnet-router (Synology Package Center install, DSM-7 TUN boot task `tailscale configure-host`, UI-minted authkey in 1Password — first homelab secret using the human-consumer → 1P rule with no Vault path; `policy.hujson` extended to add `autogroup:admin` to `tagOwners["tag:subnet-router"]` so admin-UI key minting works). Tailscale OAuth client created via Trust credentials UI, tagged `tag:terraform-tag-owner`, credentials in Vault. Tailscale Free plan (3-user cap on custom OIDC; trial reverts to Free in 14 days, no impact — all features used available on Free); split-auth: LXCs 1113/1114/1115 + one always-on user device on auto-renewing TF-minted keys, NAS authkey out-of-band (UI-minted, 1P-stored), phones/laptops with default expiry via Authentik OIDC. |
| 5e.4 — Tailnet DNS (MagicDNS + split DNS) | ✅ | Applied 2026-05-24. New `terraform/tailscale/dns.tf` — `tailscale_dns_preferences` (MagicDNS on), `tailscale_dns_split_nameservers` for `niflheim.xiiisins.com` + `midgard.xiiisins.com` → AdGuard VIP `10.0.10.200`, `tailscale_dns_search_paths` `niflheim.xiiisins.com`. Apex `xiiisins.com` deliberately NOT split — tailnet clients follow the same public path (Cloudflare tunnel) as off-tailnet clients, preserving one resolution behavior per zone. Single resolver target (the VIP, not the three AGH node IPs) — VIP is the contract; listing backings would couple tailnet config to AGH HA topology and mask VIP outages. Discovered as a gap when a tailnet-connected client couldn't resolve `vor` / FQDN — 5e.3 deployed connectivity but left DNS to client-local resolvers which NXDOMAIN'd the internal zones. Surfaced findings: (a) Tailscale provider 0.28.0 resource is `tailscale_dns_split_nameservers` one-per-domain, NOT `tailscale_dns_split_dns` map-form (the latter shipped in later provider versions) — discovered via `terraform providers schema -json`; (b) existing OAuth client needed `dns:write` scope added — accepted in-place via Tailscale admin UI, no remint, no Vault rewrite. |
| 5f — Factorio LXC | ✅ | Deployed 2026-05-16 — Terraform + Ansible end-to-end |
| 5g — PostgreSQL + Teamspeak LXCs | 🟡 | Fulla (1130) ✅ deployed 2026-05-17 standalone. Vör/Idunn pulled forward into Phase 5g.2 (cluster expansion now driven by operational pressure: Authentik PG-DNS flapping mitigated 2026-05-24 by literal-IP stopgap pointing at Fulla; structural fix is the HA cluster + VIP). Teamspeak pending. |
| 5g.2 — PG HA with Patroni + etcd-on-HAProxy | ✅ | **Closed 2026-05-24.** Patroni cluster `niflheim-pg` (Fulla/Vör/Idunn), etcd on HAProxy LXC trio (Hlin/Eir/Snotra at 1133/1134/1135), HAProxy VIP `10.0.10.210` on VLAN 10 floating across the trio via keepalived (eth1 dual-NIC for VRRP L2-adjacency, all-BACKUP election by priority 100/90/80, `chk_haproxy` track-script demotes VIP-holder if HAProxy dies, source-based policy routing for VLAN-10-out replies). Patroni REST API `/master` health-check on `:8008` + `balance first` = deterministic leader routing. Authentik cut over to VIP, literal-fulla-IP stopgap retired. Failover validated end-to-end (kill leader → election → VIP follows → Authentik survives). Generic data-driven `haproxy` + `keepalived` Ansible roles built — reusable for any future VIP/backend by group_vars. Surfaced findings across the deploy: four Patroni-adoption gotchas (psycopg3 mixed-types, postgres restart-handler, `bootstrap.pg_hba` initdb-only, `conf.d` sync) + three this-phase gotchas (VRID collision with AGH on shared L2, Patroni `postgresql.pg_hba` missing localhost replication entry, `systemctl kill` doesn't actually kill patroni) — all in [Known gotchas](../../CLAUDE.md) + retro in [incident log](../incidents/2026-05-24-5g2-haproxy-keepalived-vip.md). Step-list with commit hashes in [open-questions.md](open-questions.md). |
| 5h — Remaining LXCs | 🔲 | Zabbix, Jellyfin (HAProxy moved to Phase 5g.2; Tailscale LXCs now part of 5e.3) |
| 5i — NetBox IPAM/DCIM | ✅ | **Closed 2026-05-24.** NetBox 4.6.1 via chart `8.2.17` (OCI) in asgard K3s. External PG via HAProxy VIP, bundled Valkey standalone, internal-only HTTPRoute (`netbox.niflheim.xiiisins.com`), Authentik OIDC. Initial inventory imported via web UI per [`docs/procedures/netbox-initial-data-import.md`](../procedures/netbox-initial-data-import.md). Surfaced **in-cluster pod-to-MetalLB-VIP** class generalised fix: K3s `coredns-custom` rewrite for K8s-fronted internal FQDNs (NetBox's OIDC backchannel timed out hitting `authentik.midgard` → 10.0.20.10; rewrites the FQDN query to Traefik's ClusterIP DNS so pod skips the VIP entirely). Eleven netbox-chart gotchas documented (extraConfig.secret doesn't propagate to Django settings → use extraVolumes+subPath; existingSecret projects all keys without optional → provide email_password etc. as empty strings; Valkey replication+iSCSI fsGroup → standalone+volumePermissions; shared RWO media PVC between server+worker → emptyDir for first deploy; `small` preset OOMs migrations → `medium` floor; NetBox 4.6 = python-social-auth + granian; migrations in main container entrypoint → install.retries=-1 critical) — all in [Known gotchas](../../CLAUDE.md). Surfaced `tasks_from:` silently-ignored-in-`roles:`-block bug while iterating on PG DB provisioning — fixed via `postgres-common` role split (commit `69d72d3`). TF→NetBox standing pattern (Option A from the integration research) confirmed for Phase 5i.3 next. Step-list with commit hashes in [open-questions.md](open-questions.md); retro at [incident log](../incidents/2026-05-24-5i-netbox.md). |
| 5i.3 — TF→NetBox standing pattern | ✅ | **Closed 2026-05-24.** `terraform/netbox/` module on `e-breuninger/netbox v5.3.0` retrofits the 5i.e hand-imported records via TF 1.5+ `import {}` blocks — ~160 resources across 8 files (sites, clusters, manufacturers, device types, roles, VLANs, prefixes, devices, VMs, IPs, VIPs, tags, custom fields). Three NetBox-side placement corrections landed (PBS, idunn, Gjallarbru) where the manual import disagreed with `terraform/proxmox/*` IaC truth. Two upstream blockers required workarounds: chart-deploy gap (`API_TOKEN_PEPPERS` not auto-supplied when `existingSecret` is set — added to netbox-app ExternalSecret, commit `b576bd2`), provider gap (issue #349, integer custom_fields not writable — VMID recreated as text with `validation_regex="^[0-9]*$"`). Auth strategy pivoted from "dedicated `terraform` user + token" to "reuse admin token throughout" (commit `199b94f`) after the provider's `netbox_user`/`netbox_token` v5.3.0 incompatibilities with NetBox 4.6 (POST+PUT token-update rejected, v2 token secret unavailable in state, no `is_superuser` field) made the dedicated-user pattern untenable. Worker memory bumped 4G→8G during the apply (incident commit `d690381`, retro at [incident log](../incidents/2026-05-24-worker-oom-during-netbox-rolling-restart.md)) — cumulative workload pressure on the 4G floor was the trigger. Step-list with commit hashes in [open-questions.md](open-questions.md); full retro at [incident log](../incidents/2026-05-24-5i3-tf-netbox-retrofit.md). |
| 6 — Jotunheim K3s | 🔲 | Terraform VMs, Flux, services |
| 7 — Observability | 🔲 | VictoriaMetrics + VictoriaLogs (no Grafana — native vmui + VL UI). Zabbix is its own Phase 5h LXC. |
| 8 — Secrets runtime retrieval | 🔲 | Migrate from ESO sync-and-cache to Vault Agent / VSO. Pilot on jotunheim first. |
