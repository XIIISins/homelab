<!-- docs/operations/build-sequence.md -->

# Build sequence

*Renumbering note (2026-05-26): former Phase 6/7/8 bumped to 7/8/9 to clear slot for new Phase 6 (human-facing secret management — Vault OIDC). Sub-phases shifted accordingly (7a/b/c → 8a/b/c). Incident retros retain their original phase labels as historical record.*

| Phase | Status | Description |
|-------|--------|-------------|
| 1 — Design | ✅ | Complete |
| 2 — UCG-Ultra | ✅ | VLANs, zones, firewall |
| 3 — Synology | ✅ | Factory reset, volumes, NFS, iSCSI target |
| 4 — Proxmox cluster | ✅ | Urd/Verd/Skuld, cluster niflheim formed |
| 5a — PBS | ✅ | LXC 1101 on Skuld, connected |
| 5b — AdGuard Home | ✅ | Saga/Mimir/Kvasir + keepalived VIP + sync |
| 5b.2 — AdGuard Home IaC | ✅ | **Closed 2026-05-25.** Saga/Mimir/Kvasir migrated manual→IaC. TF `import` of all 3 LXCs (`lifecycle.ignore_changes = [operating_system[0].template_file_id, initialization[0].user_account, features[0].keyctl]` to suppress bpg/proxmox import drift). Ansible roles `adguard` (v0.107.76) + `adguardhome-sync` (v0.9.0, cron `*/1`) + `keepalived` (VRID=51) + source-policy-routing for VLAN 10. Operator state preserved via `adguard_force_overwrite_config: false` (30 rewrites + 1 persistent client + 1 filter intact on Saga). Failover validated: stop AGH on Saga → chk_adguard demotes 100→50 → VIP follows to Mimir → DNS continuity preserved → restart Saga → VIP reclaims. Sync auth fixed via group_vars override `adguardhome_sync_admin_user: ghost` (operator's admin user is `ghost`, not the role-default `admin`). Surfaced several gotchas — see CLAUDE.md Known gotchas additions (ssh_args section, BSD sed `\s`, GNU tar `--strip-components` with `./` prefix, Ansible unarchive validation bug, bpg/proxmox import drift, dual-MASTER VIP during manual→role transition, AGH admin username default). Also surfaced: Proxmox API token leak via failed BSD-sed redaction → rotated `root@pam!terraform` → `root@pam!terraform-rot-20260525-055802` (in `terraform.tfvars` for both asgard-lxcs + asgard-k3s, stashed at `secret/temp/proxmox/api-token-rotated-20260525` for 1P update). |
| 5c — Asgard K3s | ✅ | VMs ✅, K3s ✅, Flux ✅, Sealed Secrets ✅, Synology CSI ✅, Vault ✅, ESO ✅, MetalLB ✅, tigera-operator ✅. Full teardown+rebuild validation 2026-05-17 — see [incident log](../incidents/README.md). |
| 5d — KPN DMZ | ✅ | DMZ → UCG-Ultra WAN (IPv4 + IPv6) |
| 5e — Authentik + Redis | ✅ | Deployed 2026-05-17 evening. Bare LoadBalancer at `10.0.20.12`, plaintext HTTP. Traefik + cert-manager (next phase) will put it behind TLS. Then unblocks Tailscale LXCs. |
| 4a — CP taint (retro-sequenced) | ✅ | Deployed 2026-05-21. All three CPs tainted `node-role.kubernetes.io/control-plane=:NoSchedule` via kubectl on existing nodes (the K3s `node-taint:` config-template is registration-time only — covers future bootstraps). Workload pods migrated to workers. Vault HA naturally spread: vault-0/1/2 on einherjar-urd/skuld/verd. Synology CSI node-plugin evicted from CPs as intended. **Surfaced two outages and ~10 findings** — see [incident log](../incidents/README.md) entry. |
| 4b — Göndul Verd → Urd | ✅ | Applied 2026-05-22. Göndul VM destroyed on Verd and recreated on Urd via `terraform apply --target='proxmox_virtual_environment_vm.control_plane["gondul"]'`; stale etcd member cleared via `kubectl delete node gondul`; rejoined as `--server` (not init) via `-e 'k3s_init_node=hlokk'` override. Surfaced one finding (orphan LVs from a NUC7-era partial migration blocked the first clone — recovery via `lvremove`, see [incident log](../incidents/README.md)). Vault Raft stayed 3/3 voters throughout. CP topology is now Göndul on Urd, Hlökk on Verd, Sigrún on Skuld — back to the original 2026-05-14 design intent, now valid hardware-wise. |
| Worker rebuild — einherjar-urd template_node correction | ✅ | Applied 2026-05-22 evening, immediately after Phase 4b. Worker VM destroyed + recreated to correct stale `template` / `template_node` references in TF (pointed at Verd template even though VM ran on Urd). Doubled as deliberate validation of the worker-rebuild path. Surfaced four findings — see [incident log](../incidents/README.md) entry. Vault accepted 2/3 voters during the ~25 min window by design (vault-0 Pending due to chart's hard-required pod anti-affinity; once new einherjar-urd was Ready it scheduled there, attached its PVC, rejoined Raft). |
| 4c — Verd hardware refresh | ✅ | Applied 2026-05-23. Beelink MINI-S12 (N100, 16GB) → MSI Cubi (i3-1215u, 32GB). Procedure: live-migrate VMs/LXCs off Verd → shutdown → SSD transplant → boot new Cubi → fix NIC name → OS updates + reboot test → migrate workloads back. **No workload-affecting downtime** — cluster stayed 3/3 throughout. Only surface was NIC rename `nic0` (Beelink UEFI-labeled) → `enp45s0` (Cubi predictable-naming), fixed at console with vim `:%s/nic0/enp45s0/g` in `/etc/network/interfaces` + `ifreload -a`. Both Cubi nodes now identical hardware; Skuld is the N100/16GB outlier. The NIC-rename class is now in CLAUDE.md Known gotchas — silently hit during the Urd refresh too. Surfaced the reboot-test persistence-validation rule (now in CLAUDE.md process expectations). |
| 5e.1 — Traefik + Gateway API + cert-manager | ✅ | Deployed 2026-05-22 evening. Gateway API v1.5.1 Standard CRDs (vendored), cert-manager v1.19.0 (`enableGatewayAPI: true`), Traefik v40.2.0/proxy v3.7.1 (Gateway API provider only, `NET_BIND_SERVICE` for direct 80/443 binding). MetalLB pool extended `.11–.99` → `.10–.99`. Wildcard `*.niflheim.xiiisins.com` (ECDSA P-256) issued by Let's Encrypt prod (E8 intermediate), DNS-01 via zone-scoped Cloudflare token. Authentik exposed at `https://authentik.niflheim.xiiisins.com` via HTTPRoute attached to `niflheim` Gateway in `traefik` namespace. Original direct LB on `.12` released back to MetalLB pool. Sub-phases 5e.1.a (Cloudflare token + Vault entry) → 5e.1.i (DNS cutover + Service → ClusterIP). Surfaced eight findings — see [incident log](../incidents/README.md). |
| 5e.2 — Cloudflared + apex zone + WebFinger | ✅ | Closed 2026-05-23 (with HTTPRoute migration fix the day-after — see [incident log](../incidents/README.md)). Flipped 🟡 → ✅ on 2026-05-31 (Wave S6): the amber was stale bookkeeping — the only open thread was the stale `httproute.yaml` comment, since corrected (commit 3409e27). Cloudflared 2026.5.0 in `infrastructure/cloudflared/` (3× replicas, hostname anti-affinity, locally-managed tunnel `asgard-k3s`); credentials.json via ESO from Vault (`secret/k8s/cloudflared/credentials`, written by `terraform/cloudflare/`). Two new wildcards `*.midgard.xiiisins.com` + `*.xiiisins.com` (apex SAN covers bare apex) via cert-manager DNS-01. Second `midgard` Gateway in Traefik with three HTTPS listeners (`websecure-midgard`, `websecure-apex-wildcard`, `websecure-apex-bare`). WebFinger via Caddy pod in `apps/apex-static/` (revised from Traefik middleware — Traefik v3 OSS lacks built-in static-response middleware; see decision row). Authentik now at `authentik.xiiisins.com` (external) + `authentik.midgard.xiiisins.com` (internal alias to `10.0.20.10`). End-to-end validated. |
| 5e.3 — Tailscale OIDC blueprints + LXCs | ✅ | Closed 2026-05-23. All sub-phases complete: `terraform/authentik/` bootstrap + identity-as-data + Tailscale OIDC provider + OIDC smoke test (fresh tailnet via custom OIDC) + `terraform/tailscale/` ACL module (grants syntax, default-allow within tailnet, autoApprovers `10.0.0.0/16` supernet for tag:subnet-router + IPv4+IPv6 default routes for tag:exit-node) + LXCs 1113/1114/1115 (Bifrost/Heimdall/Gjallarbru) deployed and joined tailnet end-to-end (Terraform LXCs with `device_passthrough` `/dev/net/tun`, authkeys via Vault at `secret/ansible/tailscale/authkeys/<hostname>`, new `ansible/roles/tailscale/`) + **5e.3.f** Munin DSM Tailscale subnet-router (Synology Package Center install, DSM-7 TUN boot task `tailscale configure-host`, UI-minted authkey in 1Password — first homelab secret using the human-consumer → 1P rule with no Vault path; `policy.hujson` extended to add `autogroup:admin` to `tagOwners["tag:subnet-router"]` so admin-UI key minting works). Tailscale OAuth client created via Trust credentials UI, tagged `tag:terraform-tag-owner`, credentials in Vault. Tailscale Free plan (3-user cap on custom OIDC; trial reverts to Free in 14 days, no impact — all features used available on Free); split-auth: LXCs 1113/1114/1115 + one always-on user device on auto-renewing TF-minted keys, NAS authkey out-of-band (UI-minted, 1P-stored), phones/laptops with default expiry via Authentik OIDC. |
| 5e.4 — Tailnet DNS (MagicDNS + split DNS) | ✅ | Applied 2026-05-24. New `terraform/tailscale/dns.tf` — `tailscale_dns_preferences` (MagicDNS on), `tailscale_dns_split_nameservers` for `niflheim.xiiisins.com` + `midgard.xiiisins.com` → AdGuard VIP `10.0.10.200`, `tailscale_dns_search_paths` `niflheim.xiiisins.com`. Apex `xiiisins.com` deliberately NOT split — tailnet clients follow the same public path (Cloudflare tunnel) as off-tailnet clients, preserving one resolution behavior per zone. Single resolver target (the VIP, not the three AGH node IPs) — VIP is the contract; listing backings would couple tailnet config to AGH HA topology and mask VIP outages. Discovered as a gap when a tailnet-connected client couldn't resolve `vor` / FQDN — 5e.3 deployed connectivity but left DNS to client-local resolvers which NXDOMAIN'd the internal zones. Surfaced findings: (a) Tailscale provider 0.28.0 resource is `tailscale_dns_split_nameservers` one-per-domain, NOT `tailscale_dns_split_dns` map-form (the latter shipped in later provider versions) — discovered via `terraform providers schema -json`; (b) existing OAuth client needed `dns:write` scope added — accepted in-place via Tailscale admin UI, no remint, no Vault rewrite. |
| 5f — Factorio LXC | ✅ | Deployed 2026-05-16 — Terraform + Ansible end-to-end |
| 5g — PostgreSQL + Teamspeak | ✅ | **Closed 2026-05-25.** Fulla (1130) deployed 2026-05-17 standalone. Vör/Idunn pulled forward into Phase 5g.2 (Authentik PG-DNS flapping mitigated 2026-05-24 by literal-IP stopgap; structural fix is the HA cluster + VIP). Teamspeak pivoted LXC 1121 → asgard K3s (`k8s/asgard/apps/teamspeak/`): TS3 3.13+ `ts3db_postgresql` plugin against Patroni via HAProxy VIP `10.0.10.210`, single-replica StatefulSet on 5Gi RWO iSCSI PVC (expandable to 20Gi), MetalLB VIP `10.0.20.12` shared between UDP voice (9987) + TCP filetransfer (30033) via `allow-shared-ip` annotation, `externalTrafficPolicy: Local` for source-IP preservation. SRV ring `_ts3._udp.ts3.xiiisins.com` (existing, outside TF) → `hel-ts3` (homelab) + `do-ts3` (DigitalOcean fallback). End-to-end validated (external client connected via SRV → KPN public IP → UCG port-forwards → MetalLB → pod → PG-backed schema bootstrap → ServerAdmin claim). 4 gotchas surfaced — chown init needs `CAP_CHOWN`+`CAP_FOWNER` (drop ALL strips them even as UID 0), StatefulSet RollingUpdate won't replace a CrashLoopBackOff pod (manual `kubectl delete pod` to break deadlock), Ansible `include_tasks` doesn't propagate `--tags` to inner tasks (workaround: skip-tags heavy roles + pass facts via `-e '{"key":true}'` JSON), MetalLB IPAddressPool static/dynamic collision risk (Vault UI's dynamic-allocated `.11` could shift if Service is recreated). Retro at [incident log](../incidents/2026-05-25-teamspeak-k3s.md). |
| 5g.2 — PG HA with Patroni + etcd-on-HAProxy | ✅ | **Closed 2026-05-24.** Patroni cluster `niflheim-pg` (Fulla/Vör/Idunn), etcd on HAProxy LXC trio (Hlin/Eir/Snotra at 1133/1134/1135), HAProxy VIP `10.0.10.210` on VLAN 10 floating across the trio via keepalived (eth1 dual-NIC for VRRP L2-adjacency, all-BACKUP election by priority 100/90/80, `chk_haproxy` track-script demotes VIP-holder if HAProxy dies, source-based policy routing for VLAN-10-out replies). Patroni REST API `/master` health-check on `:8008` + `balance first` = deterministic leader routing. Authentik cut over to VIP, literal-fulla-IP stopgap retired. Failover validated end-to-end (kill leader → election → VIP follows → Authentik survives). Generic data-driven `haproxy` + `keepalived` Ansible roles built — reusable for any future VIP/backend by group_vars. Surfaced findings across the deploy: four Patroni-adoption gotchas (psycopg3 mixed-types, postgres restart-handler, `bootstrap.pg_hba` initdb-only, `conf.d` sync) + three this-phase gotchas (VRID collision with AGH on shared L2, Patroni `postgresql.pg_hba` missing localhost replication entry, `systemctl kill` doesn't actually kill patroni) — all in [Known gotchas](../../CLAUDE.md) + retro in [incident log](../incidents/2026-05-24-5g2-haproxy-keepalived-vip.md). Step-list with commit hashes in [open-questions.md](open-questions.md). |
| 5h — Remaining LXCs | 🔲 | Jellyfin (HAProxy moved to Phase 5g.2; Tailscale LXCs now part of 5e.3; **Zabbix promoted to Phase 8c — observability-stack core, not just-another-LXC**) |
| 5h.2 — Notifications (Hermod) | ✅ | **Closed 2026-05-26.** AppriseAPI notification aggregator on LXC 1103 (Verd, 10.0.11.22), native install (pip+venv+gunicorn+systemd; no Docker — explicitly avoiding the 5aff1dd rolled-back Docker-in-LXC attempt). Caddy reverse-proxy on :80 with `remote_ip` allowlist matcher gates inbound POSTs; AppriseAPI bound `127.0.0.1:8000`. Four Discord channels with Valkyrie display names: **Hrist** (`tag: critical`), **Mist** (`tag: alert`), **Ölrún** (`tag: media`), **Hel** (untagged-quarantine). Generic `caddy-reverse-proxy` Ansible role reusable beyond Hermod. End-to-end smoketest passed (4 positive POSTs → HTTP 200 + Discord delivery; 1 negative → HTTP 403). Producer wiring landed 2026-05-26 (commits 9c9eef7 + c344697, [producer-wiring retro](../incidents/2026-05-26-hermod-producer-wiring.md)): **Zabbix webhook media-type** via `community.zabbix.zabbix_mediatype` + `zabbix_user` (Admin attached, severity bitmask 56 = Average+High+Disaster); **Patroni `on_role_change` bash callback** on all 3 PG nodes (Fulla/Vör/Iðunn), gated by `patroni_hermod_enabled`, always-exit-0 to avoid supervisor deadlock. Routine notifications NOT routed via Hermod — vlagent ships syslog/journald to VictoriaLogs as the audit trail. Sub-phases 5h.2.a-h + 5h.2.k done (2026-05-25, [hub-deploy retro](../incidents/2026-05-25-hermod-deploy.md), commits 05e1631 + b075ba7 + d87876c); 5h.2.i + 5h.2.j done (2026-05-26). **Live failover validated 2026-05-26** via two back-to-back `patronictl switchover` runs (vor→fulla→vor): cluster moved cleanly across TL=6→7→8, both promotion + demotion callbacks delivered to Discord. Validation surfaced the Patroni 4.x `master`→`primary` role-name rename — callback case statement matched only `master`, so 4.x promotions fell into the unknown-role catch-all and the promotion-half notification was silently dropped (demotion unaffected). Fixed by folding `primary` into the master branch via alternation; new CLAUDE.md gotcha covers the rename + the always-exit-0 callback rule. Optional PBS notification hook on backup failure remains deferred. Full design + JSON schema + source→tag mapping + LXC spec in [`docs/services/notifications.md`](../services/notifications.md). |
| 5h.3 — Ansible orchestration (Semaphore + drift-check) | ✅ | **Closed 2026-05-27.** Semaphore StatefulSet (`semaphore` ns, Postgres backend via Patroni VIP `10.0.10.210` at `secret/k8s/semaphore/postgres-password`, inventory-cache on emptyDir per Synology DS223J LUN-cap, Authentik OIDC, internal-only HTTPRoute at `semaphore.niflheim.xiiisins.com`). Four templates live: `refresh-netbox-inventory` (cron 4h), `asgard-drift-check` (cron 6h, `--check --diff` against `site.yml`), `asgard-apply` (manual), `asgard-fleet-agents` (daily, added during the phase). Hermod webhook + Hermod-summary callback plugin parses `PLAY RECAP` into compact markdown. Per-host-group playbook tree (`asgard-<group>.yml` × 8 + `vlagent.yml` + `zabbix-agent.yml` cluster-wide, `site.yml` `import_playbook`s the lot). NetBox dynamic inventory primary, cache TTL bumped 4h→24h after the silent-success class surfaced; `hosts.yml` retained as DR fallback. `os-updates` role split out of `baseline` so converge runs don't pull packages implicitly. **Drift-check baseline = zero `changed=` across the fleet.** From here, an `alert` in the Hermod channel means real drift. 66 commits across the phase (~20 are the `--check` shakeout). 12 findings encoded in CLAUDE.md gotchas + memory (Semaphore pod-start pip-install pattern, undocumented Semaphore config schema, **NetBox cold-cache silent-success three-layer defence**, inventory file vs group_vars isolation, keyed_groups tag-rename, group renames per role-class, `--check` mode caveats catalogue, drift-check `--check` preflight assert, `vault kv patch field=-` stdin unreliable, paired AWS auto-unseal IAM rotation). Per-role README pass replaced by a single canonical caveats section in [`docs/architecture/ansible-orchestration.md`](../architecture/ansible-orchestration.md) (scope decision 2026-05-27 — patterns are uniform). AGH rewrite for `semaphore.niflheim → 10.0.20.10` landed in initial 5h.3.a-d commit `aa77592`. Retro: [incident log](../incidents/2026-05-27-5h3-semaphore-drift-check.md). **Follow-up 2026-06-10:** Semaphore moved to a custom GHCR image (`ghcr.io/xiiisins/semaphore-homelab`, `docker/semaphore/Dockerfile` + GitHub Actions) after the stock image's bundled `community.zabbix 2.3.1` (vs pinned 4.2.0) broke the zabbix-agent register task — Semaphore never galaxy-installs the pins, so they're baked into the ansible venv site-packages. First homelab custom image; pattern reusable. The pod-start `pip install` finding from this phase is superseded (pip deps baked too). Retro: [semaphore-collection-skew](../incidents/2026-06-10-semaphore-collection-skew.md). **Follow-up 2026-06-18:** PVE host patching — the one thing `os-updates.yml` deliberately excludes (a host reboot takes every guest down) — got its own discovery-driven, health-gated rolling playbook `proxmox-host-patching.yml` + a config-only `proxmox-host` repo role (removes enterprise+ceph apt sources, wired into `asgard-proxmox.yml` for drift coverage). Validated on the live fleet (canary urd + full round, 2026-06-18). Decision row in [decisions.md](decisions.md); runbook [`docs/procedures/proxmox-host-patching.md`](../procedures/proxmox-host-patching.md). |
| 5i — NetBox IPAM/DCIM | ✅ | **Closed 2026-05-24.** NetBox 4.6.1 via chart `8.2.17` (OCI) in asgard K3s. External PG via HAProxy VIP, bundled Valkey standalone, internal-only HTTPRoute (`netbox.niflheim.xiiisins.com`), Authentik OIDC. Initial inventory imported via web UI per [`docs/procedures/netbox-initial-data-import.md`](../procedures/netbox-initial-data-import.md). Surfaced **in-cluster pod-to-MetalLB-VIP** class generalised fix: K3s `coredns-custom` rewrite for K8s-fronted internal FQDNs (NetBox's OIDC backchannel timed out hitting `authentik.midgard` → 10.0.20.10; rewrites the FQDN query to Traefik's ClusterIP DNS so pod skips the VIP entirely). Eleven netbox-chart gotchas documented (extraConfig.secret doesn't propagate to Django settings → use extraVolumes+subPath; existingSecret projects all keys without optional → provide email_password etc. as empty strings; Valkey replication+iSCSI fsGroup → standalone+volumePermissions; shared RWO media PVC between server+worker → emptyDir for first deploy; `small` preset OOMs migrations → `medium` floor; NetBox 4.6 = python-social-auth + granian; migrations in main container entrypoint → install.retries=-1 critical) — all in [Known gotchas](../../CLAUDE.md). Surfaced `tasks_from:` silently-ignored-in-`roles:`-block bug while iterating on PG DB provisioning — fixed via `postgres-common` role split (commit `69d72d3`). TF→NetBox standing pattern (Option A from the integration research) confirmed for Phase 5i.3 next. Step-list with commit hashes in [open-questions.md](open-questions.md); retro at [incident log](../incidents/2026-05-24-5i-netbox.md). |
| 5i.3 — TF→NetBox standing pattern | ✅ | **Closed 2026-05-24.** `terraform/netbox/` module on `e-breuninger/netbox v5.3.0` retrofits the 5i.e hand-imported records via TF 1.5+ `import {}` blocks — ~160 resources across 8 files (sites, clusters, manufacturers, device types, roles, VLANs, prefixes, devices, VMs, IPs, VIPs, tags, custom fields). Three NetBox-side placement corrections landed (PBS, idunn, Gjallarbru) where the manual import disagreed with `terraform/proxmox/*` IaC truth. Two upstream blockers required workarounds: chart-deploy gap (`API_TOKEN_PEPPERS` not auto-supplied when `existingSecret` is set — added to netbox-app ExternalSecret, commit `b576bd2`), provider gap (issue #349, integer custom_fields not writable — VMID recreated as text with `validation_regex="^[0-9]*$"`). Auth strategy pivoted from "dedicated `terraform` user + token" to "reuse admin token throughout" (commit `199b94f`) after the provider's `netbox_user`/`netbox_token` v5.3.0 incompatibilities with NetBox 4.6 (POST+PUT token-update rejected, v2 token secret unavailable in state, no `is_superuser` field) made the dedicated-user pattern untenable. Worker memory bumped 4G→8G during the apply (incident commit `d690381`, retro at [incident log](../incidents/2026-05-24-worker-oom-during-netbox-rolling-restart.md)) — cumulative workload pressure on the 4G floor was the trigger. Step-list with commit hashes in [open-questions.md](open-questions.md); full retro at [incident log](../incidents/2026-05-24-5i3-tf-netbox-retrofit.md). |
| 5j — Outline wiki + Garage S3 | ✅ | **Closed 2026-05-26.** Garage v2.3.0 single-node object store (RF=1, dangerous-consistency, LMDB, 200Gi data + 10Gi meta on iSCSI) in `k8s/asgard/infrastructure/garage/` — first homelab S3 layer, chosen over MinIO (OSS pivot) + SeaweedFS (POSIX/WebDAV redundant with iSCSI+NFS). Layout-init Job drives Garage admin API v2 from alpine+curl+jq sidecar (dxflrs image is FROM scratch — no shell). Bucket+key+per-bucket grant + `secret/k8s/outline/s3` minted via `terraform/garage/` (jkossis/garage v1.0.4 provider) hitting the admin API through a `kubectl port-forward` from the operator MacBook. Outline 1.8.0-1 (outlinewiki/outline on Docker Hub — NOT ghcr.io) in `k8s/asgard/apps/outline/`: 1-replica Deployment (Recreate strategy for serial migrations), UID 1001 numeric (image USER nodejs is name-based → K8s admission rejects runAsNonRoot without explicit UID), sidecar Redis StatefulSet on emptyDir (Synology DS223J hit a 10/10 iSCSI LUN cap — Redis state is cache-class so emptyDir is the right tradeoff; cap investigation tracked in [open-questions.md](open-questions.md)). Three hostnames live end-to-end: wiki.xiiisins.com (external via cloudflared), wiki.midgard + wiki.niflheim (LAN via AGH→Traefik). OIDC via Authentik with `outline-users` group gate (declarative via `terraform/authentik/users.yaml`). PG on Patroni VIP with `sslmode=no-verify` (Outline's pg-connection-string+sequelize now treats `require` as `verify-full` and rejects self-signed). DB provisioned by ansible postgres-common against current Patroni leader (Idunn at deploy; surfaced postgres-common-databases needs leader-discovery, not hard-coded). Two ExternalSecrets split by resolve-time (redis-secret day-1, outline-app once garage+authentik land). 16 findings — see [incident log](../incidents/2026-05-26-outline-garage-deploy.md). |
| 5k — Package mirror (Hvergelmir) | 🔲 | **Design settled 2026-06-20, not started.** Full Debian (trixie) + Rocky/Alma 9 mirror **with snapshots** in asgard K3s — lightweight nginx/Caddy serving an **NFS PV**, populated by CronJobs (`aptly` for deb snapshot/promote, `reposync`+`createrepo_c` for rpm); **not Pulp** (too heavy), **not a caching proxy** (only a full mirror survives upstream *deletion*). Hosts pinned third-party `.deb`s (SFTPGo/Tailscale/Zabbix) to close the dead-repo class that bit SFTPGo's OSU repo (2026-06-18). Proxmox hosts keep upstream repos present-but-**disabled** (enable-as-fallback) + a baseline upstream-fallback flag, so the mirror never gates substrate/cluster recovery. `os-updates` + `proxmox-host-patching.yml` re-point apt/dnf at it. Full design in [open-questions.md](open-questions.md) ("Self-hosted internal repos … → Phase 5k"). |
| 6 — Secret mgmt (Vault OIDC) + Frigg watchtower | 🟡 | Two stages. **Stage 1 — Vault OIDC (6.a–6.e) ✅ COMPLETE 2026-05-31:** Vault is the primary human-lookup access point via the Vault UI behind Authentik OIDC at `vault.niflheim.xiiisins.com` (1P retained as offline mirror + bootstrap + non-homelab). OIDC provider/method + `vault-ingress` HTTPRoute + AGH rewrite deployed; browser login confirmed as `homelab-admin` (read-only `secret/*`). `VAULT_ADDR` cut over `.11` → the FQDN (1P `url` field + `homelab-env`; `vault kv get` + `terraform plan` verified). `vault-ui` **kept** as a LoadBalancer but **pinned** to `loadBalancerIP: 10.0.20.11` (static claim — closes the collision gotcha; deliberate Traefik-independent direct path alongside the FQDN). Zero downtime, nothing destroyed. Listener stays `tls_disable=1` (Traefik terminates). **Stage 2 — Frigg control-node watchtower (6.f–6.j) ✅ FULLY LIVE 2026-05-31** (HA VM 2900 on Munin-NFS, NetBox-registered, control-node role provisioned + `claude remote-control` systemd service connected, AGH `frigg.niflheim` rewrite; Tailscale-SSH + vlagent + zabbix-agent ran from Frigg-as-Linux-controller; HA validated incl. a kill-qemu crash test; tailscale-accept-dns DNS regression caught + fixed — see open-questions + 2026-05-31 incident retro): an always-on, HA, homelab-resident VM whose primary job is a persistent home for `claude remote-control` sessions so Claude Code remote-control no longer hinges on the MacBook (1P locks on MacBook screen-sleep → MacBook-hosted sessions lose creds; Frigg never touches 1P). Runs the full TF/Ansible/Flux cycle via its own `ansible-frigg` Vault AppRole (no `op` on the box). VM (own kernel, native tun); disk on a Munin **NFS datastore** so Proxmox `ha-manager` can cold-restart it on a survivor (≥2/3 quorum) — the deliberate exception to the local-LVM-thin invariant, which is PG-latency-specific. `claude remote-control` (outbound-HTTPS-only, no inbound exposure) as a systemd unit, driven from the Claude mobile app / claude.ai/code with execution on-box. MacBook retained as the zero-dependency break-glass. **Remote-host Ansible from Frigg closed 2026-06-01:** the shim now materializes the ansible SSH key (`secret/ansible/frigg/ssh-private-key` → `ANSIBLE_PRIVATE_KEY_FILE`) so Frigg runs remote plays, not just `-c local` — validated creating the n8n DB via `postgres-common` (the MacBook can't: py-3.14 hvac-fork crash). Detail + sub-phases in [open-questions.md](open-questions.md). |
| Services — Startpage | ✅ | **Closed 2026-05-31.** Public personal homepage at `home.xiiisins.com` (external-only via cloudflared, no auth — matches the prior nginx container). `k8s/asgard/apps/startpage/`: Caddy pod (3 replicas, 1/worker via required anti-affinity) serving a git-clone of the private `XIIISins/startpage` repo (branch `tokyonight_colors`) into a shared emptyDir. Init container clones over SSH (read-only ed25519 deploy key + pinned `known_hosts` from Vault `secret/k8s/startpage/deploy-key` via ESO) then **whitelist-copies only the page assets** (`index.html`, `userconfig.js`, `src`, `img`) into the webroot — no `.git`/`README`/`Dockerfile`/etc. ever served (verified: those paths 404). ~47MB of binary assets ruled out the apex-static files-in-ConfigMap pattern. Deliberately a **separate app from apex-static** so a clone failure can't take down the OIDC-critical WebFinger pod, and a content update only bounces the startpage. Routing: CF CNAME (`terraform/cloudflare/`) → cloudflared `home.xiiisins.com` → Traefik backchannel → HTTPRoute on midgard `websecure-apex-wildcard`. Validated end-to-end (in-cluster + real CF-edge path 200, git paths 404). Deploy surfaced the new-apex-record negative-cache DNS gotcha (AGH + client `mDNSResponder`, 30-min SOA neg-TTL). **Update workflow: push to the startpage repo, then `kubectl rollout restart deployment/startpage -n startpage`.** Service doc: [`docs/services/startpage.md`](../services/startpage.md). |
| Services — MicroBin | ✅ | **Closed 2026-05-31.** Public pastebin/file-share at `paste.xiiisins.com` (external via cloudflared). Chosen over PrivateBin/Cryptgeon/Opengist after a requirements pass — the only single tool covering syntax-highlighting + file/image sharing + password + expiry (opt-in per-paste encryption; accountless, so "shared janitor" cleanup rather than per-user). `k8s/asgard/apps/microbin/`: `danielszabo99/microbin:2.1.4`, 1 replica/Recreate, **JSON DB + attachments on NFS** (`nfs-client`, mounted at the hardcoded `/app/pasta_data` — see gotcha). **Partial auth**: anonymous create/view/upload open; `/list` + `/admin` gated by Authentik ForwardAuth (`paste-janitors` group) via Gateway-API longest-prefix + a same-ns Traefik Middleware `extensionRef`. Config: `PRIVATE`, `EDITABLE=false` (deletion only via gated `/admin`), `BURN_AFTER`, `HASH_IDS`, 256MB cap, `DEFAULT_EXPIRY=24hour`/`MAX_EXPIRY=never`. Admin creds TF-minted → `secret/k8s/microbin/admin`; `terraform/authentik/microbin.tf` (forward_single provider + app + policy binding) + one-time embedded-outpost attach via API. Validated: `/`→200 anon, `/list`+`/admin`→302→Authentik. Surfaced the **MicroBin JSON-DB-hardcoded-path** gotcha (ignores `MICROBIN_DATA_DIR`) + the partial-ForwardAuth pattern (both in CLAUDE.md). Exposure internet-public (A); escape hatch = gate creation / IP rate-limit if abused. Service doc: [`docs/services/microbin.md`](../services/microbin.md). |
| Services — n8n | ✅ | **Closed 2026-06-01** (commit `e2a7be1`). Workflow automation in asgard K3s (`k8s/asgard/apps/n8n/`): `docker.n8n.io/n8nio/n8n:2.23.1`, single-replica Deployment, **Recreate** (serial TypeORM migrations on boot), **no PVC — stateless-on-disk** (external Patroni Postgres + Vault-injected `N8N_ENCRYPTION_KEY` mean `~/.n8n` holds nothing that must survive restart; UI-installed community nodes would NOT survive — bake into a pinned image layer if ever wanted). PG via Patroni HAProxy VIP `10.0.10.210` (DB+user `n8n`, `DB_POSTGRESDB_SSL_REJECT_UNAUTHORIZED=false` for the self-signed leader cert — n8n analogue of Outline's `sslmode=no-verify`); DB created via `postgres-common` run **from Frigg** (the first real use of Frigg's remote-host Ansible after the SSH-key shim fix — the MacBook can't, py-3.14 hvac-fork crash). **Asymmetric exposure** (two HTTPRoutes): full app (editor UI + REST + webhooks) internal-only at `n8n.niflheim.xiiisins.com` behind Authentik **ForwardAuth** (`n8n-admins` gate — community n8n's native OIDC/SAML is Enterprise-only, so SSO is enforced at the edge with n8n's own owner login as defence-in-depth); **webhook/form trigger paths ONLY** (`/webhook*`, `/form*`) public at `n8n.xiiisins.com` (cloudflared) + `n8n.midgard.xiiisins.com` (LAN), **ungated** (third-party POSTs must skip the Authentik round-trip; n8n authenticates webhooks per-workflow) — any non-trigger path 404s at Traefik. `WEBHOOK_URL` (apex) deliberately split from `N8N_EDITOR_BASE_URL` (niflheim). One ESO `n8n-app` Secret (envFrom) reads `secret/k8s/n8n/{app,postgres-password}`. `terraform/authentik/n8n.tf` (forward_single provider + app + policy binding) + `terraform/{vault,cloudflare,adguard}/`. Service doc: [`docs/services/n8n.md`](../services/n8n.md). |
| Storage — tiering redesign | ✅ | **Closed 2026-05-30** (full hunt: [incident log](../incidents/2026-05-30-storage-redesign-nas-rework.md), runbook [`docs/procedures/synology-storage-redesign.md`](../procedures/synology-storage-redesign.md)). Escapes the **Synology DS223J ~10-LUN DSM-wide hard cap** (corrected from the 2026-05-26 per-volume misdiagnosis — an 11th LUN on a fresh empty volume still failed) by **storage tiering** instead of iSCSI-for-everything: **NFS** (`csi-driver-nfs` chart `4.13.2`, SC `nfs-client`, no LUN limit) for file-class (MicroBin, etc.); **local-path** (Rancher `local-path-provisioner` `0.0.36`, SC `local-path`, `WaitForFirstConsumer`, `reclaimPolicy: Retain`) on a dedicated **50G `scsi1` xfs data disk per worker** at `/data` (provisioned by `ansible/roles/local-path-disk` + the `scsi1` disk in `terraform/proxmox/asgard-k3s`; SELinux `context=` mount option; identify the disk by stable `by-path` not `/dev/sdX`) for app-replicated / downtime-tolerant single-instance (the 3-node **Vault Raft** cluster — Class-L migration off iSCSI — plus VL/VM); **iSCSI** kept only for block-critical single-instance (LMDB/BoltDB — mmap hates NFS); **emptyDir** for caches. Migrations used a quiesce → migrator-pod → `rsync -aH --numeric-ids` → static-rebind → STS-recreate pattern (teamspeak/VL/VM/garage-data). The NAS Volume1 recreate surfaced a five-holder "device busy" chain (iSCSI LUN → DSM package dir → `s2s_daemon` → stale `nfsd` lock → fanotify mark) + the Tailscale-subnet-router LAN-cut footgun. Many gotchas encoded in CLAUDE.md (Storage / local-path-provisioner / Storage migration / Vault Class-L / Tailscale sections). |
| 7 — Jotunheim K3s | 🔲 | Terraform VMs, Flux, services |
| 8 — Observability | 🟡 | VictoriaMetrics + VictoriaLogs (no Grafana — native vmui + VL UI). Phase 8a ✅; Phase 8c (Zabbix LXC, host/LXC layer) ✅; Phase 8b (vm-operator migration) deferred. **Wave-S4 (1.0 stabilization) added the `infra-health-check` active prober** (cert/token/Patroni+etcd-quorum/PBS → Hermod, every 12h) — see [1.0-stabilization.md](1.0-stabilization.md). |
| 8a — VL + VM stack | ✅ | **Closed 2026-05-24/25.** Helm-based deploy in asgard `monitoring` ns: vlsingle chart `0.12.5` → app v1.50.0 (50Gi iSCSI, 30d retention), vmsingle chart `0.38.0` → app v1.143.0 (100Gi iSCSI, 6mo retention), Traefik ForwardAuth middleware via Authentik proxy providers for vmui+VL UI, `/insert/*` path bypasses auth on the VL HTTPRoute. Pod-log DaemonSet via `victoria-logs-collector` chart `0.3.4` (privileged + runAsUser=0 for RHEL SELinux). Off-cluster shippers (LXCs, K3s VM hosts, Proxmox hosts, PBS — 23 hosts total) via `ansible/roles/vlagent/` systemd binary, ingesting through HTTPRoute on niflheim Gateway at `logs.niflheim.xiiisins.com/insert/native`. Companion ClusterIP `victorialogs-ingest` for K3s host-network shippers that can't reach the MetalLB VIP. vmagent (chart victoria-metrics-agent `0.39.0`) + kube-state-metrics (chart `7.4.0` → app v2.19.0) for K8s-layer metrics; Zabbix LXC handles host/LXC-level metrics (clean split). Four vmui custom dashboards shipped via `customDashboardsPath` ConfigMap (cluster-overview, resource-usage, flux-gitops, victoria-self). Surfaced findings: vmagent default RBAC missing `nodes/proxy` (cAdvisor scrape 403), VL fsGroup quirk on iSCSI (chart key `server.initContainers` for chown), Debian 13 trixie ships journald-only (role installs rsyslog), vlagent is CLI-flag-only (no YAML config file) + uses `/insert/native` protobuf endpoint, K3s host network can't reach MetalLB VIPs (companion ClusterIP), RHEL SELinux confines log-collector containers (privileged+runAsUser=0), Authentik embedded outpost has separate `config.authentik_host` cached field bypassing brand resolution (must API-PATCH + declarative blueprint), vmui's `unit` field is a label suffix not a formatter (PromQL-side `/1024^n`), cAdvisor `container_*` carry the node identifier in `instance` not `node` — all in [Known gotchas](../../CLAUDE.md); retro in [incident log](../incidents/2026-05-24-phase-7-observability.md). |
| 8b — vm-operator migration | 🔲 | Refactor vlsingle + vmsingle from raw Helm to vm-operator (`VLSingle` / `VMSingle` CRDs + VMServiceScrape). Deferred — Helm chart deploys are working; revisit when ServiceMonitor-emitting charts start landing or when alerting (VMAlert) becomes a need. |
| 8c — Zabbix (host/LXC layer) | ✅ | Zabbix 7.0 LTS on LXC 1102 (**Urd**, 10.0.11.21, identity **Hugin** — raven of Thought, pairs with Munin/Memory). PG backend via Patroni HAProxy VIP `10.0.10.210`. Authentik **native SAML** SSO (not ForwardAuth — Zabbix has built-in SAML 2.0). Traefik fronts the LXC for `hugin.midgard.xiiisins.com` (LAN, non-SAML) + `hugin.xiiisins.com` (WAN + canonical SAML, via Cloudflared tunnel) via K8s Service + manual EndpointSlice; `hugin-direct.niflheim.xiiisins.com` direct-to-LXC backdoor for K3s-down emergencies. All FQDNs identity-themed. **8c.1/8c.2/8c.7/8c.8 + Round 2 ✅ closed 2026-05-25**; **8c.3–8c.6 (SAML SSO + Traefik fronting + 3-path validation) ✅ closed 2026-05-26.** 7 SAML-deploy fix commits encoded as CLAUDE.md gotchas (community.zabbix module-shape findings × 4, nginx X-Forwarded-Proto reflection, `sign_response = true` on `authentik_provider_saml`, `username_attribute` must match an attached property mapping's `saml_name`). Single-ACS-URL limitation = apex-only SAML by design (midgard alias remains for the backdoor scenario). Retros: [2026-05-25 server+agents](../incidents/2026-05-25-zabbix-server-deploy.md), [2026-05-26 SAML SSO](../incidents/2026-05-26-zabbix-saml-deploy.md); design in [`docs/services/zabbix.md`](../services/zabbix.md). |
| 9 — Secrets runtime retrieval | 🔲 | Migrate from ESO sync-and-cache to Vault Agent / VSO. Pilot on jotunheim first. |
