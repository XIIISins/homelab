<!-- docs/operations/open-questions.md -->

## Open questions / pending tasks

**High priority — forward path:**
- [x] **Re-mint the `ansible-local` Vault AppRole SecretID + fix the 32d-clamp.** ✅ Closed 2026-05-31. The "enforced 2026-06-28 expiry < 90d role TTL" root cause was the **`approle` auth mount's `max_lease_ttl`** at Vault's 768h (32d) default, clamping every role's `secret_id_ttl` (NOT a helper `ttl` arg). Fixed via `tune { max_lease_ttl = "2160h" }` on `vault_auth_backend.approle` in `terraform/vault/` (in-place, 0 destroy); operator applied + re-minted → new SecretID carries a full 90d expiry (2026-08-29). Two adjacent bugs fixed: (a) the `rotate-approle` paste step put the new *accessor* into the 1P `username`/role_id field → `set-vault-token approle` failed `400 invalid role or secret ID`; corrected `username` + `secret_id_accessor` in 1P against authoritative Vault. (b) `vault-root-token` read a stale 1P *title* (`Asgard - Vault - Root Token`, renamed by the S1 reorg) → now UUID `7g4grolyien2yqkm7me2jficmy` in both shims. `ansible-awx` was fine (rotated 2026-05-27, separate helper). All gotchas in CLAUDE.md (Vault + HAProxy sections).
- [ ] **HAProxy stranded-reload self-heal task (notify-independent).** Investigation done 2026-05-31 Wave S3: the original Wave S7 suspicion — `--tags haproxy` filtering out the untagged `reload haproxy` handler — is **DISPROVEN**. A minimal localhost play (tagged task → untagged handler, run with `--tags`) fires the handler normally, so notified handlers are NOT tag-filtered, and tag-scoped role runs are safe. The actual strand was most likely a handler notified but never *flushed* (Ansible flushes at play end; a later task failing or an interrupt between config-render and play-end strands the reload, and the now-unchanged file never re-notifies). **`force_handlers: true` is the wrong fix** — `asgard-haproxy-etcd.yml` has no `serial:`, so it could fire `restart etcd` across all three DCS nodes on a failed play (quorum loss). **Remaining work:** add a notify-independent self-heal to the `haproxy` role — compare the running process start time to `haproxy.cfg` mtime (the documented diagnostic) and `systemctl reload` if stale. Idempotent, per-node, graceful; catches the strand on the next converge regardless of why the handler was missed. Low priority (out-of-band reload is the documented recovery; staleness is detectable).
- [ ] **CP `scsi0` ssd=true — deferred quorum-safe pass.** Workers got the SSD-emulation flag on their OS disks 2026-05-30 (validated in-place, no data touched, rotational→0). The three CPs (Göndul/Hlökk/Sigrún) still report `rotational 1` — they already have `discard = "on"`, just not `ssd`. Roll out the same flag in `terraform/proxmox/asgard-k3s` `control_plane` disk: per-CP `-target` apply (plan must show `0 to destroy` / in-place), then a **Proxmox-level `qm reboot`** to apply it (guest reboot won't), one CP at a time with etcd/K3s quorum checks between (≥2/3 healthy). Low-risk given the worker canary, but CPs are quorum-critical so sequence carefully.
- [x] **Deploy Authentik + Redis** in asgard K3s. ✅ Deployed 2026-05-17 evening. See incident log entry for the deploy story + the gotchas closed in IaC. Bare-LB / plaintext-HTTP for now; HTTPS lands in Phase 5e.1.
- [x] **Phase 4a — CP workload isolation via taint** ✅ Applied 2026-05-21. All three CPs tainted via kubectl (config-template change registers the taint at fresh bootstrap only — existing nodes need explicit kubectl). Workload pods migrated to workers. Vault HA naturally spread: vault-0/1/2 on einherjar-urd/skuld/verd. See incident log for the ~10 findings from the deploy.
- [x] **Phase 4b — Göndul Verd → Urd migration.** ✅ Applied 2026-05-22. Göndul VM destroyed on Verd and recreated on Urd via `terraform apply --target='proxmox_virtual_environment_vm.control_plane["gondul"]'`; stale etcd member cleared via `kubectl delete node gondul`; rejoined as `--server` (not init) via `-e 'k3s_init_node=hlokk'` override. Surfaced orphan-LV class (NUC7-era partial migration left LVs blocking the first clone — recovered via `lvremove`). Vault Raft stayed 3/3 voters throughout. CP topology is now Göndul on Urd, Hlökk on Verd, Sigrún on Skuld — back to the original 2026-05-14 design intent.
- [x] **Worker rebuild — einherjar-urd template_node correction.** ✅ Applied 2026-05-22 evening (immediately after Phase 4b). Doubled as deliberate validation of the worker-rebuild path; surfaced three new findings (Vault chart's hard-required pod anti-affinity, iproute 6.17 `/etc/iproute2/rt_tables` not shipped, orphan-LV class). Vault accepted 2/3 voters during the ~25 min window by design — see incident log + Appendix B (single-worker rebuild) in `docs/procedures/teardown-rebuild.md`.
- [x] **Phase 4c — Verd hardware refresh.** ✅ Applied 2026-05-23. Beelink MINI-S12 (N100, 16GB) → MSI Cubi (i3-1215u, 32GB). Same-node refresh: SSD transplanted, hostname/identity/IQN preserved. Live-migrate → swap → fix NIC name (`nic0` → `enp45s0`) at console → OS updates + reboot test → migrate workloads back. No workload-affecting downtime; cluster stayed 3/3 throughout. Both Cubi nodes now identical hardware. Surfaced two new entries for CLAUDE.md: the NIC-rename gotcha (was silently hit during the Urd refresh too) + the reboot-test persistence-validation rule (now in process expectations).
- [x] **Phase 5e.1 — Traefik + Gateway API + cert-manager + Authentik HTTPS cutover.** ✅ Applied 2026-05-22 evening. Gateway API v1.5.1 Standard CRDs vendored, cert-manager v1.19.0 with Gateway API support, Traefik v40.2.0 chart (proxy v3.7.1) with `NET_BIND_SERVICE` for direct 80/443 binding. Wildcard `*.niflheim.xiiisins.com` issued from Let's Encrypt prod (E8 intermediate, ECDSA). Cluster edge stack live. Surfaced eight findings — see incident log entry. Done: restore cert-manager + Traefik `install.remediation.retries: -1` → `3`.
- [x] **Phase 5e.2 — Cloudflared + apex zone + WebFinger.** ✅ Applied 2026-05-23. Cloudflared 2026.5.0 deployed in `infrastructure/cloudflared/` (3× replicas, hostname anti-affinity, locally-managed tunnel `asgard-k3s`), `credentials.json` via ESO from Vault (`secret/k8s/cloudflared/credentials`) written by `terraform/cloudflare/`. Two new wildcards `*.midgard.xiiisins.com` + apex `xiiisins.com`/`*.xiiisins.com` via cert-manager DNS-01. Second `midgard` Gateway with three HTTPS listeners (`websecure-midgard`, `websecure-apex-wildcard`, `websecure-apex-bare`). WebFinger served by Caddy pod in `apps/apex-static/` (revised from Traefik middleware — see decision row). Authentik migrated to `authentik.xiiisins.com` (external) + `authentik.midgard.xiiisins.com` (internal). End-to-end validated. Surfaced findings: Traefik v3 OSS lacks built-in static-response middleware (revised WebFinger to pod); `tunnel:` field accepts NAME (used `asgard-k3s` to keep config.yaml static across tunnel recreation); QUIC UDP rmem warning benign (kernel default 416KiB < QUIC's 7MiB target, no impact in our traffic envelope).

    **5e.2 prerequisites — closed 2026-05-23:**
    - [x] **Cloudflare account/zone.** ✅ Same Cloudflare account as cert-manager DNS-01 token. Tunnel + DNS records live in the same `xiiisins.com` zone.
    - [x] **Cloudflare Tunnel credential type.** ✅ Locally-managed tunnel: per-tunnel `credentials.json` (JWT) in Vault at `secret/k8s/cloudflared/credentials`, mounted via ESO. `config.yaml` ingress rules in Git as ConfigMap. NOT the `TUNNEL_TOKEN` / remote-managed path (UI-managed config violates "Git is truth"). Account-level `cert.pem` not used at runtime — Terraform handles tunnel/DNS creation.
    - [x] **WebFinger / Authentik admin email.** ✅ `ghost@xiiisins.com` — single human identity across Authentik admin user, WebFinger `subject` field, and (forward) every other OIDC consumer.
    - [x] **5e.2 service scope.** ✅ Authentik + WebFinger + plumbing only. Other externally-exposed services land in their own phases when their internal-only deploys exist.

    **5e.2 sub-phase plan:**
    - [x] **5e.2.a** — Bootstrap `terraform/cloudflare/` module. ✅ 2026-05-23. Cloudflare provider 5.19.0 pinned (IaC pin policy generalized to all providers per row above), Terraform-purpose API token stored in 1Password + local env (NEVER in state). Zone data lookup as smoke test.
    - [x] **5e.2.b** — Two new Certificates in `gateway-config/`. ✅ 2026-05-23. `*.midgard.xiiisins.com` + apex (covers bare `xiiisins.com` + `*.xiiisins.com`). Skipped staging → straight to prod since these are net-new certs (staging is a renewal-validation tool). Renamed existing `certificate-wildcard.yaml` → `certificate-wildcard-niflheim.yaml` for consistency with the per-zone-per-file naming.
    - [x] **5e.2.c** — Second `midgard` Gateway. ✅ 2026-05-23. Three HTTPS listeners (no HTTP — niflheim's no-hostname `web` listener already advertises :80 cluster-wide, second one would trigger Gateway API listener-conflict). Both Gateways on VIP `10.0.20.10`. All listeners `Accepted/ResolvedRefs/Programmed=True`.
    - [x] **5e.2.d** — Terraform tunnel + DNS + Vault KV write. ✅ 2026-05-23. `cloudflare_zero_trust_tunnel_cloudflared.asgard` (v5 provider resource name), `cloudflare_dns_record.authentik` (apex CNAME `proxied=true`), `vault_kv_secret_v2.cloudflared_credentials` writes the `{AccountTag, TunnelSecret, TunnelID}` JSON blob to `secret/k8s/cloudflared/credentials`. Module-ownership decision row settled here.
    - [x] **5e.2.e** — Cloudflared K8s manifests in `infrastructure/cloudflared/`. ✅ 2026-05-23. Hand-rolled Deployment (no Helm chart — same reasoning as Authentik Redis). 3 replicas → 12 tunnel connections to Cloudflare AMS PoPs. Targets Traefik (`https://traefik.traefik.svc.cluster.local:443` with `originRequest.httpHostHeader` + `noTLSVerify: true`) so apex Caddy pod's WebFinger Middleware applies via the apex-bare listener.
    - [x] **5e.2.f** — WebFinger via Caddy pod in `apps/apex-static/` + wire `apps/` Flux Kustomization. ✅ 2026-05-23. Revised from middleware approach (see decision row). Bare-apex DNS record + cloudflared ConfigMap entry for `xiiisins.com` added at the same time. Caddy 2.11.2-alpine, 2 replicas, RFC 7033 `application/jrd+json` response. Cross-namespace HTTPRoute (`apex-static` ns → `traefik` ns Gateway) works without ReferenceGrant because midgard Gateway listeners have `allowedRoutes.namespaces.from: All`.
    - [x] **5e.2.g** — Authentik HTTPRoute migrated to midgard Gateway. ✅ 2026-05-23. Hostnames `authentik.xiiisins.com` + `authentik.midgard.xiiisins.com`. Old niflheim HTTPRoute kept live during cutover for safety.
    - [x] **5e.2.h** — AdGuard rewrite `authentik.midgard.xiiisins.com → 10.0.20.10`. ✅ 2026-05-23. Apex deliberately NOT rewritten — internal clients use midgard FQDN, external clients let public DNS resolve to the tunnel.
    - [x] **5e.2.i** — End-to-end validation. ✅ 2026-05-23. Internal `https://authentik.midgard.xiiisins.com`, external `https://authentik.xiiisins.com`, WebFinger `curl https://xiiisins.com/.well-known/webfinger?resource=acct:ghost@xiiisins.com` (Content-Type `application/jrd+json`) — all three paths green.
    - [x] **5e.2.j** — Cleanup. ✅ 2026-05-23. Old niflheim Authentik HTTPRoute + matching AGH rewrite removed. Cloudflared first-deployed with retries `3` (didn't need the `-1` debug-pattern from 5e.1).
- ✅ **Phase 5e.3 — Tailscale OIDC blueprints + LXCs** (closed 2026-05-23). All sub-phases 5e.3.a–5e.3.f complete. Tailnet operational with custom-OIDC, three LXC-based advertisers (Bifrost/Heimdall subnet-routers + Gjallarbru exit-node), and Munin DSM as a fourth subnet-router for K3s-independent break-glass.

    **5e.3 prerequisites — closed 2026-05-23:**
    - [x] **Tailscale account state.** ✅ Tailnet existed on GitHub signup; replaced 2026-05-23 with fresh custom-OIDC tailnet via `login.tailscale.com/start/oidc`. Free plan, ghost@xiiisins.com primary user.
    - [x] **Tailscale OIDC + WebFinger flow validation.** ✅ 5e.3.c completed 2026-05-23. WebFinger discovery + Authentik OIDC + token exchange validated end-to-end.
    - [x] **LXC OS / template choice.** ✅ Debian 13 (Proxmox default). `baseline` Ansible role already supports both RHEL-family and Debian via `include_tasks` split (`debian.yml` / `redhat.yml`) — no new role needed.
    - [x] **`/dev/net/tun` passthrough for unprivileged LXCs.** ✅ Closed 2026-05-23 in 5e.3.e.iii. Hardcoded `device_passthrough { path = "/dev/net/tun" }` block directly on the Tailscale LXC resource in `lxcs.tf` (original plan was a shared `enable_tun` variable but the code base has no LXC module wrapper — each resource is independent: Factorio one-off, Postgres locals+for_each, Tailscale same pattern — so a shared input variable wouldn't flow anywhere). bpg/proxmox PR #1722 (in v0.70.1+, well below our 0.106.0 pin) made device_passthrough work at create-time on non-clone containers. Surfaced: `device_passthrough` requires `root@pam` even to *create*, not just to modify — see Known gotchas for the aliased-provider workaround (`provider = proxmox.root` with `PROXMOX_VE_PASSWORD` env).
    - [x] **Authentik OIDC provider/application blueprint shape.** ✅ Settled in 5e.3.b. Scopes `openid email profile offline_access`, `sub_mode = user_email`, `default-provider-authorization-implicit-consent` flow, default `scope_profile` provides `groups` claim out of the box (no custom PropertyMapping needed day-1).
    - [x] **Munin Tailscale install path.** ✅ DSM package (Synology's official Tailscale package). Set-and-forget, integrates with DSM auto-start + update story. Note: design doc previously referenced an existing Docker-container OOB Tailscale advertiser on Munin — that was incorrect, Munin currently runs nothing after the wipe.

    **5e.3 sub-phase plan:**
    - [x] **5e.3.a** — Bootstrap `terraform/authentik/` module. ✅ 2026-05-23. Provider `goauthentik/authentik 2026.2.0` pinned. AUTHENTIK_URL + AUTHENTIK_TOKEN via env (token from 1P `Asgard - Authentik - akadmin API token`, also mirrored to Vault at `secret/k8s/authentik/bootstrap-token`). Smoke test: `data "authentik_flow" "default_authentication"` lookup confirms provider auth.
    - [x] **5e.3.b** — Authentik IaC: OAuth2Provider + Application + identity-as-data. ✅ 2026-05-23. Files split: `identity.tf` (users + groups + cross-reference validation via `terraform_data.identity_validation` precondition), `tailscale.tf` (OAuth2 + Application + PolicyBinding), `users.yaml` + `groups.yaml` (data files). Groups: `authentik-admins` (is_superuser), `tailscale-users`, `ssh-users` (future SSSD). Existing `ghost` user migrated via `terraform import` from blueprint to TF; blueprint `01-users.yaml` deleted. Client secret generated → Vault at `secret/k8s/authentik/tailscale-client-secret` per module-ownership rule. Surfaced findings: (a) Terraform state-rename hiccup mid-apply (had to `terraform state mv` to rename `authentik_group.tailscale_users` → `authentik_group.this["tailscale-users"]` after first failed apply left the group orphaned in Authentik); (b) email mismatch — Authentik had `xiii@xiiisins.com`, GitHub primary flipped to `ghost@xiiisins.com`, TF apply corrected Authentik to match.
    - [x] **5e.3.c** — Tailscale OIDC smoke test (gate for LXC work). ✅ 2026-05-23. Initial attempt blocked by Cloudflare bot protections 403'ing the WebFinger probe (see incident log + Cloudflare re-harden pending task). After unblock, full flow worked: WebFinger discovery → Authentik OIDC → token exchange → tailnet provisioned with Authentik IdP. Note: tailnet creation is signup-time-only via `login.tailscale.com/start/oidc` — existing tailnets can't switch IdP via UI (would need support ticket). Old GitHub-bound tailnet was empty so cleanest path was delete-and-recreate.
    - [x] **5e.3.d** — Bootstrap `terraform/tailscale/` module. ✅ 2026-05-23. Provider `tailscale/tailscale 0.28.0` pinned. OAuth client created via Trust credentials UI with scopes Policy File / Devices Core / Devices Routes / Auth Keys all Write, tagged `tag:terraform-tag-owner`; credentials in Vault at `secret/k8s/tailscale/oauth-client`. Bootstrap pattern (manual two-step before TF can take over — see decision row): UI plant `tagOwners` block → create tagged OAuth client → `terraform import tailscale_acl.this acl` → TF owns ACL forward. Policy file in external `terraform/tailscale/policy.hujson` using grants syntax (default-allow within tailnet, autoApprovers `10.0.0.0/16` supernet for tag:subnet-router + IPv4+IPv6 default routes for tag:exit-node). Surfaced findings: (a) Tailscale renamed "OAuth clients page" → "Trust credentials" (per Jan 2026 changelog) — UI navigation is `Settings → Trust credentials` not `Settings → OAuth clients`; (b) plan diff was heavy on first apply because the default-on-signup Tailscale policy carries commented-out starter content (group/tagOwners/postures/ssh examples) — all stripped; (c) Tailscale grants syntax is the current recommendation, acls is legacy (decision-row); (d) Tailscale Trial → Free downgrade in 14 days from 2026-05-23, all features used (custom OIDC + Trust credentials + OAuth + ACL) work on Free, no impact expected.
    - [x] **5e.3.e** — Tailscale LXCs (Bifrost 1113 / Heimdall 1114 / Gjallarbru 1115) in Proxmox via Terraform + Ansible. ✅ Closed 2026-05-23. Bifrost+Heimdall subnet-router pair (Urd+Verd, `tag:subnet-router`, `--advertise-routes=10.0.0.0/16`); Gjallarbru exit node (Skuld, `tag:exit-node`, `--advertise-exit-node`). Debian 13 cloud-init, unprivileged, `/dev/net/tun` via hardcoded `device_passthrough` block on the Tailscale resource. End-to-end validated: all three joined tailnet, routes/exit-node auto-approved per policy, "cannot relay traffic" warnings cleared once IP-forwarding sysctls landed. Surfaced findings: `device_passthrough` requires `root@pam` to create (not just modify) — aliased-provider workaround documented in Known gotchas; Tailscale auth-key API caps lifetime at 90d (no truly indefinite, see decision-row + gotcha); `tailscale_tailnet_key.description` charset constraint (alphanumeric+spaces+identifier-hyphens only, see gotcha); IP forwarding sysctls required for Tailscale relay (kernel-state-declarative — `net.ipv6.conf.all.forwarding=1` satisfies the check even on v4-only LANs, see gotcha); the previously-cited "SFTPGo Vault proof-of-pattern" is wrong — that role still does local pwgen, real proof-of-pattern is `ansible/playbooks/test-vault-lookup.yml` (see CLAUDE.md correction + new pending task); Vault path convention pinned to `secret/<consumer-domain>/...` (see new decision row). Note: this phase was over-decomposed into i–v sub-steps (preserved below as historical record per the new phase-structure rule in CLAUDE.md; routine work like this would now be a single L3 step with checkboxes, not nested sub-phases).
        - [x] **5e.3.e.i** — Tighten `bpg/proxmox` provider pin in `terraform/proxmox/asgard-lxcs/versions.tf` AND `terraform/proxmox/asgard-k3s/versions.tf` from float `~> 0.77` to concrete `0.106.0`. ✅ 2026-05-23. Closed the proxmox half of the IaC-pin-policy debt surfaced 2026-05-22.
        - [x] **5e.3.e.ii** — Three `tailscale_tailnet_key` resources in `terraform/tailscale/authkeys.tf`. ✅ 2026-05-23. Reusable + recreate-if-invalid + max-allowed `expiry=7776000` + defensive `lifecycle { ignore_changes = [expiry] }`. Per-host scoping to `tag:subnet-router` (Bifrost/Heimdall) or `tag:exit-node` (Gjallarbru). Surfaced: Tailscale API rejects parens/em-dashes/slashes in description field — safe template `"<hostname> <tag> managed by terraform"`. Authkeys written to Vault at `secret/k8s/tailscale/authkeys/<hostname>` initially; **moved to `secret/ansible/tailscale/authkeys/<hostname>` in 5e.3.e.iv** to align with consumer-domain convention (see new decision row).
        - [x] **5e.3.e.iii** — `tailscale_nodes` locals map + `proxmox_virtual_environment_container.tailscale` for_each resource in `terraform/proxmox/asgard-lxcs/lxcs.tf`. ✅ 2026-05-23. Mirrors postgres pattern. Per-node placement Urd/Verd/Skuld. 1 vCPU / 512 MB / 4 GB / VLAN 11 / nesting=true / `device_passthrough { path = "/dev/net/tun" }`. Surfaced: `device_passthrough` needs `root@pam` even to create — fixed via aliased provider (`provider "proxmox" { alias = "root", username = "root@pam" }` + `PROXMOX_VE_PASSWORD` env + `configuration_aliases = [proxmox.root]` in versions.tf + `provider = proxmox.root` on the resource).
        - [x] **5e.3.e.iv** — `ansible/roles/tailscale/` + `playbooks/tailscale-host.yml` + `inventory/hosts.yml` `tailscale_hosts` group. ✅ 2026-05-23. Debian-only role: apt repo + signing key (trixie codename), tailscale install, IP forwarding sysctls persisted to `/etc/sysctl.d/99-tailscale.conf`, `tailscale up` with per-host advertise flags. Authkey via `community.hashi_vault.vault_kv2_get` lookup using minimal env-driven AppRole pattern (matches `playbooks/test-vault-lookup.yml`, NOT the SFTPGo role which still does local pwgen — see new pending task). Surfaced: CLAUDE.md proof-of-pattern reference was wrong (fixed in CLAUDE.md); ansible.posix.sysctl loop initially only had `net.ipv4.ip_forward` — extended to also cover `net.ipv6.conf.all.forwarding` after gjallarbru kept flagging "cannot relay" (see IP-forwarding gotcha for why kernel-state-declarative check needs both even on v4-only LANs).
        - [x] **5e.3.e.v** — End-to-end validation. ✅ 2026-05-23. Bootstrap with `-e 'ansible_user=root' --tags baseline`, then full apply as `ansible`. All three LXCs appear in tailnet admin console, Bifrost+Heimdall's `10.0.0.0/16` route auto-approved, Gjallarbru's exit-node auto-approved (after IPv6 forwarding sysctl added — see gotcha). No "cannot relay traffic" warnings after sysctl fix landed.
    - [x] **5e.3.f** — Munin DSM Tailscale subnet-router. ✅ 2026-05-23. Synology Package Center install (Tailscale's DSM-native package, ~one version behind upstream stable, fine for our use). UI-minted single-use authkey tagged `tag:subnet-router`, stored in 1Password — first homelab secret following the human-consumer → 1P rule with no Vault path (see "Munin Tailscale authkey: out-of-band" decision row). Procedure in `docs/procedures/teardown-rebuild.md` Appendix C.
        - [x] `policy.hujson` extended — `autogroup:admin` added to `tagOwners["tag:subnet-router"]` so admin-UI key minting works (TF apply, single ACL diff).
        - [x] Authkey minted via Tailscale admin UI (single-use, `tag:subnet-router`, pre-approved); stored in 1Password "Homelab" vault.
        - [x] DSM Package Center install + Task Scheduler boot-up task (`tailscale-tun-permissions`, root, runs `tailscale configure-host; synosystemctl restart pkgctl-Tailscale.service`) per Tailscale's DSM-7 TUN requirement.
        - [x] Configure-host script run manually over SSH instead of rebooting — K3s PVCs are iSCSI-attached and a NAS reboot would risk fs-RO recovery (see iSCSI/ext4-journal-abort gotcha). Boot task validates next time DSM reboots for unrelated reasons.
        - [x] `tailscale up --authkey=... --advertise-routes=10.0.0.0/16 --hostname=munin` via SSH. Verified in admin console: tagged `tag:subnet-router`, `10.0.0.0/16` route auto-approved, no "cannot relay" warning.
- [ ] **Create 1Password "Homelab" vault** if not already done. Migrate existing scattered homelab credentials in. Authentik bootstrap creds (akadmin pw + API token + personal user pw) added 2026-05-17. Cloudflare DNS-01 token added 2026-05-22.

**Immediate post-flight from 2026-05-22 Phase 5e.1:**
- [x] **Restore HelmRelease `install.remediation.retries: 3`** on cert-manager + Traefik. ✅ Confirmed already at `3` going into 5e.2; was not changed during 5e.1 close-out.
- [x] **Document `adguardhome-sync` location and config in the AdGuard role.** ✅ Closed 2026-05-25 by Phase 5b.2 — `ansible/roles/adguardhome-sync/` exists with the documented paths (binary at `/usr/local/bin/adguardhome-sync`, systemd unit `adguardhome-sync.service`, config `/etc/adguardhome-sync.yaml`) + a README. Grep-discoverable.
- [x] **AGH sync interval `*/30` → `*/1`** in the Ansible role. ✅ Closed 2026-05-25 by Phase 5b.2 — `adguardhome_sync_cron: "*/1 * * * *"` is the default in `ansible/roles/adguardhome-sync/defaults/main.yml`.
- [ ] **Investigate `adguardhome-sync` "Sync done" sub-microsecond duration when origin had changes.** During 5e.1.i, multiple cron cycles logged "Sync done" with `2.29e-07s` duration but rewrites didn't propagate to replicas. Restarting the service + bumping interval recovered it. Could be internal cache surviving cron firings, could be UI-vs-API state lag on AGH origin. Worth a real root-cause investigation before assuming `*/1` interval is a real fix.

**Control-node tooling consolidation (pending):**
- [ ] Create 1P item `Ansible - Vault - k3s` (Homelab vault) with fields:
  - `url` = `http://10.0.20.11:8200`
  - `method` = `approle`
  - `username` = current `ANSIBLE_HASHI_VAULT_ROLE_ID` (copy from `~/.config/ansible/vault-approle.env`)
  - `password` = current `ANSIBLE_HASHI_VAULT_SECRET_ID` (copy from same)
  - `secret_id_accessor` = current accessor (from prior 1P storage or `vault list auth/approle/role/ansible-local/secret-id`)
  - `expires_at` = today + 90 days from original generation
- [ ] Confirm 1P item `Asgard - Vault - Root Token` has the root token in its `password` field
- [ ] Commit `<repo>/.config/fish/conf.d/homelab.fish` to repo
- [ ] Symlink: `ln -s <repo-path>/.config/fish/conf.d/homelab.fish ~/.config/fish/conf.d/homelab.fish`
- [ ] `exec fish` (or open a new shell) to source it
- [ ] Smoke test: `homelab-env` loads vars, `set-vault-token root` + `set-vault-token approle` both work, `vault kv get secret/ansible/test/hello` returns the test value after `set-vault-token approle`
- [ ] Dry-run rotation round-trip:
  1. `set-vault-token root`
  2. `rotate-approle ansible-local` — at the "Press enter" prompt, **Ctrl+C** (does NOT update 1P; creates an orphan SecretID in Vault)
  3. `rotate-approle --fix ansible-local` — should find one orphan, show its metadata, prompt; confirm to destroy
  4. `set -e VAULT_TOKEN`

  Validates both flows without rotating the real credential.
- [ ] Delete `~/.config/ansible/vault-approle.env`
- [ ] Delete the standalone `~/.config/fish/functions/ansible-vault-env.fish` (superseded by `homelab.fish`)

**Architectural decisions surfaced by 2026-05-21 Phase 4a — pending:**
- [x] **Synology CSI DaemonSet — should it tolerate CP taint?** ✅ Closed 2026-05-21 evening. Decision: **No, CSI stays workers-only.** The Phase 4a posture (CSI evicted from CPs) is the correct steady state — it closes the cross-node iSCSI session-fight class which was the original motivation. The "stateful pods hung Terminating on tainted CP" footgun is acknowledged and mitigated by an operational rule: **drain stateful workloads from any CP before retainting** in future operations. Current state is stable (workloads are on workers, not CPs, by virtue of the taint plus normal scheduling), so the failure mode is dormant unless we ever untaint + retaint. No HelmRelease values change. CLAUDE.md "CSI eviction footgun" gotcha already captures the operational rule. Decision logged in Key decisions table.
- [x] **Helm chart pin policy — tighten now, not at 2.0?** ✅ Closed 2026-05-22 as Phase 4b prerequisite. Decision: concrete-pin all HelmRelease charts, no minor-floats. Tightened: sealed-secrets 2.18.6, vault 0.32.0, external-secrets 0.20.4, metallb 0.15.3. Renovate stays deferred until stable state — updates are deliberate operations until then. The "pin at 2.0" deferral was retired after the 2026-05-21 Phase 4a session disproved the underlying assumption that minor-version bumps would be safe.

**Cleanup pending from 2026-05-21 Phase 4a:**
- [x] **Diagnose einherjar-urd sshd dead.** ✅ Closed 2026-05-21 evening. Root cause: NUC7-era hard crash on 2026-05-20 07:00 corrupted hostkey files to zero bytes mid-write. Recovery: delete empties, `ssh-keygen -A`, restart sshd. CLAUDE.md gotchas added for hostkey-zero-byte class and `last reboot` "still running" diagnostic. See incident log entry.
- [x] **Clean up orphan iSCSI sessions on einherjar-skuld.** ✅ Closed 2026-05-21 evening. Original three UUIDs had aged out by the time this ran; cleaned two newly-surfaced orphan *node records* from Phase 4a's reshuffle (vault-0 and vault-2 PVs that are now legitimately bound elsewhere). NAS-side cleanup not needed. Structural finding: iSCSI node records persist across pod migrations and are not cleaned by CSI — recurring class, automation-vs-manual-sweep decision pending below. CLAUDE.md gotcha added.
- [x] **Investigate authentik-server pod with 53 restarts on einherjar-skuld.** ✅ Closed 2026-05-21 evening. Not currently restarting; last restart was clean Completed exit (code 0), 4h before session close. Pre-existing pattern attributed to pre-4a workload concentration on einherjar-skuld. Monitoring posture: investigate if count climbs past ~55 in coming days.

**Service backlog (in revised LXC order):**
- [x] **Factorio LXC** (1120, Urd) — ✅ Deployed 2026-05-16. UDP 34197 + TCP 22022 port-forwards active.
- [x] **PostgreSQL Fulla** (1130, Skuld) — ✅ Deployed 2026-05-17. Standalone PG 17 + TLS + management users + DB provisioning machinery. Per-service DB provisioning validated by Authentik (2026-05-17 evening). Cluster expansion to Vör/Idunn (and HAProxy VIP 10.0.10.210) deferred until further consumers exist.
- [x] **Teamspeak** — ✅ Closed 2026-05-25. Pivoted from planned LXC 1121 → asgard K3s deploy (`k8s/asgard/apps/teamspeak/`). TS3 3.13+ ts3db_postgresql plugin against Patroni via HAProxy VIP 10.0.10.210, MetalLB VIP `10.0.20.12` (UDP 9987 + TCP 30033, shared-ip annotation, ETP=Local). External clients reach via existing SRV ring `_ts3._udp.ts3.xiiisins.com` → `hel-ts3.xiiisins.com` (homelab, primary) + `do-ts3.xiiisins.com` (DigitalOcean failover). End-to-end validated. See [docs/services/teamspeak.md](../services/teamspeak.md), retro [incident log](../incidents/2026-05-25-teamspeak-k3s.md).
- [ ] **Tailscale LXCs** (1113/1114/1115) — Phase 5e.3.
- [ ] **HAProxy** (1133/1134/1135) — PostgreSQL frontend, after PG cluster.
- [ ] **Phase 6 — Human-facing secret management (Vault OIDC).** Vault becomes the primary access point for human-readable *homelab-scoped* secrets — admin passwords, API tokens, recovery info — accessed via the Vault UI with Authentik OIDC SSO. 1Password retained for: bootstrap creds (Vault root token, AWS KMS unseal token, SSH recovery key) that must survive Vault being down; non-homelab credentials (banking, personal, family-shared); offline mirror of every homelab secret as belt-and-suspenders. Vault keeps `tls_disable=1` on its listener (deferred decision); Traefik terminates TLS at `vault.niflheim.xiiisins.com`. Day-1 group mapping is single-tier: Authentik `vault-admins` → Vault `homelab-admin` policy → all paths under `secret/*`. Code structured so adding a `vault-readers` tier later is one new group + one new policy + one new role binding. Sub-phases:
    - [ ] **6.a** — Authentik OIDC application + `vault-admins` group (TF). New file `terraform/authentik/vault.tf` mirroring the netbox/outline OIDC pattern. `authentik_provider_oauth2` with redirect URI `https://vault.niflheim.xiiisins.com/ui/vault/auth/oidc/oidc/callback` (Vault UI's built-in callback path) + `oidc/callback` (Vault CLI helper-listener path on `localhost:8250` — for `vault login -method=oidc` from a workstation). `authentik_application` + `authentik_policy_binding` gated on the new `vault-admins` group (added to `groups.yaml` + `users.yaml`'s ghost entry). Client secret minted via `random_password`, written to Vault at `secret/k8s/vault/oidc` (chicken-and-egg note: this Vault write happens via the existing TF→Vault provider auth which uses the root token from operator env — same as every other TF→Vault hand-off).
    - [ ] **6.b** — Vault OIDC auth method + policy + role (TF — `terraform/vault/`). Three new resources: `vault_jwt_auth_backend` (type=`oidc`, default mount `oidc`, discovery URL = Authentik well-known endpoint), `vault_policy.homelab_admin` (granting `read+list` on `secret/*` plus `update` on `sys/wrapping/*` for the UI's secret-wrap workflow), `vault_jwt_auth_backend_role` binding `bound_audiences` = our Authentik client_id, `user_claim` = `email`, `bound_groups` = `["vault-admins"]`, `token_policies` = `["homelab-admin"]`, `token_ttl` = `1h`. Vault reads the Authentik client_id + secret from `secret/k8s/vault/oidc` (the path 6.a wrote). The TF apply order is: 6.a first (writes the OIDC client creds to Vault), then 6.b (reads them to wire the auth method).
    - [ ] **6.c** — Traefik HTTPRoute fronting Vault UI + Service type flip. The existing `vault-ui` Service in the `vault` namespace is type=`LoadBalancer` with a dynamic-allocated MetalLB IP (currently `10.0.20.11`). New `k8s/asgard/apps/vault-ingress/` kustomization: `Service` patch flipping `LoadBalancer` → `ClusterIP` (releases `.11` back to the pool — closes the static/dynamic collision gotcha) + `HTTPRoute` on the niflheim Gateway at `vault.niflheim.xiiisins.com` → `vault-ui` Service in the `vault` namespace (cross-namespace; uses the niflheim Gateway's `allowedRoutes.namespaces.from: All` setting same as apex-static). No ForwardAuth — Vault handles OIDC natively, double-auth would be redundant + would also re-introduce the K3s-Authentik dependency that the Vault UI shouldn't have.
    - [ ] **6.d** — DNS rewrite. `terraform/adguard/rewrites.tf` `locals.rewrites` += `"vault.niflheim.xiiisins.com" = "10.0.20.10"`. Same shape as every other K8s-fronted internal FQDN.
    - [ ] **6.e** — Smoketest + post-flight. Browse `https://vault.niflheim.xiiisins.com` → Authentik OIDC round-trip → land in Vault UI with `homelab-admin` policy active. Verify path-scoped reads (e.g. `secret/ansible/zabbix/admin-password` → readable). Verify CLI flow: `vault login -method=oidc role=homelab-admin` from operator workstation → opens browser → Authentik → CLI gets token. Post-flight: update CLAUDE.md "Architectural invariants → Secrets — three stores" rule with the new Vault-UI role + add a discipline note that every new homelab credential gets stashed in 1P as offline backup (no IaC change; doc-only rule).

- [x] **Phase 8c — Zabbix LXC (1102, Urd, 10.0.11.21).** ✅ Fully closed 2026-05-26 (all sub-phases ticked). Moved out of Phase 5h's "remaining LXCs" bucket — Zabbix is observability-stack core (K3s-independent counterweight to 8a's in-cluster VL/VM placement), not just-another-workload. Zabbix 7.0 LTS + PG backend via Patroni HAProxy VIP `10.0.10.210` + Authentik **native SAML** (not ForwardAuth) + Traefik-fronted LAN/WAN URLs + operator backdoor for K3s-down emergencies. Server scaffolding pre-existed (TF block in `terraform/proxmox/asgard-lxcs/lxcs.tf` lines 537–605, both Ansible roles in `ansible/roles/zabbix-{server,agent}/`, both playbooks `zabbix-host.yml`/`zabbix-agents.yml`) but undeployed; gaps are auth wiring + ingress + secrets minting. Full design in [`docs/services/zabbix.md`](../services/zabbix.md); three decision rows in [`decisions.md`](decisions.md) (phase promotion, native-SAML, Traefik-fronts-LXC). Sub-phases:
    - [x] **8c.1** — ✅ Closed 2026-05-25 (commit 2d7dc51). Four Vault paths minted under `secret/ansible/{postgres,zabbix}/*`: `postgres/zabbix-password` (32-char), `zabbix/admin-password` (24-char break-glass), `zabbix/saml-sp-keypair` (RSA 2048 self-signed), `zabbix/api-token` (placeholder, `lifecycle.ignore_changes` on `data_json` so 8c.7 bootstrap can populate without TF clobber). Pattern matches netbox/teamspeak dual-mint shape; ansible-only consumer so no `_k8s` siblings.
    - [x] **8c.2** — ✅ Closed 2026-05-25. LXC 1102 placed on **Urd** (relocated from original Skuld plan in 4000c96 — Skuld 16GB outlier already carries Fulla/Snotra/Sigrún/Einherjar-skuld/Kvasir/Gjallarbru). Sizing bumped 2G→4G. Norse identity = **Hugin** (raven of Thought, pairs with Munin = Memory). TF rename `zabbix→hugin` propagated across `proxmox/asgard-lxcs/` + `netbox/vms.tf` + `adguard/rewrites.tf` in 0070a0a; service tag stays `zabbix` (function-level). Role finalized in 22f3a18 (Vault lookup DRY, pipefail+`ON_ERROR_STOP=1` on schema load, parameterized `$ZBX_SERVER_NAME`). Three deploy-time fixes: repo URL `/release/` segment dropped (7ed0a19), nginx default-site removal added (f55d1f4), admin password comment corrected (f55d1f4). Full retro in [`docs/incidents/2026-05-25-zabbix-server-deploy.md`](../incidents/2026-05-25-zabbix-server-deploy.md). Operator step left for 8c.7: rename schema-default "Zabbix server" host in UI to `hugin` to match agent's `Hostname=`.
    - [x] **8c.3** — ✅ Closed 2026-05-26. Authentik SAML provider via Terraform. `terraform/authentik/zabbix.tf` ships `authentik_provider_saml` + `authentik_application` + `authentik_policy_binding` gated on `zabbix-admins` group, SP cert registered as `authentik_certificate_key_pair.zabbix_sp` for AuthnRequest sig verification, TF→Vault hand-off at `secret/ansible/zabbix/saml-idp`. Single canonical ACS URL = apex (`hugin.xiiisins.com`) per `authentik_provider_saml` API constraint. One mid-deploy TF-quirk: computed `url_sso_redirect` / `url_slo_redirect` return literal `"-"` until `terraform apply -refresh-only` (then a regular apply propagates real URLs into Vault). One fix landed during validation: `sign_response = true` on the provider (default false; Zabbix's `sign_messages: true` requires the outer Response signed too — commit 89f205d).
    - [x] **8c.4** — ✅ Closed 2026-05-26. Zabbix server role extension + deploy. `roles/zabbix-server/tasks/saml.yml` writes SP+IdP cert PEMs (`/usr/share/zabbix/conf/certs/`, root:www-data 0640), declares `userdirectory_saml` row via `community.zabbix.zabbix_user_directory`, enables SAML globally via `community.zabbix.zabbix_authentication`. 4 first-deploy fixes against community.zabbix 4.2.0's actual module shape: `provision_groups[].role` not `role_name` (5ef72f1), SAML fields flat at module level not nested under `saml:` (4d70db4), `disabled_usrgroup` required when JIT enabled (62ea3e8), `web-config.yml` template-render tasks needed `zabbix:saml` tag to re-render conf.php on the SAML pass (71abdbd). One nginx-side fix: `X-Forwarded-Proto` → `HTTPS` fastcgi_param reflection so Zabbix builds `https://` ACS URLs behind Traefik (2ab39d0). One IdP-attribute fix: `username_attribute` switched to UPN's actual `saml_name` (`http://schemas.xmlsoap.org/ws/2005/05/identity/claims/upn`) — the draft's `.../uid` referred to an unattached property mapping (2e0bcce). Day-1 mapping single-entry: Authentik `zabbix-admins` → Zabbix `Zabbix administrators` w/ Super admin role.
    - [x] **8c.5** — ✅ Closed 2026-05-26. Ingress live: AGH rewrite `hugin.midgard.xiiisins.com` → `10.0.20.10`, Cloudflare CNAME `hugin.xiiisins.com` → tunnel, `k8s/asgard/apps/zabbix-ingress/` (namespace + Service-without-selector + EndpointSlice → `10.0.11.21:80` + HTTPRoute on midgard Gateway with both hostnames), `cloudflared` ConfigMap += `hugin.xiiisins.com` → Traefik backchannel (rollout-restart cycled the cloudflared pods for the subPath-mount gotcha). No ForwardAuth — Zabbix handles SAML natively.
    - [x] **8c.6** — ✅ Closed 2026-05-26. 3-path validation done: WAN apex (mobile cellular) ✓, LAN apex ✓, backdoor `hugin-direct.niflheim` ✓. Single-ACS-URL limitation re-confirmed at deploy time (LAN→midgard fails with "ACS URL doesn't match Provider ACS URL" — expected, documented in advance in `terraform/authentik/zabbix.tf` comment + procedure §5). Operator decision: keep midgard alias as-is (non-SAML hostname for the backdoor scenario). Procedure §6 tightening (disable Zabbix sign-up for non-Admin) deferred — operator step when ready. Full retro: [`docs/incidents/2026-05-26-zabbix-saml-deploy.md`](../incidents/2026-05-26-zabbix-saml-deploy.md).
    - [x] **8c.7** — ✅ Closed 2026-05-25, **architecture changed mid-flight**. Original plan was a server-side bootstrap playbook with auto-registration-action creation. Recast as **declarative-per-host registration**: each agent's playbook run does `community.zabbix.zabbix_host` against the API with host_groups + templates + macros sourced from inventory group_vars (inventory = source of truth). New shared playbook `ansible/playbooks/zabbix-host-groups.yml` aggregates `zabbix_agent_host_groups` across inventory and pre-creates each named group (throttle: 1, run_once); imported by both `zabbix-host.yml` + `zabbix-agents.yml` so either can run first. API token mint stays an operator step (Zabbix UI as `ansible` user with **Super admin role** — `Admin role` alone has no global host visibility); then `vault kv put secret/ansible/zabbix/api-token value=<token>`. Auto-registration-action path obsoleted by the inventory-as-truth design.
    - [x] **8c.8** — ✅ Closed 2026-05-25. Agent2 deployed to all 23 inventory hosts (LXCs + K3s VMs + Proxmox hypervisors + PBS). Role refactored OS-aware: `install-debian.yml` (apt), `install-rhel.yml` (dnf + `ansible.posix.seboolean: zabbix_can_network`), `configure.yml` common. Inventory cleanup: `proxmox_hosts` + `backup_hosts` get `ansible_user: root` in group_vars (no `ansible` user exists on those). PBS apt repo fix (deb822 `pbs-enterprise.sources` removed; no-subscription was already in /etc/apt/sources.list). Declarative host registration via `community.zabbix.zabbix_host` with the httpapi connection plugin (community.zabbix 4.x dropped env-var auth) + `become: false` on delegated tasks + `throttle: 1` (parallel-forks race) + inline Vault lookup (set_fact-on-localhost scoping issue). Hierarchical host groups inventory-driven. Round 2 monitoring depth shipped same-day: PG (`zbx_monitor` user + agent2-plugin-postgresql), HAProxy (agent2 web.page.get → localhost:9000), Etcd (server-side HTTP scrape), Proxmox VE (new TF module `terraform/proxmox/zabbix-access/` mints user + API token, Vault-stored), Nginx + PHP-FPM on hugin (localhost stub_status + status/ping). 3478/3521 (98.8%) items healthy. Full retro in [`docs/incidents/2026-05-25-zabbix-server-deploy.md`](../incidents/2026-05-25-zabbix-server-deploy.md).
- [x] **Phase 5h.2 — Notifications (Hermod LXC 1103, Verd, 10.0.11.22).** ✅ **Closed 2026-05-26.** AppriseAPI aggregator + tag-driven Discord routing. Hub deployed 2026-05-25 ([hub-deploy retro](../incidents/2026-05-25-hermod-deploy.md)); producer wiring + live failover validation done 2026-05-26 ([producer-wiring retro](../incidents/2026-05-26-hermod-producer-wiring.md)). Sub-phases:
    - [x] **5h.2.a** — `terraform/proxmox/asgard-lxcs/` Hermod resource (Debian 13, 1 vCPU / 512 MB / 4 GB, VLAN 11 / 10.0.11.22, unprivileged + nesting). Done 2026-05-25 (commit 05e1631).
    - [x] **5h.2.b** — `terraform/netbox/` Hermod records: VM (vmid=1103, role=`notifications` — new role distinct from `monitoring`/Zabbix, device=verd) + interface + IP (10.0.11.22/24) + primary_ip binding. Done 2026-05-25 (commit 05e1631).
    - [x] **5h.2.c** — `ansible/roles/hermod-api/` — AppriseAPI install via pip+venv+gunicorn+systemd (Docker explicitly avoided, matches all-native-systemd LXC pattern). Renders `/etc/apprise/config/<config-key>.yml` from Vault. Companion `ansible/roles/caddy-reverse-proxy/` (generic, reusable) fronts AppriseAPI with `remote_ip` allowlist matcher. Done 2026-05-25 (commits b075ba7 + d87876c).
    - [x] **5h.2.d** — `ansible/playbooks/asgard-hermod.yml` — baseline + caddy-reverse-proxy + hermod-api + vlagent + zabbix-agent + hardening. `hermod_hosts` group in `inventory/hosts.yml`; producer allowlist in `group_vars/hermod_hosts.yml`. Done 2026-05-25 (commit d87876c).
    - [x] **5h.2.e** — Four Discord channels (Hrist/Mist/Ölrún/Hel for `critical`/`alert`/`media`/untagged) created in Discord UI; four webhooks minted; written to Vault at `secret/ansible/hermod/discord/{critical,alert,media,untagged}` as a single `url` field per tag (not `id`+`token` — schema changed to match Discord's UI exposure). Config-key TF-minted via `random_password` at `secret/ansible/hermod/config-key`. Done 2026-05-25.
    - [x] **5h.2.f** — Source-side wiring (separate PRs per producer):
        - [x] Zabbix media type → Hermod HTTP POST. Declarative via `community.zabbix.zabbix_mediatype` + `community.zabbix.zabbix_user` (attach to Admin; default trigger action carries it through). JS webhook script maps severity → Apprise tag (Disaster/High → `critical`, Average → `alert`, lower suppressed). Done 2026-05-26 (commit 9c9eef7; [retro](../incidents/2026-05-26-hermod-producer-wiring.md)). Pivoted from the original `zabbix_action`-binding sketch — Admin is already in the Zabbix administrators group + the stock trigger action targets that group, so no Action wiring needed.
        - [x] Patroni `callbacks.on_role_change` bash script on PG hosts. Done 2026-05-26 (commit c344697; colour-symmetry follow-up post-failover-validation; `primary` role-alias fix in follow-up after live validation surfaced the Patroni 4.x rename). All role transitions (`primary` / `master` [legacy] / `replica` / `standby_leader`) → `alert/warning` (yellow), unknown role → silent skip. Always exits 0 — supervisor-loop-safe. The originally-sketched "all-replicas-lost → critical" path is NOT handled by an `on_role_change` callback — Patroni only fires role-change on the affected node, and a node which can't see DCS can't reliably send notifications either. Cluster-wide quorum-loss detection lives in the Zabbix Patroni / etcd templates (server-side metrics scrape, already deployed Phase 8c Round 2). Defence-in-depth across two independent observation paths.
        - [ ] Ansible failure handlers in roles where drift/correction outcomes matter (not every role) — folded into Phase 5h.3 drift-check loop.
        - [ ] Optional: PBS notification hook → `critical` on backup failure (Proxmox-host-level integration). Deferred — PBS hasn't backed up enough yet for this to matter.
        - [x] Live Patroni failover validated 2026-05-26 — two back-to-back `patronictl switchover` runs (vor→fulla→vor), cluster moved cleanly across TL=6→7→8, both promotion + demotion callbacks delivered to Discord. Surfaced the Patroni 4.x `master`→`primary` rename (one-line fix appended to the producer-wiring retro).
    - [x] **5h.2.g** — `terraform/adguard/rewrites.tf` entry for `hermod.niflheim.xiiisins.com → 10.0.11.22`. Done 2026-05-25 (commit 05e1631).
    - [x] **5h.2.h** — Smoketest matrix: 4 positive POSTs (one per tag, representative `type`) from allowlisted source → HTTP 200 + Discord delivery confirmed by operator across all four channels; 1 negative POST from disallowed source → HTTP 403, body not proxied. Caddy JSON access log lands in VL via vlagent (service tag `caddy-hermod`). `@everyone` vs `@here` decision still deferred — one-character Apprise URL parameter to flip when needed. Done 2026-05-25.
- [x] **Phase 5h.3 — Ansible orchestration (Semaphore + drift-check).** ✅ Closed 2026-05-27. Semaphore live in asgard K3s, four templates scheduled, fleet-wide drift-check baseline at zero `changed=`. Full retro: [`docs/incidents/2026-05-27-5h3-semaphore-drift-check.md`](../incidents/2026-05-27-5h3-semaphore-drift-check.md). Sub-phases:
    - [x] **5h.3.a** — Per-host-group playbook restructure landed (commit `aa77592` `feat(5h.3): Ansible orchestration via Semaphore — sub-phases a-d`). 8 × `asgard-<group>.yml` files + `site.yml` orchestrator + cluster-wide `vlagent.yml` / `zabbix-agent.yml`. `test-vault-lookup.yml` deleted. Multi-play files deferred — single-play covers current needs.
    - [x] **5h.3.b** — NetBox `ansible:<group>` tags applied via `terraform/netbox/`; NetBox API token at Vault `secret/ansible/netbox/inventory-token`.
    - [x] **5h.3.c** — `ansible/inventory/netbox.yml` live with `netbox.netbox.nb_inventory` plugin. Cache TTL bumped to 24h (was 4h) after the silent-success class surfaced; cache path aligned across plugin + refresh script + pod mount (commit `9b4ba9a`).
    - [x] **5h.3.d** — Semaphore deployed in asgard K3s (commit `aa77592` initial; many follow-up commits for schema discovery). StatefulSet, **Postgres backend via Patroni VIP `10.0.10.210`** (DB `semaphore`, `dialect: postgres` in `config.json`), inventory-cache emptyDir (Synology DS223J 10/10 LUN cap forced emptyDir over PVC), HTTPRoute on niflheim Gateway, Authentik OIDC, ESO secrets, pod-start pip install of `hvac` + `psycopg2-binary` + `pytz`.
    - [x] **5h.3.e** — Semaphore project + templates + Hermod-summary callback landed via the `terraform/semaphore/` module + manual project setup where the provider gaps required it (commit `a27d5aa`). `asgard-fleet-agents` daily template added during the phase (commit `9b2c15a`).
    - [x] **5h.3.f** — Smoketest matrix passed: clean drift-check is silent + logs to VL; mutating drift produces `alert`; manual apply silent on success; forced failure produces `critical`. Baseline is zero-changes.
    - [x] **5h.3.g** — Single canonical `--check` mode caveats catalogue in [`docs/architecture/ansible-orchestration.md`](../architecture/ansible-orchestration.md) (per-role README pass dropped — patterns uniform across roles, scope decision 2026-05-27). AGH rewrite for `semaphore.niflheim.xiiisins.com → 10.0.20.10` landed in the initial 5h.3.a-d commit (`aa77592`). Post-flight + retro: 2026-05-27.
- [ ] **Jellyfin LXC** (privileged, Urd, QuickSync).
- [ ] **Startpage** (asgard K3s, `apps/`). Personal browser homepage (most-used / daily-use). Pulled into the existing `apex-static` Caddy pod as a second content source — pattern TBD between (1) initContainer `git clone` into `emptyDir` at pod start, refreshed on Flux reconcile, or (2) `git-sync` sidecar polling continuously. Repo is private; pattern (2) needs a deploy key (Vault → ESO → K8s Secret) added before it can start. Phase scope: choose pattern, add the mechanism to `apex-static` pod, expose at apex `/` (Caddyfile `handle` block currently 404s). No new HTTPRoute / no new Service — the existing apex-bare route already serves `xiiisins.com/`.

**Vault-for-Ansible migration (in progress — pattern built D1, secrets migrating individually):**

Per the bootstrap-vs-runtime architecture: bootstrap secrets stay in Ansible Vault permanently (narrow scope: `k3s_token`, RHEL keys, SSH pubkeys for ansible/breakglass users — needed before HashiCorp Vault is reachable). Runtime secrets — those needed *after* a node is up — move to HashiCorp Vault.

- [x] **Build the Vault-for-Ansible lookup pattern** (✅ D1, 2026-05-16). AppRole + `ansible` policy + `ansible-local`/`ansible-awx` roles in `terraform/vault/`. `community.hashi_vault` collection + lookup pattern in `roles/sftpgo/defaults/main.yml`. AppRole bootstrap runbook in this doc.
- [x] **Migrate `sftpgo_admin_password`** → `secret/ansible/sftpgo/admin-password` (✅ D1, proof-of-pattern). Removed from `group_vars/all/vault.yml`.
- [x] **Provision Authentik database via Ansible** → `secret/ansible/postgres/authentik-password` (✅ 2026-05-17 evening). Proves the `postgres_databases` declarative-iteration pattern against a real consumer.
- [x] **Migrate `vault_factorio_operator_password`** → `secret/ansible/factorio/operator-password`. ✅ Closed 2026-05-31 (Wave S3). Existing value **preserved** (already distributed to humans, not rotated) — lifted into HashiCorp Vault; `group_vars/gameserver.yml` now reads via `vault_kv2_get`; the `vault_factorio_operator_password` var dropped from Ansible-Vault (last runtime app secret there). Operator-minted (not TF random) since the value was preserved. Verified live: factorio LXC converge re-provisioned the operator SFTP user from Vault, 1P mirror confirmed matching.
- [ ] **Fix sftpgo role check-mode compatibility.** `roles/sftpgo/tasks/bootstrap.yml` does a POST to fetch an admin JWT, then references `result.json.access_token`. In `--check` mode `uri:` skips POSTs by default, returning a stub dict without `.json` — breaks downstream tasks. Add `check_mode: false` to the token-fetch and any downstream state-reading API calls.

**Reprovision (deliberate, on a healthy cluster):**
- [x] Move Göndul from Urd → Verd (✅ 2026-05-17 morning during asgard rebuild).
- [x] CP VM sizing across all three CPs (✅ 2026-05-17 evening — all three Göndul/Hlökk/Sigrún at 2vCPU/4GB. Forced by Authentik deploy; closes the validation question. Stays at 4 GiB with the Phase 4a taint applied — see High priority section.).
- [x] **Urd hardware refresh** ✅ 2026-05-21. MSI Cubi (i3-1215u + 32GB DDR4 reused + 1TB Lexar NM790) replaces DeskMini JB95 (N5095 + 120GB mSATA). The original 2026-05-14 etcd-storm root cause is removed.
- [x] **Phase 4b — Göndul Verd → Urd** ✅ 2026-05-22. Realises the post-hardware-refresh intent — CP topology now matches the original 2026-05-14 design (Göndul on Urd, Hlökk on Verd, Sigrún on Skuld), valid hardware-wise this time. See incident log entry.
- [x] **Worker rebuild — einherjar-urd template_node** ✅ 2026-05-22. Corrected stale TF `template` / `template_node` references that pointed at Verd's template even though the VM ran on Urd. Doubled as deliberate worker-rebuild path validation.

**K3s role correctness (from 2026-05-17 rebuild + 2026-05-21 Phase 4a + 2026-05-22 worker rebuild):**
- [x] **Config rendering on healthy nodes** ✅ 2026-05-21. `config.yml` split out of `install.yml`; runs always while install + calico still skip when healthy. Config changes now actually render. Discovered during Phase 4a when the new `node-taint:` config wasn't applying.
- [x] **K3s role `lineinfile` task for `/etc/iproute2/rt_tables` needs `create: yes`** ✅ 2026-05-22. Fixed during einherjar-urd worker rebuild — iproute 6.17 doesn't ship the file at `/etc/`, only at `/usr/share/`. Older workers had it from side-effect creation on prior runs. Task now sets `create: yes` + explicit `owner` / `group` / `mode`. CLAUDE.md gotcha added.
- [ ] **Add kubectl-taint convergence task to k3s role.** Companion to the registration-time `node-taint:` config: an idempotent task that runs `kubectl taint node <self> ... --overwrite` if the desired taint state differs from the actual. Closes the gap that bit Phase 4a (config templates only apply at fresh registration, not on existing nodes). Should run on the K3s init node after detect-state confirms cluster reachability.
- [ ] **Default `asgard-k3s.yml` to `serial: 1`.** Currently runs in default-parallel mode. A single role change that triggers `restart-k3s` will fire across all 6 nodes simultaneously — defeating HA. Override path for cluster-from-zero rebuilds: explicit per-invocation `--forks` or `-e ansible_serial=...`. Validated by Phase 4a finding that per-node K3s restart is empirically safe, but simultaneous restart across the cluster is not. Captured 2026-05-22.
- [ ] **Baseline role: tag OS package-update task as `os-updates`, skip-by-default.** Currently `dnf update "*" → latest` + reboot runs on every play. Conflates config-idempotency runs with OS-maintenance runs; the latter should be explicit opt-in via `--tags os-updates` for declared maintenance windows. Captured 2026-05-22. Documented as known behavior in CLAUDE.md gotchas; this task is to change the behavior.
- [ ] **Make restart-k3s handler safe for existing CP members — future-proofing, not blocking.** Phase 4a empirically did NOT trigger "duplicate node name" when the handler fired on healthy CPs. The previous warning was a precaution against fresh-cluster-bootstrap edge cases. **Partially addressed Wave S3 (2026-05-31):** `asgard-k3s.yml` now runs `serial: 1`, so a config change can no longer fire `restart k3s` on all 6 nodes simultaneously (the main blast-radius risk) — restarts walk one node at a time, keeping etcd quorum. Notified handlers were also confirmed to fire correctly under `--tags` (see the HAProxy self-heal item above). Remaining belt-and-suspenders options if it ever bites: (a) `kubectl delete node` before restart, (b) `systemctl reload` where K3s supports it, (c) conditional handler based on existing-member vs fresh-node state.
- [ ] **Backup Flux deploy key.** `flux bootstrap github` reissues idempotently if the deploy key still exists in repo settings, but it's not captured anywhere. Consider an out-of-band backup as a Secret manifest in 1Password, or document the regenerate-on-rebuild flow if leaving as-is.
- [x] **Decide nested-vs-flat Kustomization structure** in `k8s/asgard/infrastructure/` (✅ 2026-05-17 evening — sub-kustomizations per component; forced by Authentik's `configMapGenerator`. Closed during the Authentik deploy. Going-forward standard: every component is a self-contained directory with its own `kustomization.yaml`; parent `infrastructure/kustomization.yaml` only references directories.)
- [ ] **Baseline role: assert SSH hostkeys exist and are non-empty.** Idempotent task that stats `/etc/ssh/ssh_host_{rsa,ecdsa,ed25519}_key`, deletes any zero-byte file (so `ssh-keygen -A` doesn't skip it), runs `ssh-keygen -A` to regenerate missing keys, notifies restart-sshd handler. Same OS-invariant class as the existing `resolv.conf` management. Closes the failure mode that hit einherjar-urd post-NUC7 crash — would have caught the corruption on the very next ansible play after May 20 instead of letting it bide until Phase 4a's reboot. Discovered 2026-05-21 evening.
- [ ] **Automated iSCSI node-record cleanup, or accept periodic manual sweeps.** After every pod migration involving an iSCSI PV, the source worker is left with a stale node record in `/var/lib/iscsi/nodes/`. Options: (a) systemd timer on each worker that diffs `iscsiadm -m node` against `iscsiadm -m session` and removes records with no session AND no PV bound to that worker (cross-checked via `kubectl get volumeattachment`); (b) accept manual sweeps as periodic operational work; (c) check whether Synology CSI's node-stage hooks can be configured to clean up. Defer the decision until enough migrations have happened to feel the pain. Surfaced 2026-05-21 evening — two orphan records on einherjar-skuld from Phase 4a's reshuffle, no active harm but latent footgun if iscsid restarts.

**Authentik-specific follow-ups:**
- [ ] **`/media` PVC for user avatars.** Currently `emptyDir` — pod restart loses uploaded avatars. Pattern when needed: NFS volume from Munin (acceptable here, unlike PG, because `/media` is small reads with rare writes), or revisit the Synology-CSI-RWO-with-spread-affinity question for genuinely multi-writer needs. Defer until users actually upload avatars and complain.
- [ ] **Branding ConfigMap → S3 swap path.** Populator init container is already wired for the flip (busybox `cp` becomes `mc cp`). Pattern preserved as a forward-fit. Trigger: when a branding asset would exceed the 1MiB ConfigMap-per-key ceiling, OR when versioning/rotation of branding assets becomes a thing.

**5b.2 AGH IaC follow-ups (2026-05-25):**
- [ ] **Update 1P "Proxmox API - Terraform" with new rotated token.** Token leaked in transcript 2026-05-25 via failed BSD-sed redaction (the `\s` BSD gotcha — now in CLAUDE.md). New token id `terraform-rot-20260525-055802`, value stashed at `secret/temp/proxmox/api-token-rotated-20260525` (field `full_token`). Procedure: `vault kv get -field=full_token secret/temp/proxmox/api-token-rotated-20260525`, update 1P, then `vault kv metadata delete secret/temp/proxmox/api-token-rotated-20260525`. The new token is already in both `terraform/proxmox/{asgard-lxcs,asgard-k3s}/terraform.tfvars`; nothing breaks if 1P is stale (tfvars is operator-side anyway), but next operator who reads 1P should see the current value.
- [x] **keepalived role: enable script_security.** ✅ Closed 2026-05-25. Role defaults `keepalived_enable_script_security: true` + `keepalived_script_user/_group: keepalived_script`; tasks create the system account before render; template emits `enable_script_security` + `script_user <user> <group>` inside `global_defs`. Both consumers (AGH `chk_adguard`, HAProxy `chk_haproxy`) keep working unchanged — `/usr/bin/systemctl is-active` reads unit state via D-Bus and needs no privileges. Applies on next `asgard-adguard.yml` / `asgard-haproxy-etcd.yml` run; until then the SECURITY VIOLATION log line persists on existing nodes (harmless). CLAUDE.md AGH-gotcha row removed (role now closes it).
- [ ] **NetBox VMID retrofit for Saga/Mimir/Kvasir.** `terraform/netbox/` had empty VMIDs for the AGH trio (manual install). Now they're TF-managed at 1110/1111/1112 — populate via NetBox vms.tf custom_fields. Unblocked by 5b.2.
- [x] **Split Tailscale resources out of `terraform/proxmox/asgard-lxcs/`.** ✅ 2026-05-25. Closed by the `asgard-lxcs-root/` split (see standing-list entry below for full description). This entry was the 5b.2-aged reinforcement.

**Standing:**
- [x] **Cloudflare re-harden after stability.** ✅ 2026-05-24. `terraform/cloudflare/security.tf` imports the zone's auto-created `http_request_firewall_custom` entrypoint (`6604b5a39abe4afc93edb3c437f76e03`) and lands a single skip rule covering `xiiisins.com/.well-known/webfinger` + `authentik.xiiisins.com/application/o/*` (prefix match covers per-app OIDC discovery + token/authorize/userinfo/jwks/end-session — all Authentik OIDC endpoints live under that prefix). Re-enabled `browser_check: on` + `bot_management.fight_mode: true`. Two-phase apply (`--target` ruleset first, then plain apply) so skip rules are live before protections flip — no race window where probes could 403. Surfaced three findings — see incident log entry.
- [ ] **Cached-NXDOMAIN flapping on internal FQDNs — root cause investigation.** Authentik-specific stopgap retired 2026-05-24 by 5g.2 cutover (now points at VIP `10.0.10.210`, same NXDOMAIN-safe IP-form property as the literal-fulla-IP stopgap was — and "use VIP IPs, not internal-zone FQDNs, for K8s-to-LXC service traffic" is now the standing rule for future PG consumers). The underlying class is still latent for any other K8s workload that resolves `*.niflheim.xiiisins.com` (or other internal zones) — pods landing during a brief upstream-DNS unavailable window will cache the NXDOMAIN and serve stale failures even after upstream recovers. Hypotheses to validate if a non-PG case ever fires: CoreDNS NXDOMAIN cache TTL, pod resolver `options ndots`, AGH sync timing on Saga, K3s node `/etc/resolv.conf` fallback.
- [ ] **Authentik force-password-reset-on-first-login.** Authentik has no native flag (upstream issue #19681, still open as of 2026-05-23). Implementation requires custom Expression Policy + Prompt Stage + Stage Binding to the authentication flow. Deferred until needed (single-user homelab + 1P-managed passwords for existing users); revisit when multi-user onboarding starts firing or when upstream lands native support.
- [ ] **Cleanup stale comments after 5e.2.g HTTPRoute migration.** `k8s/asgard/infrastructure/authentik/httproute.yaml` has a comment line that still says "Attach to the niflheim Gateway's HTTPS listener" — fix to reflect midgard. Trivial; bundle with next touch of the file.
- [ ] **Authentik email-change side effect on existing sessions.** 5e.3.b TF apply updated ghost's Authentik email from `xiii@xiiisins.com` to `ghost@xiiisins.com`. Existing Authentik-issued sessions/credentials tied to the old email may need re-issue. Single user, single session, minor — but worth a check that nothing's silently broken.
- [ ] Pin HelmRelease chart versions + activate Renovate. Position revisited after 2026-05-21 Phase 4a — see Architectural decisions section above. Two pins tightened during recovery (`metallb: 0.15.x`, `synology-csi: 0.11.1`); remaining `0.x` pins still floating. Authentik already pinned to `2026.2.3` (identity infra is too important to float). cert-manager pinned to `v1.19.0` and Traefik chart to `40.2.0` from initial 5e.1 deploy.
- [ ] Document the AWS KMS key ARN.
- [ ] Decide which services get external Cloudflare Tunnel exposure (in addition to direct port-forwards via UCG). Standing decision; pick services as their deploy phases land. First entry locked in 5e.2: Authentik. Process going forward — each new service's phase explicitly answers "external via tunnel? yes/no" in its prereqs block.
- [ ] **Retroactive sweep: add file-path header `# <repo-relative-path>` to all source files** (`.tf`, `.yaml`, `.yml`, `.j2`, `.sh`, `.fish`, `.py`) that pre-date the convention (added 2026-05-23). Single sweep commit, grep `head -1 <file>` to identify uncovered files. Convention documented in CLAUDE.md Conventions section.
- [ ] Fallback static HTML doc on Munin for core K3s recovery.
- [x] AdGuard DNS records for new VMs/services as provisioned. ✅ 2026-05-24 — `terraform/adguard/` module (gmichels/adguard v1.7.0) retrofits the 27 hand-created rewrites via TF 1.7+ for_each `import {}` block; new rewrites now land via `locals.rewrites` map edits + `terraform apply`. Provider targets Saga (origin), adguardhome-sync fans out to Mimir/Kvasir. See decisions row "AGH rewrites — Terraform managed via gmichels/adguard, write-to-origin".
- [ ] Proxmox HA for asgard LXCs.
- [ ] Jotunheim K3s cluster.
- [ ] Confirm whether Vault's `tls_disable` posture should change — revisit at Vault hardening.
- [ ] Tighten `terraform/vault/versions.tf` Vault provider pin from `~> 4.0` to concrete. Known pending after the IaC pin policy generalized 2026-05-23. Trivial change; bundle with the next intentional Vault provider bump or do standalone. Same-class `bpg/proxmox ~> 0.77` pins in both `terraform/proxmox/asgard-lxcs/` and `terraform/proxmox/asgard-k3s/` closed in 5e.3.e.i.
- [x] **Tighten `random` provider pin** in `terraform/proxmox/asgard-lxcs/versions.tf` (was `~> 3.5`). ✅ 2026-05-24 bundled into 5g.2.b — pinned to concrete `3.9.0` (lockfile resolution at time of work).
- [ ] **Self-hosted internal repos for source tarballs + apt mirrors.** Surfaced 2026-05-24 during 5g.2 Patroni role write when picking install source. Currently we trust upstream CDNs at fetch time (mitigated by SHA pinning) for: Patroni tarball (GitHub), etcd tarball (GitHub), K3s binary (GitHub), Calico Tigera operator manifest (raw GitHub), PGDG apt repo (postgresql.org), Tigera apt repo, Sealed Secrets release (GitHub), Helm chart repos (varied). Goal: a single self-hosted artifact + apt mirror (Nexus / Pulp / Harbor / Artifactory tier) that proxies + caches these so (a) rebuilds aren't blocked by upstream outages, (b) version control lives inside the infrastructure, (c) supply-chain visibility (one place to audit "what version of X is reachable from infra"). Defer until post-Phase-5 — significant standing infra commitment of its own. Bumps the Patroni `patroni_source_url` (and equivalents for etcd/K3s/etc.) to point at the internal mirror when it lands.
- [x] **Split Tailscale resources out of `terraform/proxmox/asgard-lxcs/` into their own module.** ✅ 2026-05-25. New module `terraform/proxmox/asgard-lxcs-root/` houses the three Tailscale containers + their `random_password` resources under a single root@pam provider (no alias). Main `asgard-lxcs/` module reverts to a single API-token provider — `configuration_aliases` removed from versions.tf, aliased `proxmox.root` provider block removed from provider.tf, `proxmox_endpoint` variable dropped (only the aliased provider consumed it). Naming chose axis-of-split (`-root` references the Proxmox auth class, not LXC privilege — the LXCs themselves are unprivileged) over the originally-floated `-privileged` (technically misleading) and over a Tailscale-specific name (too narrow — future `fuse`/`keyctl`/additional `device_passthrough` LXCs join this module). State migration via `terraform state pull` → local-file `terraform state mv` → push-back to two S3 backend keys (`proxmox/asgard-lxcs/terraform.tfstate` retains 20 resources, `proxmox/asgard-lxcs-root/terraform.tfstate` gets 6: 3 containers + 3 random_passwords). Verified: both `terraform state list` outputs match expectations; old-module `plan -refresh=false` shows only the pre-existing zabbix add; new-module `terraform validate` succeeds (full-refresh plan deferred to operator since it needs `PROXMOX_VE_PASSWORD` env). CLAUDE.md "LXC / infra" + repo-map updated to document the two-module split. Follow-up: consider adding `PROXMOX_VE_PASSWORD|Proxmox - root|password` to the `homelab-env` `__homelab_env_map` so the cache covers root@pam apply paths uniformly with the other auth materials (today it's the only Terraform-relevant cred not in the shim).
- [x] **SFTPGo admin-password → Vault migration.** ✅ Closed 2026-05-31 (Wave S3). The existing on-disk `pwgen`'d value was **preserved** (lifted into `secret/ansible/sftpgo/admin-password` unchanged — not rotated, which avoids an SFTPGo-side API reconcile since `CREATE_DEFAULT_ADMIN` only fires when no admin exists). The role's `admin-password.yml` now reads from Vault (already its `defaults` lookup), asserts non-empty, and removes the legacy `/etc/sftpgo/admin-password.txt`. Operator-minted (not TF-managed) since the value was preserved — see the note in `terraform/vault/main.tf` for converting to TF random + rotation later. Verified live on the factorio LXC: admin auth returns 200 with the Vault pw, on-disk file gone, 1P offline mirror matches.
- [x] **Phase 5.5 — Doc tree restructure.** ✅ 2026-05-24. Migrated `docs/` from flat layout (`homelab-design.md` + `teardown-rebuild.md`) into targeted tree: `docs/architecture/` (hardware, network, identity-secrets, iac), `docs/services/` (synology, asgard-k3s, jotunheim-k3s, asgard-lxcs, factorio, postgres), `docs/operations/` (build-sequence, decisions, open-questions), `docs/incidents/` (per-incident retros, date-indexed README), `docs/procedures/` (teardown-rebuild moved here), `docs/known-issues/` (placeholder — gotchas migration still pending; CLAUDE.md stays canonical for runtime context). `homelab-design.md` collapsed to thin index linking everything. CLAUDE.md rewritten to cross-ref new doc paths, drop duplicated narrative. Phase-structure + content-separation rules in CLAUDE.md "Phase structure & doc separation" updated to reflect new tree.
- [ ] **Migrate gotchas from CLAUDE.md to `docs/known-issues/`.** Now-unblocked follow-up. Currently CLAUDE.md is canonical (for runtime context); `docs/known-issues/README.md` is the placeholder + planned-structure stub. Migration: per-subgroup file (`networking-multi-homed-workers.md`, etc. — see `known-issues/README.md`); CLAUDE.md keeps a condensed version + cross-refs into `known-issues/` for full context. Triggered when CLAUDE.md gotcha section grows large enough that the compression cost outweighs runtime-recognition value.
- [ ] Long-term: migrate from ESO sync-and-cache to Vault Agent / VSO runtime retrieval. Pilot on jotunheim first.
- [ ] Long-term: migrate Vault → OpenBao (~12 months out, once OpenBao has more production track record).
- [ ] **PG PITR via WAL archiving + `pg_basebackup` (Munin NFS).** Deferred backlog item (operator call, Wave S2 2026-05-31). The daily PG-aware backup need is met by the **logical `pg_dumpall` timer** landed in S2 (per-DB custom-format dumps + globals, local 1-day rotation, PBS captures 7d) — that covers the common homelab case (restore one app's DB, version-portable). True point-in-time recovery (restore to any second) needs continuous WAL archiving: the Patroni config already scaffolds `archive_mode: on` with `archive_command: /bin/true` (no-op), so the upgrade is (a) point `archive_command` at a Munin NFS WAL destination, (b) add a periodic `pg_basebackup`, (c) manage WAL retention/disk. More standing infra than "recovery confidence" calls for — revisit when PITR (vs daily restore points) becomes a real need.
- [x] **PG cluster expansion → Phase 5g.2 — PG HA with Patroni.** ✅ Closed 2026-05-24. Pulled forward from "after multiple consumers" to now, driven by operational pressure (Authentik PG-DNS flapping on 2026-05-23 mitigated by literal-IP stopgap; structural fix is the HA cluster + VIP). Final architecture: Patroni-managed PG cluster (Fulla/Vör/Idunn) with etcd co-located on the HAProxy LXC trio (Hlin 1133/Urd, Eir 1134/Verd, Snotra 1135/Skuld), HAProxy VIP `10.0.10.210` floating across the trio via keepalived (eth1 VLAN 10), no pgbouncer (documented decision row with revisit triggers).

    **Step list** (checkbox form per phase-structure rule; commit hashes preserved as historical pointers — the original chat draft used a `5g.2.a/b/c/...` L4 naming that was retroactively recognized as a violation of the L3-max rule. Shipped commit subjects keep the L4 letters as historical record, doc tracking switches to checkboxes going forward):

    - [x] Design + decision rows (commit `3ad9077`, was labelled `5g.2.a`)
    - [x] LXC provisioning — Vör/Idunn + HAProxy/etcd trio + baseline bootstrap (commit `6b9830e`, was labelled `5g.2.b`; also covered what would have been a separate `5g.2.d` for Vör/Idunn)
    - [x] etcd 3-node cluster (commit `f83657a`, was labelled `5g.2.c`)
    - [x] Patroni role + Fulla in-place adoption + Vör/Idunn replica join — cluster `niflheim-pg` live 2026-05-24, 3/3 streaming on TL 1 (fulla Leader on 10.0.11.230, vor + idunn Replicas with 0 lag). Surfaced four implementation gotchas, all now in CLAUDE.md Known gotchas: psycopg3 mixed-types → use psycopg2-binary; postgres `restart postgresql` handler firing on Patroni-managed nodes → handler gated `when: not postgres_patroni_managed`; `bootstrap.pg_hba` is initdb-only → use top-level `postgresql.pg_hba`; Patroni adoption preserves Debian `postgresql.conf` as `postgresql.base.conf` incl. `include_dir 'conf.d'` → conf.d/ must exist on every node. **Deferred to the next step**: fulla has 4 pending-restart params (`max_connections 100→300`, `wal_log_hints off→on` for `pg_rewind`, `cluster_name`, `listen_addresses`) — Patroni adoption kept the running PG at its original runtime values; restart applies on the HAProxy + cutover step where Authentik already reconnects.
    - [x] HAProxy + keepalived role + VIP `10.0.10.210` (commit `9a53544`). Generic data-driven roles (`haproxy_listens` + `keepalived_vrrp_instances` list-of-dicts → caller supplies per group; roles reusable for future VIPs). Dual-NIC HAProxy/etcd LXCs added (eth1 on VLAN 10 for VRRP L2-adjacency at `10.0.10.233/234/235`). VRRP all-BACKUP election by priority 100/90/80, `chk_haproxy` track-script via `systemctl is-active`. Source-based policy routing for VLAN 10 generalised into the keepalived role (extends the existing K3s-VLAN-20 pattern to N-entry). Patroni REST API `/master` health-check + `balance first` for deterministic leader routing; `on-marked-down shutdown-sessions` for clean client handover. Orchestrated `patronictl restart niflheim-pg` ran replicas-first + leader-last (no separate commit, operational) — applied the 4 pending PG params including `wal_log_hints=on`.
    - [x] **VRID 51 → 61 fix** (commit `c38163f`) — discovered at first deploy: AGH keepalived on the same VLAN 10 was at VRID 51, causing continuous "received an invalid passwd" log spam on both clusters. Spacing convention adopted: increment VRIDs by 10 (AGH=51, PG=61, next=71, ...).
    - [x] Authentik cutover: `AUTHENTIK_POSTGRESQL__HOST` from literal `10.0.11.230` → VIP `10.0.10.210` (commit `74f6079`). Retires the IP stopgap from 2026-05-24. Both `externalsecret.yaml` (the value that wins via envFrom) and `helmrelease.yaml` values block (kept in sync) updated. Post-commit: Flux reconcile → kubectl rollout restart deployment on `authentik-server` + `authentik-worker` to pick up new env at pod start. Validated end-to-end (psql via VIP returns leader address; web UI login works; existing sessions survive).
    - [x] **pg_hba localhost replication fix** (commit `d08c192`) — surfaced during failover validation: pg_rewind's local divergence-check connects to `127.0.0.1` as `replicator` for replication protocol, which the existing `host all all 127.0.0.1` rule does NOT cover (`replication` is a separate pg_hba match category). Added `host replication <user> 127.0.0.1/32` + `::1/128` to the patroni role's `postgresql.pg_hba` template.
    - [x] Failover validation. Two scenarios run: (1) graceful switchover fulla → vor (no write blackout, VIP follows, Authentik survives); (2) hard `systemctl stop` of the current leader → election to fulla/idunn within ~30s, VIP follows the new leader via HAProxy `/master` health-check, brief Authentik blip but auto-recovers. Rejoin used the fast-catchup path (no divergent WAL to rewind from, since graceful stop releases DCS lock before any divergence happens) — pg_rewind didn't fire but the config to support it is in place (`wal_log_hints=on` + the pg_hba fix). Conscious decision not to force a true-rewind test via ungraceful crash; operational failover is what mattered and that's validated. Discovered gotcha: `systemctl kill` doesn't actually kill patroni (KillMode=process + Restart=on-failure); ungraceful test requires `systemctl mask` + `pkill -9` on both patroni + postgres.
    - [x] Post-flight docs + commit bundle — this commit. CLAUDE.md (build-status + 7 new gotchas + Postgres section), build-sequence.md (5g.2 ticked with completion summary), this step list, decisions.md (5 new rows: dual-NIC LXCs, all-BACKUP election, VRID spacing convention, HAProxy+keepalived install via apt, generic-roles design pattern), services/postgres.md (HA topology + VIP + failover model live), [incident retro](../incidents/2026-05-24-5g2-haproxy-keepalived-vip.md).

- [x] **Phase 5i — NetBox IPAM/DCIM.** ✅ Closed 2026-05-24. Deployed in asgard K3s. Architecture: NetBox 4.6.1 / chart 8.2.17 (OCI), external PG via HAProxy VIP, bundled Valkey standalone, internal-only HTTPRoute, Authentik OIDC. Initial inventory imported via web UI per [`docs/procedures/netbox-initial-data-import.md`](../procedures/netbox-initial-data-import.md). TF→NetBox standing pattern picked (Option A: TF writes NetBox; tight-coupled; `e-breuninger/netbox` provider) — implementation as Phase 5i.3 next, not deferred-indefinitely.

    **Step list:**
    - [x] **5i.a** — PG DB + dual-path password mint (commit `e21ba0c`). Validated the "future PG consumers via VIP get NXDOMAIN-safety for free" claim from 5g.2. Discovered the `tasks_from:` silently-ignored-in-`roles:`-block bug while validating; fix landed as `postgres-common` role split (commit `69d72d3`) — `postgres` role does prepare-only, `postgres-common` does leader-gated user/DB provisioning.
    - [x] **5i.b** — Authentik OIDC client + groups (commit `b2bbfc8`). New `terraform/authentik/netbox.tf` mirrors `tailscale.tf` pattern. Groups `netbox-admins` + `netbox-viewers` (policy_engine_mode=any for OR semantics). Redirect URI `https://netbox.niflheim.xiiisins.com/oauth/complete/oidc/` (trailing slash mandatory per python-social-auth).
    - [x] **5i.c** — HelmRelease (commit `b9858bb` + 5 follow-up fixes). Iterated extensively: valkey standalone+volumePermissions (`90c6abe`), media emptyDir (`6cf090b`), netbox-app missing keys (`182a06f`), netbox-superuser missing keys (`40a5be3`), resourcesPreset small→medium (`5eb1f0a`), extraVolumes for OIDC secret (`715ab13`). All fixes documented as gotchas in CLAUDE.md "NetBox" section.
    - [x] **5i.d** — HTTPRoute (commit `d797dbc`) + CoreDNS rewrite (commits `44c8f63` + `cd80202`). Surfaced the in-cluster-pod-to-MetalLB-VIP class — generalised existing "Cloudflared targets ClusterIP DNS, NEVER MetalLB IPs" rule via cluster-wide `coredns-custom` ConfigMap. K3s convention: `*.override` keys (plugin directives inside `.:53`) vs `*.server` keys (full new server blocks); got the wrong key first time, hence two commits.
    - [x] **5i.e** — Initial data import via web UI. Procedure documented at `docs/procedures/netbox-initial-data-import.md` (~50 records: site / cluster / manufacturers / device types / roles / VLANs / prefixes / 5 physical devices / 17 VMs+LXCs / 3 VIP records / tags / per-entry IaC cross-references).
    - [x] **5i.f** — Post-flight docs + commit bundle — this commit. CLAUDE.md (build status, IPAM invariant, gotchas across DNS / Networking / Ansible-roles / new NetBox section), build-sequence.md (5i row), this step list, decisions.md (4 new rows: NetBox placement in asgard, TF→NetBox direction, CoreDNS-rewrite for K8s-fronted FQDNs, NetBox extraVolumes pattern), new services/netbox.md, [incident retro](../incidents/2026-05-24-5i-netbox.md), netbox helmrelease `install.remediation.retries: -1 → 3` restored.

- [x] **Phase 5i.3 — TF→NetBox standing pattern.** ✅ Closed 2026-05-24. Implementation lives in `terraform/netbox/` on `e-breuninger/netbox v5.3.0`. Sub-steps:
    - [x] **5i.3.a** — Module skeleton (`terraform/netbox/{versions,provider,main,outputs}.tf`, .gitignore). Concrete-pin e-breuninger/netbox 5.3.0 + hashicorp/vault 5.9.0 + hashicorp/random 3.9.0. Provider env-driven (NETBOX_SERVER_URL + NETBOX_API_TOKEN; v2-token-aware via `nbt_` prefix auto-detect). Stage 0 prereq was the chart-deploy `API_TOKEN_PEPPERS` fix (commit `b576bd2`). Auth strategy PIVOTED from "dedicated terraform user + token" to "reuse admin token throughout" after discovering three provider/NetBox-4.6 incompatibilities — see [decisions.md "TF→NetBox auth model"](decisions.md). Commit `b52f96d`.
    - [x] **5i.3.b/c** — Resource declarations across 8 files: `sites.tf`, `clusters.tf`, `manufacturers.tf`, `roles.tf`, `vlans.tf`, `prefixes.tf`, `devices.tf`, `vms.tf`, `vips.tf`, `tags.tf`, `custom_fields.tf`. ~160 resources total. Driven from the existing IaC locals maps (`terraform/proxmox/asgard-k3s/main.tf` + `terraform/proxmox/asgard-lxcs/lxcs.tf`). Three NetBox-side placement corrections landed (PBS skuld→was-verd, idunn verd→was-skuld, Gjallarbru skuld→was-verd) where the manual 5i.e import disagreed with IaC reality. Commits `dd1774a` (Stage 2), `70701ce` (Stage 3), `6c1d1e5` (Stage 4), `199b94f` (Stage 5), `4ec1762` (Stage 6), `cb55a93` (Stage 7).
    - [x] **5i.3.d** — Retrofit via TF 1.5+ `import {}` blocks (no separate import script needed). 23 imports in Stage 2, 20 in Stage 3, 5 in Stage 4, 72 in Stage 5, 3 in Stage 6, 14 in Stage 7 → 137 imports total. Final `terraform plan` shows zero drift.
    - [x] **5i.3.e** — CLAUDE.md rule added to "Architectural invariants → Identity / DNS / network" section: "every new LXC/VM TF resource MUST have a matching `netbox_virtual_machine` + `netbox_interface` + `netbox_ip_address` declaration in `terraform/netbox/vms.tf` locals". Provider gotchas section added at "Known gotchas → Terraform — netbox provider".
    - [x] **5i.3.f** — Post-flight (this commit). CLAUDE.md (build status, IPAM invariant update, worker memory 4G→8G, repo map, new NetBox + Terraform-provider gotcha entries), build-sequence.md (5i.3 row), this step list, decisions.md (2 new rows: auth model pivot, worker memory baseline), incident retros at [worker-OOM](../incidents/2026-05-24-worker-oom-during-netbox-rolling-restart.md) + [5i3-tf-netbox-retrofit](../incidents/2026-05-24-5i3-tf-netbox-retrofit.md).

  **Carried forward** (5i.3 surfaced but defers to a future phase):
    - Per-resource tag application — Stage 7 imported the 14 tag DEFINITIONS but NetBox has no tag→resource bindings yet. Apply incrementally as resources get touched (set `tags = [...]` in the relevant netbox_* resource block).
    - AGH trio VMID retrofit — Saga/Mimir/Kvasir have empty VMIDs in vms.tf locals (manual install, no IaC). Fill in when Phase 5b.2 (AGH IaC) lands.
    - Provider issue #349 watch — when e-breuninger/netbox gains integer custom_field support, swap VMID back from text → integer and drop the `validation_regex` workaround.

- [ ] **Phase 5i.2 — NetBox recovery LXC (fully independent).** Deferred until after 5i.3 stable. Fully isolated tier: local PG restored from pg_dump-via-PBS, local Redis, local admin auth (no OIDC), `MAINTENANCE_MODE=True`. Zero dependencies on K3s, Patroni, HAProxy, Authentik, Vault. TF + Ansible recipe ready but LXC not pre-provisioned. Runbook at `docs/procedures/netbox-recovery.md` (to write). Trigger: K3s is down, IPAM lookup needed for recovery.

- [ ] **Phase 5i.4 — NetBox → Ansible dynamic inventory.** Deferred until AWX is on jotunheim K3s — avoids doing the migration twice (once for current control-node usage, again when AWX takes over). Per the inventory-research recommendation: phased migration (populate → shadow → parallel-then-flip → prune), 2-3 week activity, `netbox.netbox` collection's `nb_inventory` plugin with `cache: true` + `cache_timeout: 900` + filters `has_primary_ip: true` + `status: active` (silent-skip-on-missing-primary-IP is the #1 gotcha). Keep a small static `bootstrap.yml` inventory forever for chicken-and-egg recovery (NetBox host + deps).

- [ ] **NetBox OIDC group→permission sync via SOCIAL_AUTH_PIPELINE override.** OIDC-created users currently get zero NetBox permissions — operator manually elevates via local-superuser login (admin → Users → check Active/Staff/Superuser). Implementation: custom Python pipeline function that reads `groups` claim from the OIDC token and maps to NetBox group memberships / permission assignments. Mounted via the `extraVolumes`+`subPath` pattern documented in CLAUDE.md NetBox gotchas. **Trigger**: manual elevation per user becomes recurring toil (>3 users). Until then, accept the one-time elevation step per new OIDC user.

- [ ] **Synology DS223J iSCSI LUN cap — investigate + raise.** Surfaced 2026-05-26 during Outline+Garage deploy: provisioning the 11th PVC (`outline-redis` 1Gi) failed with `Failed to create LUN, err: Number of LUN reach limit`. All 10 existing LUNs are legitimate (live PVs, bound, in use). DS223J spec sheets suggest up to 64 iSCSI LUNs, so this is likely a per-Volume cap that's UI-configurable on the Synology — investigation needed (DSM → SAN Manager → iSCSI → LUN limits). Tactical fix in place: Outline Redis on emptyDir (cache-class, OK to lose). Implications: every future K8s app with a small companion PVC (Redis/cache/queue sidecars) is currently blocked from iSCSI. Resolution unlocks: (a) move Outline Redis back to AOF-on-iSCSI for fewer surprises on restart; (b) future apps can default to iSCSI for everything without LUN-budget bookkeeping.

- [x] **postgres-common — bake Patroni leader discovery into the role.** ✅ Closed 2026-05-31 (Wave S3). The `postgres-common` role now self-discovers leadership: a discovery block at the top of `tasks/main.yml` queries the local Patroni REST `/primary` (200 leader / 503 replica) and sets `patroni_is_leader` — but **only when the fact isn't already set**, so a full converge (where the patroni role already sets it from `/cluster`) is unchanged, while a surgical `--tags postgres-common-*` re-run (which skips the patroni role) now self-discovers instead of defaulting false. Removes the operator's manual `patronictl list` + `-e '{"patroni_is_leader": true}'` step. Verified: surgical `postgres-common-users` run on vor+fulla self-discovered leader=vor (ran) / replica=fulla (skipped). The "retry on demotion mid-task" wrapper remains unbuilt — deferred as low-value (a mid-task demotion is rare; re-run picks the new leader).

- [ ] **Long-deploy VAULT_TOKEN + provider-cred re-verification.** Surfaced 2026-05-26 during Outline+Garage deploy: ~6h end-to-end chain, VAULT_TOKEN expired mid-flight (`permission denied / invalid token` on `terraform/cloudflare/`), recovered via `fish -c 'homelab-env; set-vault-token root; homelab-env --refresh'`. Cloudflare API token was also stale + the rotated 1P value invalid (operator-side error). Pattern: any deploy that spans more than ~2h should re-verify every dependent credential before each TF module — Vault token via `vault token lookup`, CF via `/user/tokens/verify`. Long-term shape: a `homelab-preflight` shim that walks every cached credential + validates it, prints a one-line OK/FAIL summary. Defer until it pays back operator time. Could also be a Semaphore template that runs ad-hoc.
