<!-- docs/known-issues/dns-adguard.md -->

# Known gotchas — DNS / AdGuard Home

*Migrated from `CLAUDE.md`. Recovery commands + rules. Incident retros in [`../incidents/`](../incidents/).*

## DNS

- **Public resolvers as DNS fallback poison internal-zone resolution.** Cloudflare/Google return NXDOMAIN for `*.niflheim.xiiisins.com`; glibc + CoreDNS cache NXDOMAIN as authoritative. Fix in `roles/baseline/tasks/main.yml`: `manage_resolv_conf: false` cloud-init drop-in + templated `/etc/resolv.conf` from `baseline_nameservers`. Fallback resolvers may point at peer AdGuard (`10.0.11.201/202/203`), NEVER public.
- **AGH sync interval `*/30 * * * *` is too long** — 30min lag during keepalived failover serves stale answers. Recommended `*/1`. Sync is cheap + idempotent. Role default is now `*/1` (5b.2, 2026-05-25).
- **A new external apex record (`*.xiiisins.com` via `terraform/cloudflare/`) can be NXDOMAIN-cached by AGH + clients if the name was ever queried before the record existed.** Public DNS (1.1.1.1/8.8.8.8) returns the Cloudflare edge IPs immediately, but a resolver that cached the pre-creation NXDOMAIN keeps serving it for the **apex SOA negative-TTL = 1800s (30 min)**. Two layers cache it independently: **(1) AGH** — flush via `curl -u "$ADGUARD_USERNAME:$ADGUARD_PASSWORD" -X POST http://<node>/control/cache_clear` on **all three** nodes (each has its own cache: `10.0.11.201/202/203`); diagnose with `dig home.xiiisins.com @10.0.10.200` showing `status: NXDOMAIN` while `@1.1.1.1` is `NOERROR`. **(2) the client OS** — macOS `mDNSResponder` holds its own negative cache that `dig` (which queries the nameserver directly) bypasses but `curl`/browsers (via `getaddrinfo`) do not; symptom is `dig` resolves yet `curl` returns `000`. Flush: `sudo killall -HUP mDNSResponder` (needs sudo — not runnable from a bg job). **Isolation trick:** `curl --resolve <host>:443:<cf-edge-ip> https://<host>/` bypasses ALL client DNS and proves the tunnel→Traefik→pod path independently of any cache. Both layers self-heal within 30 min; flush only matters when you need the new name live *now*. Surfaced 2026-05-31 (startpage deploy).
- **`adguardhome-sync` "Sync done" with sub-microsecond duration is NOT trustworthy.** 230ns reads as "no diff" — sometimes accurate, sometimes stale-cached. Diagnose by comparing `/control/rewrite/list` on origin vs replicas directly; if origin has the rewrite but sync reports no diff, restart `adguardhome-sync.service`.
- **K3s `coredns-custom` ConfigMap: `*.override` vs `*.server` keys matter.** K3s's shipped CoreDNS deployment imports `data` keys ending in `*.override` INSIDE the default `.:53` server block (use for plugin directives like `rewrite`, `forward`, `hosts`), and `*.server` keys at TOP LEVEL (use for full new server blocks, e.g. `myzone.:53 { ... }`). Putting a bare plugin directive in a `*.server` key is a syntax error at top level and CoreDNS silently rejects the file — queries fall through to default forwarding and return REFUSED for unmatched zones. Verify the corefile in the cm + the deployment's mount of the custom CM exist before assuming K3s wires it automatically.
- **In-cluster K8s-fronted FQDNs: use a CoreDNS rewrite, not hostAliases.** When a pod needs to reach an internal FQDN (e.g. `authentik.midgard.xiiisins.com`) that AGH resolves to a MetalLB VIP for external clients, the pod→VIP path tromboning kills the connection (same class as the existing "Cloudflared targets ClusterIP DNS, NEVER MetalLB IPs" rule). Cluster-wide fix in `k8s/asgard/infrastructure/coredns-custom/`: `rewrite name exact <fqdn> traefik.traefik.svc.cluster.local` — CoreDNS resolves the rewritten name to Traefik's ClusterIP at query time (ClusterIP-volatility-safe), pod connects to Traefik with the original SNI + Host header, Traefik routes by hostname to the actual backend. External clients (browsers, tailnet devices) unaffected (they bypass CoreDNS, resolve via AGH directly). **Do NOT rewrite FQDNs that point to LXCs/VMs** — those aren't behind Traefik. One rewrite per K8s-fronted internal FQDN; grow the list as more land.

## AdGuard Home

- **AGH admin user is `ghost`, not the role-default `admin`.** Both the `adguard` role's config template (renders `users[0].name`) and the `adguardhome-sync` role's auth (sync→AGH API basic auth) default to `admin` — works for fresh installs but breaks against the migrated manual trio where the operator's username is `ghost`. Override in `ansible/inventory/group_vars/adguard_hosts.yml`: `adguard_admin_user: ghost` + `adguardhome_sync_admin_user: ghost`. The 1P "Adguard - admin" item stores `username: ghost, password: <plaintext>` — `username` is part of the credential, not always `admin`. Symptom of mismatch: sync reports `HTTP 401, username: admin` in journal. Surfaced 2026-05-25 during 5b.2.d.
- **Rotating the AGH admin password is NOT a plain `adguard` role re-run.** The role's `AdGuardHome.yaml` render task only fires `when: not <file exists> or adguard_force_overwrite_config` — deliberately, to protect the live dynamic state (rewrites/filters/clients) AGH itself writes back into that same file — so on an already-bootstrapped node, pushing a new hash to Vault and re-running the role is a silent no-op (`changed=0`, no error). **Do not set `adguard_force_overwrite_config: true` to force it** — that re-renders the *entire* file from a template that only captures listen/upstream/admin/retention, wiping everything AGH has accumulated at runtime. Instead, edit the live file surgically: `ssh` in, back up `/opt/AdGuardHome/AdGuardHome.yaml`, replace just the `password:` line under `users:` in place, `sudo systemctl restart AdGuardHome.service`, on all three nodes (Saga/Mimir/Kvasir — each has its own independent copy). Two sub-gotchas hit doing this live:
  - **sed delimiter must not collide with the bcrypt hash's own charset.** `htpasswd -B` output uses alphabet `./0-9A-Za-z` and commonly contains literal `/` — `sed -i 's/^    password: .*/.../'` breaks (`unknown option to 's'`) whenever the generated hash happens to contain one. Use `|` (or any delimiter guaranteed absent from bcrypt output) instead: `sed -i 's|^    password: .*|    password: "<new-hash>"|'`.
  - **Comparing the file's password hash against Vault's stored hash is never a valid "did it work" check.** bcrypt salts randomly per invocation — two independent `htpasswd` runs of the *identical* password essentially never produce byte-identical hashes. A hash mismatch here is expected, not a failure signal. Verify by testing the actual login instead: `curl -X POST http://<node>/control/login -d '{"name":"'"$ADGUARD_USERNAME"'","password":"<new-plaintext>"}'` → `200`. Once confirmed live, re-sync Vault's copy to the *file's actual current hash* (read it back from the node), not the originally-generated one, so a future rebuild seeds the right value. (Contrast: AppRole secret_ids and plain API tokens ARE deterministic strings, so hash-comparing those to verify a 1Password/Vault write *is* the correct pattern — the distinction is salted-hash-of-a-secret vs the-literal-secret-itself.)
  Also worth noting while editing live: this version of AGH re-serializes the whole config file with its own YAML writer on every state change, and does **not** quote the password value the way the original Jinja template does — don't assume quoting either way when grepping/extracting it back out. Surfaced 2026-09-03 during a full-fleet credential rotation ([incident](../incidents/2026-09-03-claude-transcript-secret-leak.md)).
- **Migrating manual AGH → role-managed: brief dual-MASTER VIP window is unavoidable** when the manual keepalived's `auth_pass` differs from the Vault-minted role one (always true — manual is operator-set, role generates random 8-char). Each side rejects the other's adverts on auth, both think no peer, both claim MASTER. ARP responds twice for the VIP, DNS queries may hit either node. Bounded duration: from "role's keepalived deploys on first non-current-VIP-holder" until "role's keepalived deploys on current-VIP-holder". For the AGH trio that was ~30 min during 5b.2. **No mitigation needed** if Mimir/Kvasir converge BEFORE Saga (current VIP holder), chk_adguard track-script verifies AGH service health on each peer, and AGH is up + serving on at least one node during the window. Eventual election after Saga's role-keepalived deploys is clean (priority 100 wins).

